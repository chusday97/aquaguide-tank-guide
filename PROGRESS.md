## 当前任务目标
收口核心鱼缸事实链路：把“现实中已经在缸里”和“未来准备养”拆成两套明确流程，停止生成虚假鱼缸资料，并让创建与新增生物统一通过 Repository 命令保存。

## 已完成
- 状态：`核心鱼缸事实链路已完成并通过独立 Critic 与 Evaluator 六维验收`。`record_existing` 先保存事实再评估，任何四态结果均不能回滚现实记录；`planned_addition` 先评估且不写鱼缸，只有用户明确确认已实际入缸才进入记录流程。新鱼缸不再生成尺寸、水体、温度、设备或换水伪数据；创建和新增生物均通过 Repository 命令，旧 `add-species` 深链兼容映射到规划流程。云端新增使用原子 RPC 提交父物种、批次和幂等结果，404/409/503 语义保持清晰。（commits: `56c486b`, `7284008`, `f04f189`, `8d272aa`, `9accb48`, `f97e6ca`, `3af5c61`）
- 状态：`已合并并推送 GitHub main`。`main` 从 `de61600` 安全快进到已验收 P0 `153695e`，远端无独立提交、无冲突、无历史改写；功能分支 `codex/activation-evaluation-v1` 继续保留作为阶段记录。（本次仅补交接记录）
- 状态：`体态编辑未保存退出竞态已修复`。体态任务不再因父层异步状态同步而覆盖正在编辑的局部选择；数量、日期和体态选择会同步标记草稿，Esc 由草稿所有者优先打开确认。中文手机的部分数量调整及英文 600px 的继续编辑/放弃修改真实 Chromium 路径均通过。（commit: `4e11196`）
- 状态：`鱼缸时间线与循环养护本地闭环已完成，云端待真实 Supabase 验证`。鱼缸标题右侧直达 `/aquarium?action=timeline&tank=<id>`；建缸、设置、加减生物、体态、换水、喂食、巡检和计划完成按时间倒序展示，旧数据标记“由旧记录整理”。页面不再绕过 Repository 直接写本地时间线；云端完成循环计划改为单个数据库事务，同时完成当前项、生成下一期并登记幂等结果，避免半完成状态。（commits: `0112998`, `2b32de9`）
- 状态：`缸内体态三步任务已完成`。体态调整改为“选择数量 → 选择体态 → 核对保存”；部分数量更新会在服务层自动生成独立批次，界面不再要求用户理解“拆分”术语。水草、硬景和珊瑚不显示繁殖状态；手机与 600px 英文窄桌面验证总数量不变、未保存退出确认和无横向溢出。（commit: `4c027b5`）
- 状态：`今日推荐收藏已贯通游客/登录 Repository 并验证失败回滚`。推荐收藏/取消均停留当前物种，心形原位切换并显示 loading、成功或失败；只有“换一个”消耗每日队列。收藏写入不再只调用本地服务，登录模式通过 API Repository；真实 Chromium 注入本地存储失败后确认心形、持久化状态和“换一个”均正确恢复。按钮静态审计覆盖 13 个正式页面与共享组件共 49 个交互表面。（commits: `13c6994`, `cd66bc3`）
- 状态：`按钮门禁已补运行时结果验收`。静态审计现扫描 50 个正式页面/共享表面，运行时再遍历 14 个正式路由检查页面错误和无名称按钮，并真实点击验证 `route / view / mutation / dialog / section / external` 六类结果。审计同时发现并修复图鉴原位筛选缺少 `aria-expanded / aria-controls` 的状态契约。（commit: `1f6a51e`）
- 状态：`养护动作已逐项绑定候选来源，人工审核仍是内容发布门禁`。修复“缸内有斗鱼时水浑也追加躲避物”的跨问题条件错误；41 篇内容的立即动作、禁止动作、观察、复查和下一步共生成 228 条稳定动作依据，页面在动作旁直接显示候选来源和审核状态。Critic 发现关键词曾错误授予“已审核”，现改为显式人工登记：必须记录审核人、时间及核对过的来源 ID；当前尚无可验证人工登记，因此 228 条全部保持待复核，不能包装成可靠发布。快速评测结果已改为直接展示处理步骤、禁止动作和复查目标。（commits: `1367000`, `9fe79df`, `b8627ad`, `3da4587`）
- 状态：`养护提醒已统一 Repository`。文章详情设置提醒不再直接写本地服务，游客走 Local Repository、登录用户走 API Repository，并具有 loading、防重复、失败保留面板和可见错误；鱼缸页的本地 care activity 订阅仅在 local 模式刷新，不能覆盖已加载的云端提醒。（commit: `b8627ad`）
- 状态：`P0 独立 Critic 最终复验通过`。审查推动关闭云端 Repository 绕过、循环完成非原子、动作来源粒度、体态重试操作号、推荐收藏云端/回滚和按钮仅静态审计六类问题；二次审查再关闭养护提醒直写与关键词自证审核，`3da4587` 清除最后一项来源无关文案。Critic 最终裁决本地 P0 PASS；真实 Supabase 和 228 条动作人工审核继续作为外部门禁。
- 状态：`独立 Evaluator 最终验收通过`。Evaluator 对照原始 P0-1 至 P0-4，从需求、逻辑、边界、代码、测试和实际运行六维裁决本地 P0 PASS；发现并修正 `CARE_GUIDANCE_EVIDENCE_AUDIT.md` 遗留的旧 35/6 数字。验收不包含真实 Supabase、专家内容审核、视觉模型、真机或真人效果。
- 状态：`当前 P0 已发布生产预览`。Vercel deployment `dpl_Dsw3wUTfmaHWZw3v2BPQhtCysJrq` READY 并已别名到 `https://ice-glide.vercel.app`；`/aquarium` 返回 200，HTML 加载 `index-0k6PQE5x.js`。生产站时间线专项及 14 路由、六类动作运行时验收通过。发布时修正了本地 Vercel 旧绑定 `directory: "."` 与 CLI 58 的校验冲突；未修改数据库或业务数据。
- 状态：`混养结论已启用审核证据门禁`。`Aggressive` 不再等于捕食者，旧 `housingReason` 不再直接阻断；虎皮鱼与迷你鹦鹉鱼使用追鳍、追逐、领地和繁殖防御资料推断，界面显示审核状态与外部来源。486 个物种全部进入审核清单，目前 3 个物种画像和 1 条特殊配对规则已审核，剩余 483 个在行为资料影响结论时安全降级为资料不足。（commit: `985da98`）
- 状态：`2.5.0 可信证据与时间线契约已完成`。新增混养证据来源、物种审核画像、特殊配对规则、养护步骤引用、时间线事件来源和循环养护字段；公开 API 映射审核证据并保持现有页面兼容。三层契约、API 类型、lint 和生产构建通过。（commit: `716fdad`）
- 状态：`GitHub 全量同步与最新生产部署已完成`。当前分支完整推送到 `origin/codex/activation-evaluation-v1`；最新构建发布于 `https://ice-glide.vercel.app`。首次发布发现 `/aquarium` 深链接 404，补充 Vercel SPA rewrite 后重新部署，现 `/aquarium` 返回 200 并加载 `index-BtS9X_Qg.js`。原 Cloudflare Pages 地址仍是旧版本，不作为当前验收地址。（commit: `44fd894`）
- 状态：`激活与会话事件基线已完成`。应用启动、真实鱼缸完整混养、识别/分诊和受控 AI 任务通过同一会话事件适配器记录；只接受安全白名单字段，不记录自由描述或结构化答案。旧用户和已有有效记录不补发首次激活。`lint`、API 类型检查、生产构建、事件白名单、引导激活和混养专项均通过。（commit: `a29d7df`）
- 状态：`今日推荐回迁与常用操作直显已完成`。今日推荐使用现有每日 10 个稳定队列回到鱼缸首页第 3 区，图鉴删除重复卡；图片卡保留进度、详情、换一个和收藏，详情关闭返回鱼缸首页。第 2 区六个常用操作全部直显，不再进入“更多工具”。`lint`、`build`、推荐深链、手机优先级、首页 C、折叠门禁及 7×17 全路由响应式回归通过。（commit: 待本阶段提交）
- 状态：`阶段 3 结构化 AI Evaluation 已建立`。既有 47 个程序化场景迁移为版本化 JSONL，新增 Zod Case/Result/Badcase 契约、deterministic/mocked/live 三类 Runner、统一指标报告和失败回流脚本；视觉 manifest 保持空白并明确未校准。（commit: 待上一阶段提交）
- 状态：`阶段 2 识别/分诊隔离门禁已补齐`。确认候选继续先到独立识别完成页；手动搜索与视觉候选复用同一确认函数；只有 describe/question 且已有症状文字时保护离开，非鱼类不能进入鱼类分诊。（commit: 待本阶段提交）
- 状态：`阶段 1 激活任务契约已补齐`。既有真实鱼缸适配门禁保持不变，任务 ID 改为明确的 setup/choose/complete 语义；新增专项覆盖两条目标顺序、收藏不能代替适配、浏览路线不强制巡检、历史用户保护、旧引导兼容和清单单一来源。（commit: 待本阶段提交）
- 状态：`阶段 0 证据基线已建立`。新增证据矩阵、H1–H5 产品假设、真人测试空结果门禁和 AI Evaluation 状态页；当前产品状态与路线图明确区分自动测试、真实模型与真人证据，并明确当前没有激活率、TTV、留存或视觉准确率结论。（commit: 待本阶段提交）
- 状态：`物种分类边界与今日推荐已通过 Critic 与 Evaluator 最终裁决`。486 条物种全部进入审计；修正丁香珊瑚被名称规则判成水草、两条五彩青蛙被两栖关键词误判、珊瑚获得鱼类/小缸标签，以及三个人工覆盖项英文回退中文。当前派生出 40 条珊瑚生命类型、鱼类角色泄漏 0；61 条旧来源分类差异仅进入人工复核。今日推荐有搜索/筛选时隐藏，直达详情关闭安全回到图鉴，站内关闭恢复位置；真实浏览器覆盖收藏后推进、已收藏进入水族册和 390/600/1280px 命中。（commits: `38ba7b4`, `28f58ed`, `a81131a`, `e26f278`）
- 验证：`test:taxonomy`、`audit:taxonomy`、`test:taxonomy-ui`、今日推荐深链浏览器专项、`lint`、`check:api`、`build`、`git diff --check` 均通过；Critic 六维复验与 Evaluator 用户路径最终裁决均 PASS；构建仅保留既有大代码块警告。
- 历史决策：2026-08-01 曾将常用操作拆成首层三项和“更多工具”，并把完整推荐移到图鉴；该页面归属已被 2026-08-02 用户确认的新决策替代，手机页头的鱼缸切换、新建和“更多”仍保留。
- 状态：`拍照识别与健康分诊已拆分`。确认候选后先进入独立识别结果，展示物种、图像证据、收藏/缸内状态和鱼缸上下文；混养判断、物种资料与健康分诊成为三个不同动作。非鱼类禁用分诊但仍可查看资料，未保存离开保护只在症状描述和追问阶段生效。390px 真实 Chromium 拆分专项、完整识别回归、lint 和 build 通过。（commit: 本次功能提交）
- 状态：`新手引导核心价值闭环已完成`。建缸与浏览两条路线改为目标专属四步；收藏或加入物种不再冒充混养完成，只有与真实鱼缸关联的完整混养计算记录才能完成该步骤。欢迎页移除过早下载，新手清单仅在完成至少一项任务后从步骤区下载。（commit: 本次功能提交）
- 状态：`统一导出与分享中心及清晰 PNG 已完成`。新增 `/aquarium?action=exports` 任务页，集中展示健康评分、诊断结果、本周计划、新手清单、鱼缸档案和百日纪念六类导出，并承接 7 天脱敏链接；设置页提供明确入口，上下文下载按钮改为文字动作。定位到原 PNG 全透明根因是离屏 foreign-object 渲染，现六类记录卡改由固定 1080px Canvas 直接绘制，绕开 oklch/透明画布。真实下载为 1080×1000，RGB 对比度标准差 41–47；390/600/1280px 无溢出。（commit: 本次功能提交）
- 状态：`今日推荐物种深链已完成`。鱼缸首页继续保留每日 10 个推荐队列，但“查看物种详情”进入 `/encyclopedia?species=<id>&source=daily-discovery`，复用正式物种档案；删除首页专属重复详情弹窗。关闭档案后返回鱼缸首页，390/1280px Chromium 专项通过。（commit: 本次功能提交）
- 状态：`养护筛选原位化已完成`。删除养护指南遗留且无真实入口的遮罩式 FilterBottomSheet 和重复草稿状态；分类、搜索、收藏继续在原页面更新，并在分类区提供明确“清除全部”。中英文 390/600/1440px 真实 Chromium 均无页面错误与横向溢出，清除后恢复 41 篇完整列表。（commit: 本次功能提交）
- 状态：`反馈入库与邮件直送已完成`。设置页反馈提交先写入 `feedback_submissions`，再由后端通过 Resend 尝试发送；正文经过 HTML 转义，邮件请求以反馈 ID 幂等。邮件未配置或失败不丢失数据库记录，界面明确显示“已保存，邮件暂未送达”；配置成功时显示已送达。`lint`、API check、业务 API 契约与邮件投递专项通过。（commit: 本次功能提交）
- 状态：`结构化死亡原因与现代化录入已完成`。记录死亡和生命纪念编辑支持最多五项预设原因，“暂不确定”保持独占，“其他”才要求自定义文字；旧原因文本兼容读取。日期改为今天/昨天/自选，批次、数量和拆分数量改为可见选择卡与步进器，记录流程不再打开第二个嵌套 Dialog。云端 API、原子 RPC、本地 Repository、成就追溯和详情展示已贯通；`lint`、API check、build、纪念服务与成就测试通过。（commit: 本次功能提交）
- 状态：`2.4.0 契约完成，等待功能层实现`。生命纪念新增最多五项受控原因代码，`unknown` 只能单独选择，旧 `reason` 保留为其他原因兼容字段；反馈新增 `not_configured / sent / failed` 邮件投递状态。数据库 migration 同步扩展纪念原子 RPC 与反馈投递元数据，`lint`、API check、业务 API 契约、三层契约和 diff check 通过。（commit: 本次契约提交）
- 状态：`已完成，Critic 与 Evaluator 最终 PASS`。生命纪念不再把“已填写现象/改进但未填写原因”误标为未记录；原生确认改为项目内确认框，应用内导航与浏览器返回均保护未保存草稿。英文今日推荐不再混入中文结构字段，体态观察主操作会把确定性观察重点带入巡检。旧七参数纪念 RPC 在 migration 中显式删除，避免 PostgreSQL 重载遗留。`lint`、`build`、API check、体态服务、养护分类/引导及真实 Chromium 首页与水族册回归通过；独立裁决确认六项原计划均形成闭环。（commit: `df1532b`）
- 状态：`中英文养护分类一致性已完成并推送`。分类、搜索与单篇详情状态分离，页面使用 `new_stock / water_quality / fish_health` 等稳定 ID；显示语言不再参与文章集合判断，匹配始终读取中文基准字段。“新鱼入缸”中英文均返回同样 2 篇，分类点击不自动打开单篇，关闭文章后恢复原分类。分类单测、双语浏览器回归、八类养护语义、四类文章详情、lint 与 build 通过。（commit: `054916e`）
- 状态：`设置页桌面化已完成并推送`。删除大型渐变宣传头和移动卡片堆叠；900px 以上使用紧凑双栏工作区，左侧分类直接定位通用、新手引导、分享隐私和意见反馈，右侧语言单行切换、分享状态行与反馈表单。手机保持单列，600px 英文桌面与 390px 手机无横向溢出，未提交反馈保护及失败保留输入均通过。（commit: `e20adc9`）
- 状态：`生命纪念独立档案页已完成并推送`。新增 `/collection/memorial/:recordId`，旧 `?item=` 深链自动兼容跳转；列表与水族册首页均直达独立页面，不再打开内容弹窗。旧记录可直接补录或编辑“当时现象 / 可能原因 / 后续改进”，写入统一 Repository，保存后刷新仍存在；再次加入继续进入统一混养复核。`lint`、`build` 与 1440/600/390px 水族册浏览器回归通过。（commit: `2728359`）
- 状态：`体态编辑与观察任务已完成并推送`。缸内物种不再打开嵌套大弹窗；同一任务表面用图标卡选择幼年/成年/未知、用状态胶囊选择怀孕/抱卵、生产/繁殖和产后恢复，支持拆分、合并、删除、修改摘要与未保存退出确认。确定性体态信号会补充今日巡检或在巡检后生成观察任务；生产/繁殖优先，但体态字段被门禁禁止直接进入健康评分，混养规则保持不变。批次测试、任务闭环、首页及引导浏览器回归通过。（commit: `fd5776d`）
- 状态：`首页内容收口已完成并推送`。今日推荐显示稳定的 `N / 10` 进度，同日刷新保持当前候选，图片上的收藏与切换按钮不再被媒体层拦截；首页删除进阶水质检测和“另有 N 种 / N more species”占位卡，手机常用操作保留六项真实功能但移除重复辅助说明。320–1440px 浏览器回归、lint 与 build 通过。（commit: `8ecfb58`）
- 状态：`生命纪念契约与视觉基线已完成并推送`。`DeceasedRecord`、数据库、API、Repository 与本地兼容存储增加“当时观察 / 可能原因 / 后续改进”，旧记录仍可读取；三套桌面/手机方向已归档，正式实现采用 A「纪念档案」。（commit: `b79793f`）
- 状态：`独立 Critic 安全阻塞已修复，等待同线程复验`。删除普通用户分享表通用 UPDATE RLS，撤销改为 Express 核对 owner 后使用服务端凭据写入；公开成功/错误响应均 `no-store`。设置撤销增加不可恢复二次确认，文件名清洗覆盖特殊字符和空值，公开报告与分享管理补齐中英文。真实 PNG 下载已验证为 `1080×651`，并修复 html2canvas 不支持 `oklch` 导致的实际下载失败。（commit: 本次审查修复提交）
- 状态：`脱敏报告前端闭环已完成，本地验证通过`。鱼缸档案可生成 7 天分享链接并在创建后复制；游客会得到登录/服务配置提示。公开 `/report/:token` 不进入私人应用壳，只显示脱敏健康、环境、物种和本周计划，并可再次下载 PNG。设置页列出本人报告的有效/过期/撤销状态并支持撤销；原令牌仅在创建时返回，刷新后不尝试从哈希恢复。390/1280px 公共报告浏览器回归、隐私字段扫描、lint、API check 与 build 通过。（commit: 本次前端提交）
- 状态：`七天脱敏分享 API 已完成，本地契约验证通过`。创建链接使用 32 字节 HMAC 派生令牌并只保存 SHA-256 哈希；同一用户、鱼缸和幂等键可安全重放。列表与撤销要求登录，公共读取使用 service role 只查询白名单快照，并明确区分无效、已撤销和已过期状态而不返回报告内容。隐私扫描确认自定义鱼缸名、用户 ID、自由描述、AI 原文和内部 ID 会被契约剥离。（commit: 本次 API 提交）
- 状态：`PNG 导出阶段已完成，本地验证通过`。健康评分、诊断结果、本周养护计划、新手开缸清单、鱼缸档案和百日纪念共用固定导出模板；导出不包含自由描述或 AI 原文，并统一标注“基于用户记录、非智能设备实时检测”。旧鱼缸日期推算后必须确认才解锁百日卡。`lint`、API check、build、导出模型专项和 390/1280px 健康卡预览浏览器验证通过。（commit: 本次功能提交）
- 状态：`导出与分享契约已完成并推送`。新增六类导出模型、建缸日期来源字段、七天分享快照契约、`aquarium_share_reports` 表与 owner RLS。（commit: `54f11d6`）
- 状态：`已完成，Critic 与 Evaluator 最终 PASS`。统一详情、任务和确认表面的返回/关闭语义与 44×44px 控件；物种喂养及环境摘要直接显示；养护自查明确全缸、单种和多种范围；首页今日行动、单条计划、缸内物种和今日推荐改为紧凑直显；折叠只允许次级证据、进阶数据、超长列表剩余项和备选方案。审查补充关闭八类养护语义、安全动作直显、完整动作列表、英文确认、任务未保存退出保护；生成结果但未保存时也会拦截退出，保存后可直接关闭。`lint`、`build`、折叠、八类养护、视觉结果、物种详情、首页、任务闭环及 7×17 路由响应式回归通过。（commits: `d06f7df`–`392e829`）
- 状态：`Critic 首轮问题已修复`。手机今日推荐恢复横向双列并在 320–430px 保持 200px 内；多物种自查中心改为“所选 N 种生物”；八类问题分别生成对应的立即动作、禁止动作和复查项，水质自查不再复用浮头语义；折叠审计覆盖 `<details>` 与 `aria-expanded`；7×17 路由浏览器扫描同时验证可见图标按钮至少 44×44px。`lint`、`build`、首页、任务闭环、四类养护、物种详情、视觉结果和按钮审计均通过。（commit: `9faecad`）
- 状态：`阶段 5 已完成`。建立折叠用途白名单 `secondary_evidence / advanced_data / overflow_list / alternative_plan`，为物种证据、结果依据、知识扩展、新手完整路线、进阶检测和备选方案标记用途；知识型养护正文改为直接显示。新增 `test:disclosures`，阻止喂养摘要、安全动作、首页核心模块和单条内容重新进入折叠。同步修复物种风险主操作只切换隐藏视图、不更新正式地址的问题，现直达 `/encyclopedia?mode=compatibility`。（commit: `b29e902`）
- 状态：`阶段 4 已完成`。鱼缸首页删除手机“管理 / 学习”整区折叠，核心任务始终直显；今日行动删除重复“为什么”，紧急依据最多直显两条；单条养护计划直接提供指引、完成、改期和删除，多条时只折叠其余项。缸内预览使用“条/只”和“另有 N 种”，风险禁止动作不再折叠。（commit: `b1da90c`）
- 状态：`阶段 3 已完成`。养护自查新增当前会话范围选择：环境问题默认检查整个鱼缸，行为、体态和喂养问题明确选择“全缸 / 单种 / 多种”；展示和规则只消费用户选择的对象，禁止动作直接显示。（commit: `c908fce`，语义补强 `9faecad`）
- 状态：`阶段 2 已完成`。物种档案将推荐食物、频率、份量与禁忌移到名称之后直接展示；适配结论后增加水温、水体、空间、换水和观察摘要，删除“养护要点”总折叠，只保留适配、混养和公母资料等次级证据。浏览器专项已最终通过。（commit: `35927a8`）
- 状态：`阶段 1 已完成`。新增共享 `SurfaceHeader`，物种档案和缸内物种详情统一使用右上角 44px 关闭按钮；默认 Dialog 关闭按钮同步扩大到 44px 并补焦点样式，避免标题栏重复关闭和触控区域过小。（commit: `d06f7df`）
- 状态：`已完成，Critic 与 Evaluator 最终 PASS`。统一任务路由覆盖创建、完善、添加、巡检、换水、缸内物种、混养和设置；41 篇养护文章按内容用途分为操作 13、诊断 11、护理 4 和知识 13。操作与护理完成状态刷新可恢复，空清单不可保存；两题全“不确定”进入资料不足并以补充检查为唯一下一步；复查提醒在可处理结果区显示日期；知识文章没有伪勾选，水族册已收藏文章无无效按钮，换水文章进入统一鱼缸记录。生产构建及四类型、任务闭环、手机养护、三步交互、国际化和全路由响应式回归通过。（commits: `c4a3832`–`f30529d`）
- 状态：`已完成，本地验证通过`。水族册首页不再用整块模块按钮包裹预览：种草 3 个、养护 2 篇、生命纪念 2 条和勋章 2 项均可通过 `?item=<id>` 深链直接打开或定位；模块标题与仅在有剩余内容时出现的“更多 N 项”进入完整列表。种草按稳定插入顺序倒序、养护按 `favoritedAt`、纪念按记录日期排序，浏览详情不会重排；失效 ID 会清除参数并显示错误。`lint`、`build`、8 枚成就规则和 1440/600/390px Chromium 深链、刷新、关闭及返回回归通过。（commit: `e58b438`）
- 状态：`已完成，Critic 与 Evaluator 最终 PASS`。关闭“一屏物种名片”首轮审查的 3 个阻塞：手机首屏压缩图高和间距，三条原因完整位于固定主操作上方；异常温度/空间/设备指标恢复键盘可用的精准设置定位；风险状态删除混养折叠区的重复同目标 CTA。同步删除整套 `{false && ...}` 旧详情，并把真实滚轮与父页面位置比较加入筛选回归。`lint`、`build`、物种详情与筛选 Chromium 专项通过。（commit: `e1553c3`）
- 状态：`已完成，Critic 与 Evaluator 最终 PASS`。物种详情重构为“一屏物种名片”：大图、名称、当前鱼缸结论、最多三条原因和唯一主操作进入首屏；删除重复适配结果页签，适配依据、混养关系和养护要点改为默认收起的原位展开区。无比较对象时混养显示资料不足。状态化物种详情 Chromium 与 390/1280px 边界检查通过，无页面错误或横向溢出。（commit: `e80ec5f`）
- 状态：`已完成，Critic 与 Evaluator 最终 PASS`。图鉴原位筛选增加独立滚动边界，内部滚到顶部或底部后不再把滚动传给下方物种列表；保持原位半下拉、无遮罩且不锁页面。搜索/筛选 Chromium 使用真实滚轮确认父容器与物种结果位置不变。（commit: `fa8fd1f`）
- 状态：`已完成，本地验证通过`。我的水族册首页四格由空白模块卡升级为真实数据预览：种草展示 3 个物种，养护展示 2 篇收藏文章，生命纪念展示 2 条日期与复盘状态，成就展示最近解锁与下一项进度；每格保留唯一“查看全部”路径，空状态有明确下一步，今日种草不进入水族册。`lint`、`build`、8 枚成就规则与 1440/600/390px 浏览器布局和四路由点击回归通过。（commit: `c8172f4`）
- 状态：`已完成，本地验证通过`。建立确定性共享搜索索引，按完整名、前缀、别名、学名、名称包含和结构化关键词排序；相似物种按 ID 保留，最多先显示 8 个。全局搜索、桌面侧栏、图鉴、养护和识别手动搜索已接入两步确认；图鉴四组筛选改为工具栏下原位展开，关闭草稿不改结果，零结果不可应用。侧栏异步加载具备可见 loading 和失败降级提示。单测、lint、build 与分组 Playwright 覆盖中文单字、英文长名称、键盘 Enter、390px 无溢出、侧栏、养护和识别降级路径。（commits: `c740dc7`, `5343d80`, `a1bca37`）
- 状态：`已完成，Critic 与 Evaluator 最终 PASS`。关闭物种详情独立审查发现的动作与本地化缺口：指标改用稳定 `type` 而非翻译标签匹配；水族册无鱼缸、加入鱼缸和已拥有物种的主按钮均进入真实目标；鱼缸来源的已拥有物种原位进入养护要点；pH/硬度低优先级提示只保留在折叠依据。手机适配页签只保留一个始终可见的吸底主操作，结果卡不再重复按钮。Evaluator 两轮复验推动“不建议”按钮改为“查看风险与替代建议”，并补银龙鱼与极火虾真实捕食组合，确认视觉状态、文案、完整混养落点和直接加入阻断。状态化浏览器回归覆盖六状态、中英文指标一致及 Manage/Learn 深链接；生产预览核心体验、首页 C、14 项混养、lint/build 与 7×17 路由扫描通过。（commits: `2c80073`, `440376d`, `b289098`, `b3c34b0`）
- 状态：`已完成`。新增全局响应式路由扫描，覆盖 390/600/820/1024/1440px、中英文、13 个正式页面与操作/诊断/清单/知识四类养护详情，共 119 个浏览器场景；页面错误、整页横向溢出和非滚动容器内控件越界均为 0。同步更新核心、手机养护和国际化浏览器回归以匹配唯一缸内物种弹窗与新详情关闭语义，并清除登录和混养中的三个条件表达式字符串。lint/build、按钮审计、国际化、手机养护、核心体验、首页 C、可视化结果和混养规则均通过。（commit: `79ba243`）
- 状态：`已完成`。单物种详情采用 A「图像结论」：桌面中央 900px、手机 92dvh 满宽底部面板；首屏以大图、名称/学名/标签、当前鱼缸适配结论、最多三条关键原因和固定唯一主操作为核心。页签统一为“适配结论 / 混养关系 / 养护要点”，收藏、混养和更多操作降为次级；无鱼缸时不显示伪适配原因。图片使用失败占位解析，英文页签和次级按钮可换行。lint/build、可视化结果和 14 项混养规则通过；390px 英文、600px 英文和 1280px 中文真实 Chrome 无页面错误与横向溢出。（commit: `59168b7`）
- 状态：`已完成`。桌面 600–1023px 现在自动使用 76px 图标侧栏，1024px 恢复用户原有手动侧栏偏好；真实手机首页第 2、3 模块改为原位任务折叠，深链接会自动展开、定位、高亮并聚焦。英文手机底栏固定使用 `Tank / Species / Care / Collection`，全局按钮与页签补充长文本换行规则。真实 Chrome 验证 600/768/820px 均保持桌面结构且无横向溢出，390px 英文手机无按钮溢出；lint/build 与首页 C 深链接回归通过。（commit: `d326dc2`）
- 状态：`已完成`。恢复未完成翻译改动造成的运行与类型基线：养护详情不再因 `buildCareGuide()` 读取未定义 `isEn` 崩溃，物种详情、混养、搜索、登录、AI 助手、加热提醒和鱼缸配置中的国际化作用域/条件表达式已修正。`npm run lint`、`npm run check:api`、`npm run build` 通过；中英文手机直达新鱼过水详情均打开中央/底部详情表面且无页面错误。（commit: `1823b88`）
- 状态：`本地预览 Goal 已完成`。同一 Critic 在两轮复验中关闭正整数、稳定幂等重试、pH 非阻断、导航清理和反馈限流问题；Evaluator 对首页 C、缸内物种、安全移出、风险三步、侧栏切缸/重命名、反馈和 pH 路径最终判定 PASS。登录云端生产仍以真实 Supabase migration、RLS、并发和回滚为发布门禁。（commits: `5f3084c`, `本次文档提交`）
- 状态：`代码与本地重放契约已完成，真实 PostgreSQL 待外部环境`。同一次移出确认草稿现在持有稳定操作号，失败重试复用且锁定原数量；只有取消后重新发起才生成新号。API 已移除 RPC 前的活跃父记录检查，使“最后批次已软删除”仍能先命中数据库幂等记录并返回当前鱼缸。新增重放顺序专项和浏览器复跑。（commit: `d681ecd`）
- 状态：`已完成，待真实 Supabase 执行`。独立审查发现云端按数量移出在响应丢失时可能重复扣减；现改为 `remove_aquarium_species_batch_quantity` 数据库事务函数，在同一事务内锁定批次、扣减/软删除并写入幂等记录。移出数量在弹窗、本地服务、共享契约、API 和数据库均限制为正整数，小数输入不再出现界面数量与实际扣减不一致。（commit: `b3d8d01`）
- 状态：`已完成`。删除首页遗留的第二套 pH 区间阻断逻辑，普通物种继续只按水体类型、温度、空间和确定性混养规则判断；pH 仅作为敏感物种的可选检测提醒。同时修复侧栏鱼缸快照订阅清理函数错误返回布尔值的问题。（commit: `5dc8900`）
- 状态：`已完成`。反馈提交增加代理感知且容量有界的单实例限流，设置页在空内容提交时聚焦错误字段，失败后保留正文，离开未提交草稿前确认；浏览器回归覆盖确认取消/继续。多实例共享额度明确交由生产入口网关。（commit: `2544ef6`）
- 状态：`已完成`。回归审计发现桌面侧栏若只读游客 localStorage，会在云端 Repository 模式显示旧鱼缸；现改为页面发布、侧栏订阅的会话级导航快照，本地与云端鱼缸列表、当前选择和重命名均同步，首页浏览器回归复跑通过。（commit: `5535a02`）
- 状态：`已完成`。设置页新增意见反馈：用户选择建议/问题/纠错/其他并提交 10–2000 字正文，系统自动附带页面、语言、版本和设备布局，不采集联系方式、鱼缸参数或诊断原文；Express 支持游客提交、可选登录归属、每小时限流，管理员可分页查看和更新状态。API 类型检查、业务契约、生产构建、390/1280px 成功与失败恢复 Playwright 通过；真实 API 已验证 400 与未配置数据库时的明确 503。（commit: `5e38185`）
- 状态：`已完成`。桌面鱼缸切换移入左侧栏并使用 `/aquarium?tank=` 直达当前鱼缸，页头删除重复切缸菜单；鱼缸名称支持行内编辑，通过 Repository 保存，具备 loading、防重复、成功/失败反馈，侧栏和首页即时同步。生产构建与真实浏览器切缸/重命名回归通过。（commit: `26258bf`）
- 状态：`已完成`。风险说明改为可执行处理向导：明确受影响物种、建议调整数量、三步操作和禁止动作，唯一主操作直达缸内物种或鱼缸设置；普通物种不再因缺少 pH 进入资料不足，敏感物种仅收到可选检测提醒，水体环境只派生酸/中/碱倾向而不虚构数值。兼容性 14 场景、生产构建和 320–1440px 首页 Playwright 通过。（commit: `4174cda`）
- 状态：`已完成`。首页 C 将缸内物种预览并入第一步，第二步与第三步按 900px 内容容器并排/降列；点击预览打开统一中央弹窗，物种卡右上角可按批次和数量安全移出，确认前提示转缸、送养与禁止放生，移出不创建生命纪念。批次、Repository、生产构建和 320–1440px Playwright 回归通过。（commit: `8204664`）
- 状态：`契约已完成，功能实施中`。确认意见反馈是本轮唯一新增持久化实体，新增 `feedback_submissions` 表、RLS、共享校验与数据库类型；物种移出复用现有批次，pH 只使用不持久化的水体倾向派生模型。（commit: `a432139`）
- 状态：`已完成实现，静态独立复验通过，最终浏览器补跑待恢复`。鱼缸首页按“观察 → 管理 → 学习与养护”重组：鱼缸画面与今日行动并列，缸内物种与常用操作并列，今日种草与新手基础信息并列；新手起步压缩为进度条、唯一下一步和可展开四步；pH、氨氮、亚硝酸盐仅进入默认折叠的可选进阶说明。首轮浏览器复验发现并修复 600px 控件重叠/裁切、320px 新手入口裁切、DOM/视觉顺序和 Manage 空白；同一 Critic 静态复验确认代码与增强断言关闭问题。修复前后已有 1440/1000/600px 桌面、真实 iPhone、布局/国际化/按钮回归证据；专项已补入空鱼缸和 1/2/4 物种预览、添加与展开场景，但最后小改后的浏览器补跑因当前 Codex 审批额度限制尚未执行，必须补跑最新 `test:aquarium-home-c` 后才能宣称最终运行通过。（commits: `d3c396a`, `b5e499c`, `7b9f10a`, `dd12821`）
- 状态：`已完成（本地预览）`。补齐审查发现的云端/本地一致性：登录切换不会绕过本地迁移确认；批次拆分、同会话改体态再合并与生命纪念均使用事务/幂等边界；响应丢失或幂等日志中断后可恢复最后一条纪念；旧 debounce 快照不再覆盖刚保存的体态。真实 Chromium 已覆盖新手建缸、侧栏搜索/识别/设置、390px 拆分合并、600px 英文与未保存修改确认；Critic 与 Evaluator 均通过。（commits: `67ecc0d`, `cb9d413`, `9121f47`, `3c7e95e`, `984c555`, `79ebd56`, `394cd83`）
- 状态：`已完成`。侧栏与手机页头的搜索、拍照识别、养护指南和设置均进入正式路由；新增 `/search`、`/settings`、`/welcome`，搜索结果通过稳定 ID 打开详情并恢复查询上下文；首次引导直达真实建缸或新手物种列表，鱼缸首页四步任务由真实记录自动推进。（commits: `551f34a`, `6b95493`, `b19280e`）
- 状态：`已完成，待真实 Supabase 验证`。新增 `aquarium_species_batches` 契约、迁移、RLS、父级数量触发器和批次 API；游客模式支持旧记录回填、修改、拆分和确认移除，登录 Repository 逐批次同步；体态进入观察、养护推荐与症状上下文但不改变混养结论。（commits: `5c3032d`, `e079432`, `e0c91ca`, `5dc311c`, `70b22cd`）
- 状态：`已完成，待真实登录会话验证`。新手引导状态在本地保存并同步现有 `profiles.preferences.onboarding`；新设备仅在本地没有选择时读取云端，失败时保留本地状态并给出提示。旧侧栏语言设置浮层已删除。（commits: `315e0d9`, `22a3876`）
- 状态：`已完成`。新增任务式导航/体态浏览器回归，覆盖首次建缸直达、搜索/拍照/设置路由、390px 批次拆分、600px 英文桌面无溢出；修复唯一缸内物种模块的英文硬编码。lint、build、批次、Repository、业务 API、混养、三步路径和双语浏览器回归通过。（commit: `6f49f80`）
- 状态：`已完成`。已固定物种识别与动态症状判断契约：视觉模型与文本 AI 分离，图片只在内存处理；新增匿名未命中聚合表及无客户端 RLS 策略；共享类型覆盖候选、观察、确定性追问、原因排序和紧急动作。`npm run lint`、`npm run check:api` 与 `git diff --check` 通过。（commit: `2ec147d`）
- 状态：`已完成`。已实现视觉图片内存去 EXIF/缩放、OpenAI 兼容视觉调用、匿名未命中会话/云端降级、症状观察提取和动态诊断 step API；确定性规则覆盖红旗优先、信息增益选题、最多三问、紧急动作前置与三档原因排序。（commit: `9da053a`）
- 状态：`已完成（可交付本地预览）`。`/identify` 已接入图鉴，覆盖图片上传、手动兜底、候选详情、鱼类类别确认、物种+鱼缸结构化上下文、动态单题追问、连续死亡红旗、环境视觉节点和可展开原因排序；AI 观察值限制在登记枚举内。Critic 首轮发现上下文未消费、非鱼类误诊、请求竞态和导航保护缺口，Builder 经三轮修复后由同一 Critic 静态复验通过；Evaluator 发现鱼缸页残留旧关键词追问，已删除并将鱼类异常入口统一导向 `/identify`，复验确认唯一实现缺口清零。lint、API check、build、14 个规则/映射场景、上一版真实手机/英文桌面与 600–1440px 浏览器主流程通过；API 已验证 400/413、未配置视觉降级和全缸急促呼吸紧急分诊。最新 Back/Forward Playwright 已编写但因 Codex 审批额度耗尽尚无运行证据；真实视觉准确率和 Supabase migration/RLS 仍待外部验证。（commits: `1e96d31`, `802e655`, `cdeec7f`, `c9fa49a`, `f1c535e`）
- 状态：`已完成`。混养完整计算、图鉴 Mini、物种适配、每日巡检和养护自查统一接入“视觉结论层 + 折叠证据层”：真实物种大图突出当前关注对象，小图切换关联原因，首屏只保留结论、现在先做和一个主操作，完整依据默认折叠；图片失败独立占位，不影响页面。390px 手机与 600/820/1200px 桌面无横向溢出或页面错误；lint、build、可视化/混养/Mini/巡检专项、核心与三步浏览器回归通过。（commit: `ec36e6b`）
- 状态：`已完成`。当前功能分支 11 个本地提交已推送至 GitHub，远端与本地同步到 `ead449b`；随后新增中英文国际化首个里程碑：浏览器语言首选、`aquaguide_locale` 持久化、桌面左下角与手机鱼缸页头设置、即时切换、`html lang` 更新、四张翻译表/RLS、内容 API `locale` 与中文回退元数据、登录用户 `/api/v1/profile` 语言偏好接口。同步修复手机养护推荐轮播真实宽度超屏但被裁切的问题。lint、API check、build、24 表契约、API 边界、手机养护与桌面/手机双语浏览器回归通过。（commits: `d542fd0`, `3619e05`）
- 状态：`已完成`。图鉴普通物种卡新增独立快捷收藏，品类只允许具体变种逐个收藏；统一收藏服务即时同步水族册、计数和成就，失败会回滚并提示。修复桌面反馈条被侧栏遮挡与品类弹窗焦点不稳定，新增桌面/真实手机浏览器回归。（commit: `5f84ff1`）
- 状态：`已完成`。鱼缸首页把养护计划内嵌到“今日行动”，移除独立计划卡；正式“缸内物种”入口收敛为鱼缸画面下方唯一可展开区域。每日检查选择后自动聚焦下一道有效问题，最后只聚焦结果按钮；公开 AI 入口只保留 AI 建缸助手和异常/自由描述巡检解读。（commit: `5f84ff1`）
- 状态：`已完成`。18 张中优先级图片已逐项人工复核：16 张主体完整并继续使用非破坏性显示保护，`sp_0357` 与 `sp_0452` 确认需要重新生成；新增真人新手、真实鱼缸跨入口与低端真机 3D 外部证据协议，不虚构外部结果。（commit: `cf5bea2`）
- 状态：`已完成`。完成本轮全量回归：混养、Mini、收藏、水族册、生命纪念、业务状态、每日检查、按钮、AI 建缸契约与 20 场景评估、3D 素材、设备布局、会话事件、响应式图片、三步路径及桌面/手机浏览器核心路径全部通过；同步修正旧回归脚本中已失效的水族册 query 页签和逐题巡检断言。文档链接检查覆盖 36 个 Markdown 文件并通过。（commit: 本次提交）
- 状态：`已完成`。每日巡检从 7 个逐题页面改为六个必答项加可选描述的单页检查表；养护问题自查从四阶段改为“单页回答 → 结果”；添加生物的混养复核进入独立第二屏。新增 12 条正式路径登记、三步上限断言和真实浏览器验收，`npm run lint`、`npm run build`、`test:three-step-paths` 与 `test:three-step-ui` 通过。（commit: `a52181e`）
- 状态：`已完成`。手机种草与养护收藏入口统一进入“我的水族册”，水族册增加四类二级说明；图鉴分页恢复为首页、上一组、组数、下一组、尾页单行布局，320/375/390/430px 均无折叠或横向溢出；养护推荐取消自动轮播与相邻卡片露出并移除重复底部安全区。桌面鱼缸恢复常驻切换、新建和缸内物种入口，手机 3D 全屏预览补充物种列表。养护详情重做 A/B/C 三套桌面与手机 Demo，正式采用“大图 + 三步指引”的 A「图示操作台」。旧提醒在原存储键上升级为应用内养护计划，支持明确日期、鱼缸绑定、今日/即将到期/逾期、完成、改期和确认删除，不新增提醒中心或系统通知。`npm run lint`、`test:state-services`、`test:product-actions`、`test:core-ui` 与 `test:mobile-care-ui` 通过。（feature commit: `4dce9b8`）
- 状态：`已完成`。鱼缸、图鉴和水族册按内容容器完成 600–1440px 窄桌面重排，桌面缩窄不再显示手机工具栏；勋章页补充“自动解锁、无需领取”说明，并统一显示三态、当前、目标、差值与唯一下一步。鱼缸、巡检和养护活动的业务写入收口到服务层，保留原存储键并统一派发更新事件；每日检查新增正常、浮头、泡沫、异味、浑浊、拒食和多异常样本。云同步仅完成方案评估，3D 与物种数据完成事实基线记录。全量 lint、build、专项测试与最新源码核心浏览器路径通过。（commit: `5b3f0d5`）
- 状态：`已完成`。审计 486 张物种图片并生成可视化报告与返工队列：0 个高优先级问题，18 个中优先级候选，其中浅色主体低对比 11 张、主体贴边 5 张、主体碎片化 2 张；未覆盖用户原图。2D 卡片与 3D 版本化素材 URL 一致性测试通过。（commit: `844a190`）
- 状态：`已完成`。收口单物种详情主路径与生命纪念：无鱼缸时只显示一个“尚未选择鱼缸”状态；图鉴、鱼缸和水族册的“检查混养”都会保留当前物种并进入完整计算；已有温度、水体、尺寸和设备问题会打开并高亮对应鱼缸设置，修复了设置 hash 在鱼缸数据加载前失效的问题。死亡记录改为填写日期和原因、确认后才通过统一纪念服务写入，保存后派发应用状态事件并同步鱼缸、水族册与勋章；重复关闭按钮已移除。`npm run lint`、`npm run build`、纪念/成就/按钮专项测试、完整核心浏览器回归及真实点击验收通过。（commit: `4ede92a`）
- 状态：`已完成`。桌面物种、养护、纪念和水族册详情统一改为中央弹窗（最大约 900px / 88dvh），真实手机继续使用底部面板；养护百科移除强制最小画布和旧网格跨行拼接，按内容容器在双栏与单列间切换，600–1440px 无横向裁切。正式详情采用“结论先行”方案，并交付 A 结论先行、B 双栏指南、C 任务清单三张 1440×900 高保真 Demo。`npm run lint`、`npm run build`、`git diff --check` 和桌面/手机浏览器表面验收通过；构建仍保留既有两个大代码块警告。（commit: `f38d1bd`）
- 状态：`已完成`。建立完整产品文档基线：定义层覆盖 PRD、用户故事、官方竞品资料与逐模块现状；设计层覆盖正式信息架构、物种详情与核心路径交互、现有视觉令牌、浏览器数据模型和受控 AI/API；开发层覆盖技术架构、模块边界、本地运行与 QA；规划层记录窄桌面、按钮闭环、死亡确认、写入服务化、3D 与云同步卡点。`/3d-demo` 明确为内部实验，概念图中的“设置、数据统计、提醒中心”未进入正式架构；两份旧报告标记为历史审计。（commit: `d8f85b3`）
- 状态：`已完成`。上线“我的水族册”四合一页面：种草、养护收藏、生命纪念和成就勋章共用 `/collection`，旧收藏路由兼容跳转；8 枚勋章从既有数据自动追溯，不新增持久化集合。物种、养护和纪念详情按设备使用桌面右侧抽屉或手机底部面板，添加、巡检、设置和 AI 建缸助手进入任务流程表面；用户界面已统一移除 Copilot 旧名称。新增成就规则测试、按钮语义审计及桌面/手机核心浏览器验收。（commit: `006ec2d`）
- 状态：`已完成`。确认 `main` 与 `origin/main` 同步到 `8f5ea46`，建立 `codex/aquaguide-ux-refactor` 分支；完成自然水族册、分层交互、核心路径和技术设计文档，并在 `CONTRACT.md` 固定水族册与派生成就契约。确认不新增数据库、API、字段或持久化集合。（commit: 本次提交）
- 状态：`已完成`。完成核心验收与会话级事件漏斗：仅记录收藏页访问、Mini 结果与跳转、每日检查开始/完成、补救文章打开等动作白名单，不保存自由描述或问卷答案，也不新增持久化数据。浏览器验收覆盖桌面 600px 仍保持桌面结构、iPhone 手机结构、iPad 桌面结构、独立收藏页、Mini 选择传递和每日巡检单条写入；物种与养护详情关闭后的来源焦点/高亮断言已加入脚本。基础核心验收已通过，新增详情断言因本轮工具额度限制未完成最终浏览器复跑；`npm run lint`、`npm run build`、`test:session-events` 与 `git diff --check` 通过。（commit: `4416e34` / `9e49b6d`）
- 状态：`已完成`。完成每日鱼缸检查与受控 AI 解读：我的鱼缸核心操作区显示“今日未检查 / 今日已检查 / 建议重新检查”，巡检覆盖呼吸、水体、泡沫/油膜、异味、行为、近期操作和自由描述；本地规则先给出风险、立即动作、禁止动作和候选百科，仅异常或自由描述时调用 `tank_daily_check_interpretation`。前后端均约束 AI 不得降低风险或推荐候选外文章，失败时保留本地结果；补救文章在检查结果上层打开，关闭后返回结果。同缸同自然日巡检通过 `diagnosisRecords` 更新原记录，不新增存储集合，也不保存 AI 原始回复。`CONTRACT.md` 与诊断类型已同步；专项测试覆盖未配置、超时、任务非法、降级风险、虚构文章及跨日记录策略。`npm run lint`、`npm run build`、服务端语法检查与 `test:daily-check` 通过。（commit: `008a2ee`）
- 状态：`已完成`。完成图鉴 Mini 混养判断：统一规则引擎新增 `species_only` 范围，只比较所选生物的水体、温度、pH、性情、单养和捕食关系，不读取当前鱼缸；Mini 只显示所选对象、四态结论和一条主因，唯一主操作进入完整 `tank` 计算并通过会话级选择服务保留组合，不提供直接入缸。专项测试覆盖兼容、谨慎、不建议、资料不足及选择顺序，既有 11 项完整混养测试无回归；`npm run lint`、`npm run build` 与 `git diff --check` 通过。（commit: `dec28d1`）
- 状态：`已完成`。新增独立 `/wishlist` 与 `/care-favorites` 路由：页面只展示收藏数量、收藏内容、删除操作和空状态，不带入图鉴筛选、今日种草或养护推荐；桌面侧栏与手机端明确入口均改为直接路由。物种和养护详情在收藏页内打开，关闭后恢复原卡片与滚动位置；继续复用既有收藏服务、旧存储 key 和共享详情实现。`npm run lint`、`npm run build`、`test:favorites` 与 `git diff --check` 通过。（commit: `c3f3cc9`）
- 状态：`已完成`。完成设备级布局壳分离：启动时按真实设备识别 `phone / desktop`，iPhone 与 Android 手机进入 `MobileAppShell`，iPad、Android 平板、桌面和桌面触控设备进入 `DesktopAppShell`；判定不监听窗口宽度，桌面缩窄仍保留桌面侧栏与桌面画布。新增功能对照文档和 8 项设备策略断言，`npm run lint`、`npm run build`、`test:layout-mode` 与 `git diff --check` 通过。（commit: `dccbece`）
- 状态：`已完成`。修复 3D 鱼缸素材不同步：鱼、水草与硬景统一使用 `getSpeciesDisplayImage` 解析覆盖素材，纹理基于解析后的版本化 URL 加载，并为每个渲染实例克隆和释放纹理，避免继续复用旧贴图；新增 3D 素材一致性断言。`npm run lint`、`npm run build`、`test:3d-assets` 与 `git diff --check` 通过；Three.js 代码块体积仍为既有约 939KB。（commit: `c26b2d6`）
- 状态：`已完成`。新增 AI 建缸 Copilot 自动评估集，覆盖缺失信息、候选安全边界、非法动作、模型虚构候选、AI 未配置、网络失败、超时与非法 JSON 等 20 个场景；全部通过，当前通过率 100%。（commit: `5933bd8`）
- 状态：`已完成`。完成 P1 返回上下文与养护分页收口：详情关闭后等待页面完成两帧布局再恢复右侧工作区滚动位置，来源焦点恢复时提供短暂高亮；养护百科翻页统一定位结果区，避免翻页后停留在旧位置。`npm run lint`、`npm run build` 与 `git diff --check` 通过。（commit: `1ebbc91` / `9e49b6d`）
- 状态：`已完成`。完成 P0 按钮行为第一轮全量审计与清理：逐页检查混养、鱼缸、图鉴和养护页面的主要操作，确认不存在空 `onClick`、仅 `console.log` 或原生 `alert` 的死按钮；今日建议将重复打开同一详情的“开始处理”改为任务型真实动作，换水任务可直接写入记录并即时反馈，其余任务只保留一个主动作；图鉴详情/选择计算及养护打开/收藏因结果不同予以保留。`npm run lint`、`npm run build`、`git diff --check` 及浏览器动作验证通过；长页面定位与返回上下文归入 P1。（commit: `d2b9353`）
- 状态：`已完成`。完成 P0 弹窗层级与互动降噪：混养提醒合并重复结论和风险说明，详细依据收敛为单一层级；自定义底部弹窗 footer 回归容器布局，不再用全局 fixed 覆盖正文；通用关闭按钮统一 40px 点击区，筛选弹窗补充对话框语义和焦点管理，大图预览打开/关闭会正确转移并恢复焦点。`npm run lint`、`npm run build`、`git diff --check` 和筛选弹窗安全边距浏览器检查通过；构建仍保留既有物种库与 Three.js 大包警告。（commit: `9bd217f`）
- 状态：`已完成`。已完成混养决策流闭环收口：混养结果的信息不足动作可直接定位鱼缸设置的尺寸、参数或设备区域；添加成功后可回到当前鱼缸；鱼缸页支持相应设置 hash；专项断言确认同一输入经统一引擎、结果页决策和添加复核返回一致状态。`npm run lint`、`npm run build`、11 项混养专项断言及图鉴混养页面 smoke 均通过；构建仍保留既有大包警告。（commit: `65e6574`）
- 已完成 Copilot 候选与 fallback 安全策略：模型返回的候选 ID 会再次限制在本地安全/可调整候选池，虚构的缺失问题和不可执行动作会被丢弃，动作文案由本地固定；AI 未配置、超时或非法响应时，fallback 会根据真实缺失信息和候选池选择“完善信息 / 查看候选 / 重新描述目标”，不再固定套用同一答案。`npm run lint`、`npm run build` 与 Copilot 契约断言均通过。（commit: 本次提交）
- 已接通 AI 建缸 Copilot 的三个真实产品动作：完善信息会打开并定位鱼缸设置；查看候选只展示模型所选且仍存在于本地安全/可调整候选池的物种；模拟添加确认会再次经过统一物种添加服务，阻断或缺信息时不会写入真实鱼缸。`npm run lint` 与 `npm run build` 已通过；本次浏览器验收因当前 in-app browser 安全策略禁止访问 `localhost:3000`，未执行页面点击验证。（commit: 本次提交）
- 已固定 AI 建缸 Copilot 的三步数据契约：目标、补充答案、鱼缸摘要、本地安全/可调整候选与阻断原因使用独立结构传输；前后端统一为 `goalUnderstanding / missingQuestions / recommendedActions / selectedCandidateIds / blockedExplanation`，鱼缸页不再读取旧 `reply / nextAction / safeCandidates` 字段，候选只按本地规则 ID 池匹配。`npm run lint` 与 `npm run build` 已通过；新增契约断言脚本，但本次因运行环境禁止 `tsx` IPC 且沙箱外授权额度受限，未实际执行该专项脚本。（commit: 本次提交）
- 已完成 AI 对外入口审计与收敛：移除页面层旧“智能混养推荐”死弹窗和 `recommendation_assist` 排序解释入口，独立 `AIAssistant` 保持无路由状态；页面仅保留鱼缸页“AI 建缸规划”和规则详情中的低频“让 AI 帮我解读”。新增入口策略文档与自动断言，`npm run lint`、`npm run build`、`test:ai-entry-policy` 已通过。（commit: 本次提交）
- 已恢复物种喂养资料的来源与复核状态：同步脚本保留 `sourceUrl/sourceFields/needsReview/reviewReason`，模糊继承与规则模板不会再伪装成已核验事实；486 个物种中 438 个命中结构化资料、48 个明确降级、161 个进入待核验队列，并生成全量 `docs/species_knowledge_audit.csv`。类型检查、构建、物种知识、分类和混养专项测试均通过。（commit: 本次提交）
- 已修复鱼种详情的混养与喂养资料映射：混养 Tab 改为当前物种与缸内其他活体逐对调用统一规则并展示状态、依据和行动；空缸明确显示尚未评估；`housingReason` 仅作为类别参考。喂养 Tab 不再把 `housingReason` 当造景资料或把 `description` 当风险，新增资料来源状态与专项断言；`npm run lint`、`npm run build`、`test:species-knowledge` 已通过。（commit: 本次提交）
- 已为混养组合清空增加二次确认：显式“清空”和不建议结果的“重新选择组合”都会说明将移除的物种数量，确认后才清空，取消会保留当前组合；浏览器验收确认 2 种组合取消清空后仍保留，`npm run lint` 与 `npm run build` 已通过。（commit: `7207b76`）
- 已将图鉴普通物种卡的详情交互收敛为单一入口：图片和标题改为纯展示，仅保留明确的“查看详情”按钮，避免同一卡片三个重复入口与返回焦点不确定；`npm run lint` 与 `npm run build` 已通过。（commit: `279add2`）
- 已统一图鉴与鱼缸物种详情的返回上下文：普通物种卡、品类卡、品类变种、鱼缸 3D 生物、缸内档案和种草列表在打开详情前记录右侧工作区位置与来源焦点，关闭后恢复；品类切换到具体物种详情不再使用盲目动画帧延迟。`npm run lint` 与 `npm run build` 已通过。（commit: `7f51af8`）
- 已将图鉴搜索、快速筛选、更多筛选、顶部/底部分页及混养返回浏览接入统一工作区导航：补齐 `atlas-toolbar / atlas-results / atlas-grid / atlas-pagination-bottom` 稳定目标，移除页面自建的 `requestAnimationFrame + scrollIntoView`，结果定位会避让 sticky 工具区；`npm run lint` 与 `npm run build` 已通过。（commit: 本次提交）
- 已将鱼缸常用操作与长页面定位接入统一工作区导航：添加成功后的“查看鱼缸”和今日种草入口会准确定位对应区块；换水、喂食、养护记录、混养风险和水质自查改为结果型文案，无生物时记录喂食会显示错误反馈，不再静默；`npm run lint` 与 `npm run build` 已通过。（commit: 本次提交）
- 已收敛养护百科列表的重复 CTA：推荐卡与文章卡分别改为单一点击区域，不再同时出现多个“查看内容”；打开详情会记录来源位置，关闭后恢复右侧工作区滚动位置与来源焦点；`npm run lint` 与 `npm run build` 已通过。（commit: 本次提交）
- 已将养护百科的推荐、搜索、分类与结果区接入统一工作区导航：移除页面级固定延迟和 `scrollIntoView`，分类/收藏/hash 入口会定位右侧真实结果区，搜索回车后准确定位结果并重置分页；`npm run lint` 与 `npm run build` 已通过。（commit: 本次提交）
- 已新增统一工作区导航基础设施：提供路由、视图 hash、真实滚动容器 section 定位、sticky 偏移、目标高亮、失败 Toast 和上下文恢复能力；Sidebar 已移除固定 80ms 延迟与盲目 `scrollIntoView`，二级入口改为明确视图导航。`npm run lint` 与 `npm run build` 已通过。（commit: 本次提交）
- 已完成核心 CTA 与导航行为审计：梳理了 Sidebar、鱼缸、图鉴、混养计算、物种详情和养护百科的当前行为与目标契约，标记固定延迟滚动、重复“查看内容”、返回上下文缺失等问题，并形成 `docs/UX_NAVIGATION_AUDIT.md`。（commit: 本次提交）
- 已清理旧首页残留的静默存储读取失败：历史鱼缸或死亡记录损坏时会记录错误并显示内联提示，不再无声吞掉异常。（commit: 本次提交）
- 已完成前端性能最小收口：确认 3D 已具备离屏/后台暂停、桌面 30 FPS、移动端 24 FPS、像素比上限与卸载清理；图鉴维持每组 20 项及图片懒加载；大图预览、筛选弹窗和养护卡导出改为按需加载，添加流程步骤完成中文化。构建中大图预览由约 643KB 降至 4.3KB、筛选弹窗由约 639KB 降至 3.1KB、养护页由约 285KB 降至 83KB。（commit: 本次提交）
- 已完成 Copilot 与 AI 推荐反馈收口：鱼缸智能推荐候选完全由本地兼容规则产生，AI 只解释候选排序；Copilot 移除重复“生成方案”按钮并保留单一 Footer 主动作；分享操作用页面内反馈替代原生 alert。（commit: 本次提交）
- 已修复物种分类与水体边界：筛选标签、角色文案和工具属性统一移入物种服务层，工具属性不再依靠描述中的泛化关键词；补充甜心柠檬灯、人字蝶、巴西龟人工覆盖，水草/硬景不再进入图鉴生物列表，淡水设置会排除珊瑚砂和海水对象。分类自动测试覆盖 486 条数据并通过。（commit: 本次提交）
- 已将物种种草与养护文章收藏收口到统一收藏服务：兼容旧 `wishlistFishIds` / `aqua_care_favorites` key，同步本地应用状态、数量统计和跨页面事件；移除 Sidebar 的 1.2 秒轮询，并补充收藏服务自动测试。（commit: 本次提交）
- 已修复详情弹窗互斥与环境指标语义：品类详情会先关闭再打开具体物种详情；过滤与加热拆成独立指标，不再复用同一段设备文字；缺失项按钮改为“补充 pH / 配置过滤 / 配置加热”等明确动作。（commit: 本次提交）
- 已完成养护详情降噪：诊断问卷改为点击“开始问题自查”后再进入，按问题主题筛选 2–4 个相关问题；知识类移除纯滚动次按钮，详细说明与判断依据合并为一个折叠区，相关推荐继续限制最多 3 篇。（commit: 本次提交）
- 已统一所有“加入鱼缸”写入出口：新增物种添加服务集中执行混养预检、四态写入策略、谨慎二次确认和同种数量合并；鱼缸添加、图鉴详情与混养结果回写不再各自直接修改 `aquarium.fishes`。混养专项测试扩展至 10 项并全部通过。（commit: 本次提交）
- 已恢复当前前端类型契约基线：Copilot、推荐、鱼缸、图鉴、养护百科和登录页不再读取不存在的鱼缸/物种字段，补齐 Vite 环境类型并为旧数据展示路径增加显式类型适配；`npm run lint` 与 `npm run build` 均通过。（commit: 本次提交）
- 已新增 `src/lib/tankCompatibilityEngine.ts`，开始建立统一混养判断入口 `evaluateTankCompatibility`。
- `evaluateTankCompatibility` 已补充旧数据防御，避免历史混养条目缺字段时白屏。
- 混养计算组件已部分接入统一兼容性状态：`compatible / caution / not_recommended / insufficient_data`。
- 推荐服务已部分接入统一混养判断，用于过滤 `not_recommended` 候选。
- 推荐服务候选池已进一步收敛到 `evaluateTankCompatibility`：不再额外使用页面内旧冲突过滤覆盖候选结果。
- 混养计算页的主要冲突与下一步建议已改为读取统一规则结果，减少页面自行生成第二套混养结论。
- 混养计算页已补充“系统判断依据”展示，直接显示统一规则的通过项、风险/阻断项和缺失信息。
- 鱼种详情页已停止打开时自动 AI 审计；AI 改为用户点击“让 AI 帮我解读”后才调用，且不再合并或覆盖系统规则结论。
- 鱼种详情页发送给 AI 的内容已收窄为系统规则结论和规则条目，不再把完整鱼种/鱼缸资料交给 AI 重新判断。
- AI 不可用 fallback 文案已从“补充检查”调整为“AI 解读”，避免暗示 AI 会改变系统结论。
- 鱼种详情页的标题、结论与 AI 解读输入已接入 `evaluateTankCompatibility`，旧环境指标只作为展示层保留。
- 鱼种详情页的 AI 解读已从旧 `risk_audit` 调用切换为 `risk_explanation` 调用，展示内容改为解释、原因和建议，不再显示“额外风险审计”口径。
- AI 解释接口已要求返回 `statusRestatement`，前端会校验它必须与系统规则状态一致；不一致时丢弃 AI 输出并使用本地兜底说明。
- 普通推荐输出 `RecommendationItem` 已补充统一混养状态、规则摘要、通过项、风险/阻断项和缺失信息；旧 `riskLevel/canAdd` 暂时保留用于兼容现有 UI。
- `evaluateTankCompatibility` 已保留缸内已有生物数量，不再把所有已有生物默认当作 1 条/只计算。
- 图鉴“适合当前鱼缸”筛选和品类详情适配文案已改为优先消费 `evaluateTankCompatibility` 的四态结果。
- 混养计算推荐候选已改为携带缸内已有生物数量调用统一规则，推荐候选不再忽略已有数量。
- 混养计算页已保留从鱼缸导入的生物数量，并改为对已选组合逐个交叉评估后合并统一规则结果。
- 混养结果页已改为结论优先、物种操作优先：可保留/建议移除/原缸已有分组展示，具体规则依据默认折叠。
- 图鉴侧已存在品类分组、隐藏筛选标签、桌面工具区和收藏动效等未完成改造痕迹。
- 桌面 AppShell 已存在左侧 Sidebar 与右侧工作区的基础结构，但仍有布局错位和内容遮挡问题。
- 已将“统一混养判断、AI 按需解释、推荐候选接入统一结果”从脏工作区拆出并提交：`f8397b8`。
- 已完成弹窗系统第一轮收口：统一普通弹窗层级、筛选底部弹窗安全边距、大图预览最高层级和外边距，并通过构建验证：`0195db0`。
- 已完成桌面全局骨架收口：`>=768px` 使用固定 Sidebar 与右侧独立滚动工作区，移动端仅 `<768px` 显示底部导航，Sidebar 不再自动缩回，并移除桌面整页 scale/窄屏遮罩锁定，通过构建验证：`44a3183`。
- 已接通混养结果页“添加选中的新生物到当前鱼缸”闭环：图鉴混养计算可把勾选的新物种写入当前鱼缸，同物种合并数量，入缸日期默认今天，并给成功/失败反馈。（commit: 本次提交）
- 已完成核心闭环第一组即时修复：今日种草“感兴趣”同步写入统一种草图鉴收藏源；点击 3D 鱼缸中的生物可打开生物详情；状态卡提醒按钮不再贴底遮挡；混养提醒按组合合并展示，减少重复卡片。（commit: 本次提交）
- 已完成图鉴筛选第一轮兜底：扩大“观赏鱼”隐藏标签覆盖，更多筛选命中 0 条时保留弹窗并提示减少条件，避免用户应用死条件后只看到空结果。（commit: 本次提交）
- 已完成鱼种详情页操作区重排：隐藏“建缸要求”入口，将混养计算、加入种草、收藏、记录死亡前移到物种信息卡片下方，并移除底部重复操作区。（commit: 本次提交）
- 已修复图鉴页隐藏筛选标签的初始化顺序错误，避免 `role` 未初始化导致 `/encyclopedia` 渲染异常；已通过构建和浏览器 smoke 验证。（commit: 本次提交）
- 已完成产品逻辑降噪第一步：鱼种详情去掉“收藏/加入种草”重复入口，AI 解释降为折叠低频项；混养结果页仅在谨慎结果保留“查看提醒依据”次按钮，减少无意义并列 CTA。（commit: 本次提交）
- 已完成混养结果页二次降噪：同一组生物的多条提醒合并展示，结论卡改为直接给下一步动作，减少重复解释并保留可加入/移除/原有物种操作区。（commit: 本次提交）
- 已完成「今日建议」卡片重构：移除不可追溯的健康百分比，改为基于养护记录的状态、任务、依据、步骤和按需 AI 解读入口，并通过构建验证。（commit: 本次提交）
- 已完成前端性能第一轮最小优化：3D 鱼缸改为可见时按需渲染，浏览器后台或组件不可见时停止请求帧，桌面限制 30 FPS、移动端 24 FPS，并为图鉴列表/品类图片增加懒加载与异步解码；已通过构建验证。（commit: 本次提交）
- 已新增水族知识与决策层：补充物种知识画像、pair 级混养知识决策、三类诊断图和展示适配器；混养提醒可定位到具体 pair，鱼种公母辨别不再用名称或分类猜测，新增断言脚本覆盖 pair 数量、硬冲突优先、诊断引用和标签上限。（commit: a6df94e）
- 已新增 AI 建缸规划 Copilot 第一版：鱼缸页“智能推荐”改为“AI 建缸规划”，前端先用本地推荐/规则结果构造受控上下文，再调用 `build_tank_copilot`，AI 只能解释目标、追问缺失信息和组织行动建议，不直接写入鱼缸或覆盖规则结论；AI 不可用时显示本地兜底方案，并通过构建验证。（commit: 本次提交）
- 已修复收藏闭环的即时同步问题：桌面侧栏种草图鉴计数合并读取旧 `wishlistFishIds` 与统一本地状态，鱼缸页、图鉴页和养护百科收藏写入后会主动通知侧栏刷新。（commit: 本次提交）
- 已修复今日种草“感兴趣”未稳定进入收藏列表的问题：鱼缸页收藏状态改为从统一本地收藏源初始化并监听收藏变更，图鉴页收藏列表同步监听外部收藏事件；“感兴趣”点击会直接写入当前物种 ID，不再只依赖推荐服务返回值。（commit: 本次提交）
- 已修复种草图鉴列表与每日种草收藏源不同步的问题：图鉴页收藏列表改为合并读取统一本地收藏和旧 `wishlistFishIds`，鱼缸页刷新收藏时也统一走合并读取函数，避免“感兴趣”后入口数量变化但列表看不到。（commit: 本次提交）
- 已修复今日种草“感兴趣”数量先增后减的问题：物种收藏写入改为即时保存，避免后续今日种草牌堆的延迟保存用旧收藏状态覆盖新收藏。（commit: 本次提交）
- 已修复「今日建议」顶部卡片遮挡：顶部卡片改为紧凑摘要，只保留一个主动作和“查看详情”，完整步骤、推迟/完成与 AI 解读移入详情弹窗，并把鱼缸页顶部三卡行从固定 250px 调整为更稳定的最小高度；已通过构建和浏览器验证。（commit: 本次提交）
- 已完成 AI 维护收口第一步：AI client 统一返回 `source/failureReason/task/generatedAt` 元信息，`/api/health` 补充 provider/model/configured/timeoutMs，鱼种详情页明确区分“模型回复”和“本地模板”，避免 fallback 被伪装成真实 AI 回复；已通过构建验证。（commit: 本次提交）
- 已完成 AI 入口收敛第一步：今日建议详情不再提供 AI 连续问答输入框，只保留低频“一键解释这条建议”按钮；AI 仍只解释本地记录生成的建议，不改变处理结论，并已通过构建验证。（commit: 本次提交）
- 已完成 AI 建缸规划 Copilot 三步闭环：弹窗明确拆成“说目标 / 补信息 / 看方案”，缺失信息阶段支持用户填写最多 3 个补充答案后重新生成，方案与候选只在补充后进入第三步展示；已通过构建验证。（commit: 本次提交）
- 已完成 AI 入口收敛与 Copilot 可执行化：建缸 Copilot 第三步只保留目标理解、推荐方向、候选生物和一个固定下一步动作；混养提醒里的大块 AI 分析按钮降级为折叠低频解释入口，AI 不再抢主路径；已通过构建验证。（commit: 本次提交）
- 已完成 AI 建缸 Copilot 动作闭环收口：完善鱼缸信息会定位到对应设置区域，查看候选/模拟添加只消费本地规则允许候选，fallback 统一标记为本地模板并隐藏未通过规则候选；已通过构建验证。（commit: 本次提交）
- 已完成 AI 来源展示与 fallback 口径收口：今日建议、混养提醒、鱼种详情和 Copilot 统一区分“模型回复/本地模板”，fallback 只提示“AI 暂不可用，系统规则仍可使用”，不再伪装成大段 AI 分析。（commit: 本次提交）
- 已完成混养提醒弹窗降噪：同一组合的多条提醒合并为一张主要提醒卡，并在结果页折叠入口显示风险/缺失/通过数量摘要，降低重复文字对决策的干扰。（commit: 3412498）
- 已完成混养结果页决策流收口：结论卡改为“系统结论 + 下一步”，对象区明确可加入/移除/原有物种的操作含义，谨慎结果只保留低优先级提醒入口；已通过构建验证。（commit: 本次提交）
- 已完成鱼缸添加生物混养预检：添加弹窗在写入前调用统一混养规则，兼容物种直接加入，谨慎状态需确认，信息不足时先引导补充鱼缸信息，明确不建议加入的组合默认禁止写入；同物种追加合并数量且不把自身当作冲突对象。（commit: `5618203` / `c021358`）
- 已统一混养四态的写入策略：`insufficient_data` 不再从鱼缸添加入口绕过规则写入，而是打开对应鱼缸设置；同种追加保留原有数量参与负载计算，但不参与自身冲突比较。（commit: `c021358`）
- 已新增统一混养引擎专项测试，覆盖水体冲突、缺失信息、软性风险、捕食阻断和同种追加负载。（commit: `4d114a0`）
- 已将 pair 混养判断改为双向合并，选择顺序不再改变最终状态；“建议移除”只依据阻断 pair 生成，无法安全归因时不再猜测删除对象。（commit: `731f78a` / `b1fb4bb`）
- 已将水草和硬景从混养推荐、搜索和鱼缸导入候选中排除，实际页面验证后候选不再出现凤尾藓、莫斯墙、牛毛毡等非混养对象。（commit: `33fac2e`）
- 已抽取统一混养添加策略 `getTankCompatibilityAddPolicy`，鱼缸直接添加与混养结果页现在共同执行：兼容直接加入、谨慎二次确认、信息不足先补资料、不建议则阻断；专项测试覆盖状态映射及核心规则共 7 项并全部通过。（commit: `b18cf2d`）

## 当前目标

- 将现有本地优先应用拆分为 React 前端、Express 业务 API 与 Supabase PostgreSQL/Storage 标准三层；游客继续本地使用，登录后经预览确认迁移。
- 当前阶段先落地字段级契约、SQL、RLS、Storage 策略和共享类型，随后按内容只读、用户写入、游客迁移、管理后台顺序接入。

## 三层架构已完成

- 已落地 2.0.0 数据契约、20 张表的首个 Supabase migration、逐表 RLS、Storage 策略、自动用户档案/角色触发器、幂等记录和 camelCase 数据库共享类型。（commit: `3e644a3`）
- `npm run lint` 与 `test-three-tier-contract` 通过；当前环境缺少 PostgreSQL/Supabase CLI，迁移尚未在真实 Supabase 项目执行。
- 已建立 `apps/api`、`apps/web`、`packages/contracts`、`packages/domain-rules` workspace 边界；Express TypeScript 入口复用现有 AI 路由并提供版本化健康、物种和养护只读接口、统一请求 ID 与错误响应。（commit: `82e5653`）
- `npm run check:api`、`test:api-boundary` 和 Web 生产构建通过；数据库未配置时内容 API 明确返回结构化 503，现有页面不受影响。
- 已实现鱼缸、鱼缸生物/设备/配置、每日巡检、收藏、生命纪念、养护计划和养护事件 API，并建立游客本地/登录云端 Repository 边界。（commit: `e7ee912`）
- 已实现管理员物种与养护文章 CRUD、发布/下线、版本化图片上传和 WebP 衍生处理；私有原图不会经公开内容接口返回，替换失败会恢复旧素材版本。（commit: `fb2a6fd`）
- 已实现本地内容批量迁移工具：预检 486 个物种、41 篇养护文章与 2,026 项素材，按哈希跳过未变化版本，并报告 93 组跨内容重复素材供迁移前复核；默认 dry-run，只有显式 `--commit` 才写云端。（commit: `834db83`）
- 已上线不进入普通导航的 `/admin/content` 简易内容后台：物种/养护内容支持列表、新建编辑、发布下线和图片替换，具备管理员权限错误、骨架、空状态、loading、反馈、未保存提醒与状态确认。（commit: `b5face3`）
- 内容后台浏览器验收覆盖 390/1280px、加载、编辑保存反馈和 403 权限状态；按钮审计、lint、API check 与生产构建通过。
- `test:business-api-contract`、`test:repository-boundary`、`test:admin-content-contract`、`test:api-boundary`、API/Web 类型检查与生产构建通过。

## 待办
- 原 `aquaguide-frontend.pages.dev` 仍是旧资源；只有在需要继续使用该 Cloudflare 地址时才完成 OAuth 与发布。当前最新生产验收统一使用 `https://ice-glide.vercel.app`。
- 按 `docs/04-planning/NEXT_EXECUTION_PLAN.md` 推进去重待办：养护规则与权威来源、体态三步任务、鱼缸时间线与循环计划，再进入 Supabase/视觉模型/真机与真人验收。
- 在测试 Supabase 执行 migration、RLS 双账号、不同操作号并发扣减、最后批次同号重放、事务失败回滚和反馈真实写入；通过前不得宣称云端生产就绪。
- 将剩余用户端固定文案按模块迁入 i18next；生成 486 个物种、喂养资料和 41 篇养护文章的英文草稿，完成术语/数字/禁止动作校验与人工审核后再发布，未审核内容继续安全回退中文。
- 为管理后台增加翻译对照、覆盖率和审核发布页面；把已登录前端语言切换接到 `/api/v1/profile`，新设备在没有本地选择时读取云端偏好。
- 为 `sp_0357` 莫斯墙和 `sp_0452` 公子小丑生成候选透明源图；人工确认后才替换版本化素材并重跑图片、响应式和 2D/3D 一致性测试。
- 按 `docs/04-planning/EXTERNAL_VALIDATION_PROTOCOL.md` 执行真实水族新手任务、真实鱼缸跨入口人工验收和低端真机 3D 五分钟采集。
- 复核 93 组跨内容重复素材；确认迁移提交请求的数据携带方式后完成游客迁移/离线队列，再逐页把现有前端接入 Repository，并在测试 Supabase 项目执行 migration 与 RLS 验证。

## 本轮已完成

- 2026-07-28：按最终 C 方案重排鱼缸首页。桌面新手起步收进侧栏，侧栏统一鱼缸切换与新建；观察区改为左侧鱼缸＋三种物种预览、右侧今日行动，管理与学习并排，进阶检测横跨底部。修复侧栏 `action=create` 只开设置却不新建的问题。`npm run lint`、`npm run build` 与 `scripts/verify-aquarium-home-c.mjs`（1440/1000/600px 桌面、320–430px 真手机、物种弹窗、移出重试和风险向导）通过。（commit: `eb0fe80`）
- 今日行动改为同一优先级选择器生成状态、原因和唯一主操作，长详情弹窗已删除，异常 AI 解释改为低频原位展开。
- `/collection` 改为四张模块卡首页，四个模块使用独立地址；桌面侧栏提供直达二级标题，旧 query 与旧收藏路由兼容重定向。
- 鱼缸页删除重复“死亡图鉴”；生命纪念详情的“再次加入”通过查询参数打开现有添加流程并预选物种，最终仍执行统一混养复核。
- `npm run lint`、`npm run build` 与 1280/390px 浏览器直达回归通过，无页面错误。（commit: `917b80a`）
- 核心路由增加独立错误边界，动态块加载先重试一次且自动刷新最多一次；损坏本地数据安全降级并提示，图片 404 只显示占位图。生产故障注入确认侧栏保留、无技术堆栈、无刷新循环。
- 生成 486 个物种两档 WebP 与 54 张养护图两档 WebP；卡片缩略图均小于 100KB，3D 继续使用原版本化贴图。生产预览手机首屏图片为图鉴约 153KB、鱼缸约 45KB、养护约 148KB，核心内容约 1.1–1.6 秒可用；2G/节省流量不自动下载 3D。（commit: `b38508c`）
- 完成混养跨入口自动一致性验收：统一引擎、选择顺序和添加策略测试通过；带真实缸内活体的页面点击仍按外部协议执行。
- 将 AI 风险审计改为“用户点击后解释”，AI 不得自动影响最终结论。（基础链路、详情页调用和状态一致性校验已完成，后续可继续统一其它入口的命名与展示）
- 继续验证有真实缸内活体时的“按鱼缸导入”与结果页写入交互；空缸空状态、本地候选推荐和非混养对象过滤已通过页面验证。
- 18 张中优先级素材已逐张复核；16 张接受现状，2 张待重新生成。轻量飞入或 `+1` 动效继续作为 P1 增强，不影响快捷收藏 P0 闭环。
- 继续按文章类型细化养护详情：操作型已采用大图与三步指引的“图示操作台”，诊断型自查与护理型清单后续结合真实文章逐项验收。
- `risk_audit`、`risk_explanation`、`recommendation_assist` 只保留内部兼容，正式页面不再公开调用。
- 清理或隔离外部项目目录，避免误提交非 AquaGuide 文件。
- 后续性能优化先在真实低端设备完成 5 分钟帧率与 GPU 内存曲线，再决定是否拆分 `ThreeAquarium`、物种数据和图片资源；本轮未做大拆分。

## 已知问题/阻塞点
- Antigravity 的全局翻译任务已暂停且未完全完成；本轮只补了导航、引导、缸内物种与体态相关词条，不能宣称全站英文完成。
- `aquarium_species_batches`、引导偏好同步和批次 RLS 只有静态契约/API/Repository 证据，尚未在真实 Supabase 测试项目执行。
- 原子移出已通过代码、路由顺序和专项测试；尝试用隔离 Docker PostgreSQL 执行真实重放时，Docker daemon 连续从官方 registry 拉取镜像返回 EOF，因此真实数据库重放仍待测试 Supabase 或 registry 恢复。
- 当前英文模式已覆盖应用壳、导航、错误恢复和设置，但业务页面大量固定文案及英文内容尚未审核完成；因此只能作为本地预览，不能标记为正式英文内容全覆盖。
- 生产构建仍提示两个超过 500KB 的按需代码块：`species.service` 因静态依赖完整物种库约 739KB，`ThreeAquarium` 因 Three.js / React Three Fiber 运行时约 939KB。两者均已按路由或组件延后加载；进一步拆分需要物种仓库分层和 3D 模块边界重构，不属于本轮核心收口，未通过调高警告阈值掩盖。
- 子项目此前缺失 `PROGRESS.md`，历史进度主要来自对话上下文和 git diff；本文件从当前状态开始补记。
- 真实低端设备的 3D 五分钟帧率与 GPU 内存曲线尚未采集；当前只确认了构建体积、限帧、离屏/后台暂停和释放路径。

## 关键决策记录
- 国际化只改变展示语言，中文主数据继续供规则引擎使用；未发布英文翻译必须回退中文并显式标记，不允许机器草稿直接公开。
- 不修改原始物种数据结构。
- 2026-07-16 起迁移策略更新为：游客继续兼容现有 localStorage；登录用户业务数据与内容库进入 Supabase，前端只通过 Express API 访问业务表。
- AI 只能解释规则结果，不能决定 `canAdd`、风险等级或推荐候选池。
- 混养判断统一为四种状态：`compatible / caution / not_recommended / insufficient_data`。
- 手机端按真实手机设备判定，不再按 `<768px` 判定；桌面缩窄仍保持桌面工作台，平板默认使用桌面布局。

## 2026-08-28 Admin Content / Species SEO 附属项目
- 独立分支：`feature/admin-content-v0`；不合并 main，不写 Production Supabase。
- 详细实时状态统一记录在 `.ai/LIVE_STATUS.md`，执行目标见 `.ai/CURRENT_GOAL.md`，决策见 `.ai/DECISION_LOG.md`，任务队列见 `.ai/TASK_QUEUE.md`。
- Species SEO Admin 已从 486 条平铺 catalog 升级为 `Category → Base Species → Variant` 分组模型。
- 当前扫描结果：486 条 catalog → 276 个 Base Species；83 个组可批量；293 条记录位于多成员组；223 条识别出明确 Variant；28 条疑似重复；5 个组存在跨分类冲突并强制进入人工复核。
- 批量 SEO 当前只作用于 Editorial SEO 草稿，不修改温度、pH、混养、难度等 Product Truth；分类冲突组禁止直接批量发布。

### 2026-08-28 Species SEO 继承模型
- Base Species 公共 SEO 层与 Variant Override 已在隔离 Admin 分支实现：`Variant Override ?? Base Species Template`。
- 批量操作不再把同一段 SEO 文案复制到多个变种，只建立 Draft shell；未 Override 字段继续继承 Base。
- 真实 Chrome 验证 Base 模板/共享简介会立即影响同组 Variant 预览，单个 Variant Override 清空后恢复继承。
- 新 `species_seo_groups` migration 已仅在本地隔离 Supabase 执行；管理员写入通过，普通用户 Draft 读取为 0 且写入被 RLS 拒绝。Production Supabase 未执行。

### 2026-08-28 Species SEO 中英文转化与数据复核
- Admin 新增独立 `zh-CN / en` 内容层；同一 `catalog_key` / Base Group 可分别保存中文与英文 Draft/Published 状态，互不覆盖。
- English 增加 editorial `localized_name`，只用于英文 SEO/展示，不修改 `fishData.ts` 中的 Product Truth 名称。
- 新增中→英翻译工作台：中文 Source → AI English Suggestion → 人工修改/确认 → English Draft；机器结果不能直接发布。
- 服务端翻译接口复用现有 DeepSeek/OpenAI-compatible 配置，并再次校验 Supabase JWT + admin role；AI key 不进入浏览器。
- 科学名、catalog key 与 `{{template_tokens}}` 受保护；Base 模板变量丢失或改名会直接拒绝结果。
- 5 个分类冲突与 28 条疑似重复已升级为可查看证据的 Data Review Queue，不自动删改源 catalog。
- 本地隔离 Supabase 已验证中文/英文同 key 共存及普通用户 Draft 不可见；Production Supabase 未执行新 migration。
- 真实 Chrome 已验证 English 双栏翻译界面、神仙鱼分类冲突证据与米虾重复证据；后续已补齐 public Species 多语言 URL/canonical/hreflang contract 与静态生成器。

### 2026-08-28 Species SEO Route / Index / 页面效果预览
- 隔离 Admin 已新增稳定 Species 路由提案：英文 `/species/<scientific-slug>/<catalog-key>.html`，中文对应 `/zh/species/...`；沿用现有 Problem SEO 页的 English-default + `/zh/` + hreflang/x-default 结构。
- 所有 Species 默认 `noindex`，可人工选择独立 Index 或同 Base Species 内 canonical；分类冲突/疑似重复继续 fail closed。
- Canonical 不再由运营手填，改为路由契约自动生成；Base Species 继续只承担共享内容继承，不自动产生公共 landing page。
- Admin 已增加真实 HTML“公开 Species 页面效果预览”，组合 SEO H1/简介与现有 catalog 水温、pH、缸体、难度等只读 Product Truth。
- 新 migration 004 仅在全新本地隔离 Supabase 验证；管理员写入成功，普通用户 Draft 读取为 0 且写入被 RLS 拒绝。Production 未执行。
- 正式 Species HTML 生成器与 canonical/hreflang/robots/sitemap 运行时门禁已在隔离分支完成；中英文 Published 仍保持禁用，版本历史/回滚现已在后续里程碑通过本地验证；当前剩余发布门禁是 staging 端到端验证。

### 2026-08-28 Species SEO 静态生成器与版本回滚
- 新增 fail-closed `generate-public-species.mjs`：只消费显式 `local/test/preview/staging` 发布快照，拒绝 `production` 输入，也不会隐式写入根 `public/`。
- 生成结果包含双语静态 Species HTML、`sitemap-species.xml` 与 manifest；Title、Meta、H1、robots、canonical、hreflang/x-default 和 sitemap 进入运行时回归。
- 首轮运行时测试实际发现并修复英文页面 locale 未透传的问题：英文路径曾错误渲染中文 `<html lang>` 与事实标签，修复后回归通过。
- 生成层重复执行 Index 安全门，不信任 Admin UI：分类冲突/疑似重复不能直接 Index，canonical 目标必须同语言 Published 且独立 Index；sitemap 只纳入 self-canonical `index,follow`。
- migration 005 新增数据库 `content_revisions`、Base/Variant 自动 revision trigger 与 `restore_species_seo_revision`；回滚强制恢复为 Draft、清空 `published_at` 并记录 source revision，不能借回滚重新发布。
- 全新隔离 Supabase 已从 core + migrations 001–005 重建并验证：Variant 与 Base 均通过 `v1 Draft → v2 Published fixture → v3 rollback Draft`；普通用户历史可见 0、回滚 RPC 被 `Admin role required` 拒绝；最终 SEO/group/revision 测试残留均为 0。
- Admin UI 新增 Base / Variant History 面板和二次点击恢复；只读 Review 不连接真实 history。真实 Chrome 验证两个 History 面板、Public Species Preview 与两个 disabled Published option，无 page error。
- `npm run test:contract -w @aquaguide/admin-content` 与 Admin production build 通过；仅保留既有 >500KB bundle warning。
- 上一提交 `43eec47` 的独立 Vercel `admin-content` Preview 已确认 READY / HTTP 200 / noindex；本批次待集中 commit/push 后再核对新 SHA Preview。
- Production Supabase 与 `main` 仍未修改。A+B 已完成 migrations 001–006 + schema probe + snapshot → generator → rendered-page 的端到端验证；下一阶段转为 publish-readiness、人工数据复核决策与非 Production Preview Publish。

### 2026-08-28 Species SEO staging release gate hardening
- Generator/history milestone committed and pushed as `cd363b4`; `main` and Production Supabase unchanged.
- New Vercel Preview could not be created because the Hobby account hit `api-deployments-free-per-day` (>100/day). Previous Admin Preview remains healthy; no Production deployment was attempted.
- Supabase inventory confirms AquaGuide has no existing development branch; the separate IceGlide staging project is not reused.
- Fixed a release-safety gap: static Species generation no longer defaults to the Production canonical host. `siteUrl` is mandatory and the known Production host is rejected for non-production generation.
- Added staging-only Published snapshot export and end-to-end staging verifier with explicit staging/Production DB and public-site deny-lists.
- Staging verifier requires at least one bilingual self-canonical Index pair and validates generated EN/ZH pages plus sitemap over a temporary HTTP server.
- With no staging env configured, `verify:staging-publish` intentionally exits non-zero; Published remains disabled.

### 2026-08-28 Species SEO release-gate schema probe
- 新增 migration 006 `species_seo_release_gate_status()`，只暴露 schema readiness，不暴露 SEO 内容或 revision 数据。
- staging exporter 在读取 Published 内容前先调用 probe；`schema_version < 6` 或任一能力缺失都会 fail closed。
- 全新隔离 Supabase 从 core + migrations 001–006 顺序应用成功；anon publishable 身份读取 probe 全部为 true，同时直接读取 `content_revisions` 仍被 `permission denied`。
- staging generator 进一步要求显式 Production public URL deny-list；直接 staging 调用也不能只靠固定域名保护。
- 临时 Supabase 已关闭并删除，Production 未修改。

### 2026-08-28 Admin A+B 稳定验证模式
- Admin 后续采用 A+B：B 为 Mac 本地 Supabase 快速开发，A 为 GitHub Actions 临时 Supabase 干净机验证；不再把付费 Development Branch 作为当前必选前提。
- A/B 共用同一个 `test:supabase-gate`，避免本地和 CI 维护两套数据库验证逻辑。
- GitHub workflow 固定 Ubuntu 24.04、Node 24.14.0、Supabase CLI 2.115.0，并只给 `contents: read` 权限；无 Production Supabase/Vercel secret，无自动 commit/deploy。
- 临时数据库只加载 core schema + 当前 Admin migrations 001–007，避免历史无关 migration 冲突影响 Admin 门禁；后续新增 Admin migration 必须显式加入 allow-list。
- 本地第一次执行发现临时 `migrations/` 目录缺失，第二次发现 service_role 无 `user_roles UPDATE`；均在 push 前修复，未通过临时扩权绕过。
- 最终本地门禁已通过：schema v6、普通用户 Draft 可见 0、普通用户不可写/不可看 revision/不可 rollback、Base/Variant rollback 强制 Draft、数据库读取后生成 2 个中英 Index 页面并验证 canonical/hreflang/sitemap。
- A 层已由 GitHub Actions run `33147127271` 真实 Ubuntu clean-run PASS 证明；下一步不再重复基础设施，转入 publish-readiness 与 Preview Publish。


### 2026-08-28 Admin A+B 首次完整 CI 通过
- `ef2f6ae` 修复 root lockfile 对 Admin workspace 的缺失登记；实际 `date-fns` 解析版本仍为 4.1.0，没有依赖降级。
- GitHub Actions `33147127271` 在 Ubuntu 24.04 干净 runner 全绿：Node 24.14.0 / Supabase CLI 2.115.0 / npm ci / contract / 临时 Supabase 001–006 + RLS/rollback/双语静态页 / build / generated catalog parity / diff hygiene 全部 PASS。
- 至此 A+B 已成为正式稳定门禁：B 本地快速迭代，A 干净机重复验证；两层都不持有或修改 Production。

### 2026-08-28 Admin 下一阶段：发布准备与数据复核闭环
- A+B 基础设施已验证完成，后续不再把付费 Supabase Development Branch 当作必选阻塞。
- P0 改为 publish-readiness：对 Base / Variant / locale 显示内容完整性、Base Published/ready、数据复核、Index/canonical、双语 counterpart、revision 等阻塞原因；“Ready”只代表可进入 Preview Publish，不代表 Production 上线。
- Data Review Queue 要从只读告警升级为人工决策记录：分类冲突记录确认分类/备注；疑似重复记录确认 duplicate / distinct / canonical candidate。不得自动改 `fishData.ts`。
- P1 为非 Production Preview Publish：显式临时输出 → static generator → canonical/hreflang/sitemap → A+B gate → 可查看 Preview。
- Live AI 翻译只做 suggestion smoke test，仍要求人工确认并保存为 Draft。


### 2026-08-28 Admin Publish Readiness + Data Review 可执行闭环
- 新增 branch-only migration 007：Base / Variant 增加 `editing → ready_for_review → approved` Editorial Review State；任何正文、SEO、Index/Canonical 修改都会由数据库自动把旧 Approved 退回 Editing。
- Rollback 继续强制 Draft，并进一步强制 Editing，旧 revision 的审核结论不会跟随恢复后的内容。
- 新增 `species_data_reviews`：5 个分类冲突与 28 条疑似重复可记录人工决定、备注和 duplicate canonical；该 Admin 仍不自动修改 `fishData.ts` Product Truth。
- 对静态发布只暴露脱敏 review-resolution RPC；审核备注与审核人保持 admin-only。
- 新增 Publish Readiness 面板，区分 Blocked / Ready for Review / Publish-ready；后者只代表可进入非 Production Preview Publish。
- 修复结构缺口：276 个 Base Species 全部拥有 Base 编辑/审核层，单成员组不再因为缺少 Base Editor 永久无法满足 Generator。
- Generator 现在要求 same-locale Base + Variant 均 Approved，并在生成时再次验证 Data Review / duplicate canonical / Index 规则。
- B 层从空库顺序应用 core + migrations 001–007 并通过：schema v7、普通用户 Draft/Data Review/History 权限隔离、Approved 修改自动失效、rollback→Draft+Editing、DB→2 个中英 Index 页面。
- 真实 Chrome 覆盖重复、分类冲突和单成员 Base，修复一个 `groupMember is not defined` 运行时错误后 `PAGE_ERRORS=0`。
- 下一门禁：push 后要求 GitHub A 层 clean Ubuntu run 对 migration 007 全绿；通过后进入 Controlled Preview Publish。


### 2026-08-28 Admin Controlled Preview Publish
- migration 007 已由 GitHub Actions run `33149941551` 在干净 Ubuntu A 层全绿验证，Publish Readiness/Data Review 不再只是本地证据。
- 新增受控 Preview 发布：Approved Draft 可进入 Preview，但正式 release/staging 仍只接受 Published。
- Preview 复用同一个 Species static generator，不建立第二套页面渲染逻辑；实际 HTML 强制 `noindex,nofollow`，同时保留 intended robots 用于人工判断。
- Preview 根目录生成 `robots.txt` 全站 Disallow，并故意不生成正式 `sitemap-species.xml`；根/应用 public 与 Admin dist 输出目录均硬拒绝。
- Admin Publish-ready 状态可导出当前 Species 的最小 Preview Snapshot，审核人/备注不会进入导出。
- 本地实际生成中英 2 页 Approved Draft Preview；`http://localhost:4020/`、英文页、中文页均 HTTP 200，Chromium 无 page error。
- 正式 Production Published 仍锁定；下一步转向 Data Review / Publish Readiness 队列级总览与筛选。

### 2026-08-30 Admin UI Studio 视觉接线（第一阶段）
- Google AI Studio 输出已同步到独立仓库 `aqua-fronted-cms`；该仓库只作为视觉/交互参考，不成为 AquaGuide 数据或发布逻辑的新来源。
- 先收口并提交 `374db2f`：Data Review / Publish Readiness 队列级筛选，真实 Chrome 证明 33 个待复核事项对应 32 个 Base Group，清除后恢复 276 组。
- 真实 Admin 已改为三栏编辑工作台：左侧 Category→Base→Variant，中心专注编辑，右侧固定实时前端页面预览。1600px 验收宽度为 270 / 870 / 460。
- Base 父级与 Variant 子级现在对应不同编辑上下文；语言切换只保留在中心工作区。
- 右侧 Page / Google / Mobile 由真实 AquaGuide state 驱动；中间未保存 H1 修改可以即时反映到右侧。
- Data Review、发布检查、翻译、批量、History 和 Workflow Overview 均降级为二级展开工具，不再常驻挤压编辑区。
- 首轮曾把 Product Truth 补进 `species-groups.generated.json` 以验证真实预览，随后因主包膨胀改为按需读取 catalog；当前 Group JSON 不再复制 image / 水温 / pH / 缸体 / 难度 / description。
- Local contract/build/Chrome 与 schema-v7 Supabase gate 均保持通过；Production Supabase 和 `main` 未修改。

### 2026-08-31 Admin 三栏工作台 + 全局中英文界面
- `aqua-fronted-cms` 仅作为 UI 参考，真实 Base/Variant/Data Review/Readiness/Preview/History 逻辑继续来自 `feature/admin-content-v0`。
- Admin 已重构成“左选对象 / 中间编辑 / 右侧实时前端效果”的三栏工作台；Variant 未保存修改也会即时反映到右侧。
- 新增全局界面语言 `appLocale`，支持中文 / English、刷新记忆和 `<html lang>` 同步；它与内容语言 `contentLocale` 独立。
- 右侧 Product Truth 改为按需读取 catalog，避免向 Species Group JSON 重复复制图片、水温、pH、缸体等事实数据。
- B 层 contract/build/Supabase gate 和真实 Chromium 回归均通过。

### 2026-08-31 Admin 工作流状态色 + 下一阶段 Inspector
- 顶部 `数据复核 / 待审核 / Preview-ready` 不再使用同一视觉：分别使用琥珀色、蓝色、绿色语义，并保留可点击筛选；真实浏览器 computed style 已验证。
- `539baf4` 三栏工作台/全局中英文里程碑已通过 GitHub Actions run `33323234484`，A 层 contract、migration 001–007 ephemeral Supabase、build、catalog parity、diff hygiene 全绿。
- 下一阶段 P0 改为 Editor ↔ Preview 双向 Inspector，而不是继续大面积修 CSS：中间 focus/edit → 右侧自动定位高亮；右侧点击元素 → 中间自动定位对应字段。
- 首批映射：localized name、H1、Intro、Image Alt、SEO Title、Meta Description。SEO Title/Description 可自动切到 Google Preview。
- 水温、pH、缸体、难度、科学名等 Product Truth 也允许被 Inspector 识别，但必须显示 `Product Truth · Read only`，Content Admin 不提供修改入口。
- Inspector 使用稳定元素 registry，不做任意 DOM/WYSIWYG 编辑，避免破坏现有 Base/Variant inheritance、RLS、Readiness 和 generator 安全边界。
### 2026-08-31 双向 Preview Inspector
- 新增稳定 Element Registry，将中间编辑字段与右侧真实 Page / Google 元素建立一一映射。
- 中间 focus H1/Intro/Image Alt 等字段时，右侧会自动滚动并高亮；SEO Title / Meta Description 会自动切 Google Preview。
- 右侧 Hover 显示元素名，点击后锁定选框；中间对应字段自动滚动并高亮，但不会强制抢占输入光标。
- 选中信息显示元素来源与修改路径：Custom / 继承自 Base / Product Truth 只读，以及 Current page / Base Species / Product Truth。
- Base 编辑模式已纳入映射；如果元素实际属于 Variant-only 或已有 Variant Override，系统会切回“当前页面”避免误导。
- 水温、pH、缸体、难度、学名保持 Product Truth 只读，仅用于解释“为什么页面有这个元素但这里不能改”。
### 2026-08-31 继承编辑交互收敛
- Meta Title / Meta Description / H1 不再用空输入框表达继承，而是直接显示当前 Base 生效值。
- 用户点击“单独编辑 / Override”后才进入 Variant 输入；未输入时不会物化 Base 文案。
- 自定义后提供“使用 Base 值 / Use Base value”，本质是清空 Variant Override，继续由继承 resolver 计算。
- 与右侧 Inspector 联动保持不变：Inherited/Custom 状态和 Page/Google Preview 会实时同步。

### 2026-08-31 Admin 二级工具抽屉化
- Data Review、Publish Readiness、Translation、Batch SEO、Revision History、Workflow 不再在编辑器底部大块展开，统一进入中间编辑列的 `EditorToolDrawer`。
- 1600px Chromium 实测：Editor 270–1140，Drawer 580–1140，Preview 1140–1600；抽屉不侵占右侧 Preview。
- 支持 ESC / × / 中间遮罩关闭；右侧点击可编辑 Inspector 元素会关闭抽屉并返回对应字段，Product Truth 只读元素不会伪造编辑路径。
- Workflow Drawer 的 Pending 点击会关闭抽屉并真实作用到左侧筛选；English Translation Source/Target 正常渲染。
- 清理 Species SEO UI 中错误遗留的 Archived，只保留 Draft / Published（Published 继续锁定）。
- Contract/build/B 层 ephemeral Supabase schema v7 全部 PASS。

### 2026-08-31 Admin Preview 与发布生成器对齐
- 右侧 Page Preview 已改为真实静态 Species generator 的同一页面结构：Header → Breadcrumb → Hero（图/H1/学名/Intro）→ 4 个事实卡 → Product Truth 说明。
- 新增 `speciesPagePresentation.js`，Preview 与 `generate-public-species.mjs` 共用发布文案标签和 English tank-size 本地化规则。
- 删除此前 Preview 独有但 generator 不会发布的 `Overview & Care / Care essentials` 假正文，避免“后台看得到、上线没有”的误导。
- Chromium 发现 1120/1080px 旧三栏会把 400px Preview 裁出视口；现改为 1180px 以下双栏，并提供显式 Preview 浮层，不丢核心预览能力。
- 1120px 实测：编辑 H1 → Preview 浮层自动打开并高亮；点击 Preview H1 → 浮层关闭并回到中间字段。1200px+ 仍保持常驻右栏。
- Contract / generator tests / build / schema-v7 B gate 全部 PASS。

### 2026-08-31 主编辑器密度收敛 + Inspector 权威路由
- `d79058f` 的 GitHub Actions run `33326654737` 已全绿。
- Variant 顶部由 4 个重复状态 pill 收敛为单行 `Draft · 编辑中`，Base Species 同步统一。
- Focus Keyword / Index Strategy / Canonical / URL 收入默认折叠的“高级 SEO”；若存在索引/Canonical 阻塞会自动展开，安全问题不会被隐藏。
- Variant 中完整 Base 共享简介改为按需展开，避免 Base 文案增长后把每个 Variant 页面永久撑高。
- 1440px Chromium 实测常规 Variant 编辑区约 1032px，高于密度优化前约 1333px 的页面减少约 23%，Inspector 仍正常。
- 修正右侧 Inspector 的“在哪里改”逻辑：从 Preview 点击继承 H1/Intro/metadata 自动进入 Base；Variant-only/自定义内容进入当前页面；Product Truth 只读。中间字段点击仅负责高亮 Preview，不会突然切换编辑上下文。
- Contract / build / 本地 schema-v7 Supabase gate 全部 PASS。

## 2026-08-31 — Primary editor density refinement
- Variant authoring default height reduced from ~1333px to ~936px at 1440×900 without removing any persisted field.
- Primary editor now emphasizes the five daily fields; low-frequency SEO routing controls are progressively disclosed.
- Lifecycle/review state is summarized once in the header; actual review/status controls remain in the sticky save footer.
- Intro remains Inspector-mapped after reducing its default textarea to four rows.

## 2026-08-31 — Base / Variant state-control parity
- Footer controls now state their meaning directly (`审核 · 编辑中`, `状态 · Draft`) instead of exposing contextless dropdown values.
- Base and Variant share the same review/lifecycle color semantics and continue to use the same persisted fields and fail-closed publication lock.

## 2026-08-31 — Three-pane visual hierarchy convergence
- Left navigation, center editor Inspector and right Preview now use one shared selection language instead of three different greens.
- Product Truth facts use a separate graphite read-only Inspector state; editable fields remain green.
- Selecting a Variant now keeps its parent Base relationship visible through the Base name and hierarchy guide without creating a second strong active row.
- Browser regression and Admin contract pass; no Product Truth, inheritance, review or publishing behavior changed.

## 2026-08-31 — Workflow filter visual semantics
- Species quick filters now mirror the top workflow state colors: issue amber, review blue, Preview-ready green.
- zh-CN and English UI modes no longer produce mixed-language workflow filter labels.
- Existing filter counts and group mapping remain unchanged and browser-verified.

## 2026-08-31 — Navigation count-unit clarity
- Fixed the misleading `All 486` sidebar count: the Species tree contains 276 Base groups, so navigation now uses 276.
- 486 remains the catalog-record total in the summary.
- Workflow filters keep their native counts and explain units through localized hover help plus affected Base-group counts in the active banner.
