# AquaGuide 当前交接快照 — 2026-08-14

> 本文只记录 2026-08-14 当前项目状态、未合并 PR、风险与下一步执行顺序。历史实现细节继续保留在根目录 `HANDOFF.md` / `PROGRESS.md`，避免覆盖旧交接记录。

## 1. 当前结论

AquaGuide 已经从“功能补齐期”进入 **数据准确性 + 规则一致性 + Evaluation 收敛期**。当前不是继续加新功能，而是在确认：

1. 物种数据是否可信、是否会被错误分类或重复；
2. 同一事实是否被 taxonomy、water type、compatibility、species fit 用同一语义解释；
3. 植物 / 硬景 / 动物是否走正确的业务规则；
4. CI 是否能在未来数据变动时阻止同类问题重新出现；
5. UI 是否仍然把历史共享 schema 的字段用错误标签展示给用户。

**当前最重要的事实：PR #29 / #30 / #31 都已经通过各自完整 Golden Path，但都还没有合并进 `main`。因此“代码已验证”不等于“生产已上线”。**

## 2. 当前进度表

| 阶段 | 状态 | 已完成 | 验证证据 | 还剩什么 |
|---|---|---|---|---|
| 已合入 `main` 的产品基线 | ✅ 已完成 | GP-001 / GP-002、核心鱼缸事实链路、任务路由、兼容性 evidence gate、响应式与交互基线等此前工作 | 根 `HANDOFF.md` / `PROGRESS.md` | 本轮 #29/#30/#31 尚未进入 main |
| PR #29：taxonomy / water type / catalog integrity | ✅ 已实现、✅ 全绿、⏳ 未合并 | 统一 `freshwater / saltwater / brackish / unknown`；修海水无脊椎判断；清理 28 条精确重复并保留 legacy ID alias；旧鱼缸缺尺寸/水体不再伪造默认值；responsive smoke 纳入 Golden Path | head `58fea834...`；Product Golden Path #131 全绿；PR mergeable | 需要 review / merge 到 main；这是 #30/#31 的共同基础 |
| PR #30：scientific-name collision evidence audit | ✅ 已实现、✅ 全绿、⏳ Draft | 11/11 alias-like collision 全部建立 review ledger；拆成 `probable_alias / hold_distinct_strain / hold_coarse_taxonomy / hold_taxonomy_conflict`；新增“0 unreviewed collision”硬门槛 | head `13b86d2...`；Product Golden Path #136 全绿 | 7 个 `probable_alias` 仍不能直接删；未来必须有外部实体证据 + canonical 内容选择 + legacy ID migration |
| PR #31：plant / hardscape species-fit routing | ✅ 已实现、✅ 全绿、⏳ Draft | 修复植物和硬景被套用鱼类式 `tank volume / predator / density / single housing` 规则；已有 species-fit 测试正式接入 CI | head `018936b...`；pre-fix #137 能稳定复现错误；fix 后 #140 全绿 | routing 已修，但共享 `Fish` schema 的 UI / 数据字段语义仍未解决 |
| 下一阶段：detail/UI schema semantics | 🟡 准备开始 | 已定位风险：植物仍可能拥有 fish-style `tankSize`、`feedingProfile.recommendedFoods` 等历史字段 | 尚未形成独立 PR | 检查物种详情、推荐列表和卡片是否把植物字段展示成“最低缸容 / 推荐食物”；按 life type 做展示语义分流 |
| Phase-2 alias dedupe | ⏸ 暂停删除 | 已证明 7/7 `probable_alias` 没有真正 care-parameter conflict，只存在 description / specialNotes narrative drift | PR #30 audit | 不能仅凭同 scientificName 自动合并；需要逐组外部物种/品系证据后再迁移 |
| 外部环境验证 | ⏸ 未完成 | 静态契约、CI、浏览器 Golden Path 已较完整 | 历史 HANDOFF 中已有记录 | 真实 Supabase migration/RLS、真实新手可用性、真实视觉模型照片准确率、低端设备 3D 仍需外部环境 |

## 3. 三个当前 PR 的关系

```text
main
 └─ PR #29  fix/marine-invertebrate-water-type
     ├─ PR #30  audit/species-collision-evidence
     └─ PR #31  fix/non-animal-species-fit
```

- #29 是本轮准确性修复的基础 PR。
- #30 和 #31 **都直接 stacked 在 #29 上**，彼此不是父子关系。
- #30/#31 为了触发现有 `base=main` 的 Product Golden Path，验证期间曾临时 retarget 到 `main`，验证完成后都已经恢复为 #29 的 stacked base。
- 三个 PR 当前都未合并；#29 是 ready PR，#30/#31 仍是 draft。

## 4. PR #29 当前事实

### 已解决

- 物种 water type 有唯一 canonical 语义：`freshwater | saltwater | brackish | unknown`。
- taxonomy、Care filter、species fit、compatibility 不再各自猜水体类型。
- brackish / unknown 不再被默认为 freshwater。
- 海水清洁虾、寄居蟹等 marine invertebrate 判定补齐，同时避免误伤 freshwater shrimp。
- `Thor` genus 规则被约束，避免把 `Thorichthys` 错认成 marine invertebrate。
- exact duplicate business entity 已降到 0。
- 删除 28 条精确重复前先建立 28 个 stable legacy ID alias，并覆盖 aquarium livestock、wishlist、deceased、imported state、compatibility、discovery 等持久化表面。
- 旧鱼缸缺尺寸时不再制造 `60×40×40`；缺 water type 时不再制造 Freshwater certainty。
- responsive smoke 已覆盖 7 个 viewport/locale profile × 17 routes。

### 当前 audit baseline（PR #29）

| 指标 | 当前值 |
|---|---:|
| Catalog records | 458 |
| Freshwater | 389 |
| Saltwater | 69 |
| category→freshwater fallback dependency | 0 |
| lifeType/category contradiction | 0 |
| pending category normalization | 0 |
| exact duplicate business entity | 0 group / 0 record |
| stable legacy species aliases | 28 |
| scientific-name collision | 18 groups / 39 records |
| 原 alias-like review candidates | 11 groups / 22 records |

## 5. PR #30 当前事实

11 个 alias-like scientific-name collision 已全部人工语义分类：

| 分类 | 数量 | 含义 |
|---|---:|---|
| `probable_alias` | 7 | 可能是同一业务实体，但没有足够证据允许自动删除 |
| `hold_distinct_strain` | 1 | 明确应保留不同品系 |
| `hold_coarse_taxonomy` | 2 | scientificName 太粗，不能据此判断实体等价 |
| `hold_taxonomy_conflict` | 1 | taxonomy 本身仍存在冲突，需要独立核对 |

7 个 `probable_alias` 的 full-record parity 结果：

| 检查 | 结果 |
|---|---:|
| 完全等价（排除 id/name/image） | 0 / 7 |
| 只有 narrative drift | 7 / 7 |
| care-parameter conflict | 0 / 7 |

因此下一次真正 dedupe 必须满足：

1. 外部实体证据证明两条确实是同一物种/品系/商业实体；
2. 明确选择 canonical record；
3. 合并 description / specialNotes，不丢掉更完整 caveat；
4. 先建立 stable legacy ID migration；
5. 再删除旧 row；
6. CI 继续保持 0 unreviewed collision。

## 6. PR #31 当前事实

### 已证明的旧 bug

旧 `speciesFitEngine` 会把水草和硬景当成 livestock 继续执行：

- minimum tank volume；
- tank length；
- filter / heater / oxygen；
- predator / prey compatibility；
- livestock density；
- single housing。

回归测试故意给真实植物 / 硬景塞入脏的 fish-style 字段，例如 `至少 800L`、`建议单养`。修复前 Golden Path #137 能稳定得到 `volume_too_small / predation_risk / density_high / single_housing`，证明问题真实存在。

### 修复后的 routing

| Life type | 现在执行的规则 |
|---|---|
| Fish / invertebrate / reptile / coral | 保留现有 animal fit 规则 |
| Aquatic plant | 保留 water type、temperature、植物养护确认；跳过 livestock volume/density/predator/single-housing |
| Hardscape | 不参与 livestock fit；改为材质 / 摆放 / 稳定性确认 |
| Unknown | 不允许静默进入 stocking logic |

`Product Golden Path #140` 已全绿，包括旧的动物控制组和新加的 plant/hardscape adversarial cases。

## 7. 当前真正进行到哪一步

从产品修复路径看，当前已经完成到：

```text
功能是否能用
→ 核心用户路径是否闭环
→ 规则是否有 executable evaluation
→ taxonomy / water-type 是否一致
→ exact duplicate 是否安全迁移
→ scientific-name collision 是否有证据审计
→ plant/hardscape 是否走正确规则
→ 【当前】UI / schema 是否仍把不同 life type 用同一种字段语义展示
→ 外部真实性 / 用户可用性验证
```

因此当前不是“项目还在修基础功能”，而是进入 **准确性系统的最后几层收敛**。现阶段主要风险已经从“按钮点不了 / 页面报错”转成“系统内部有值，但这个值对当前 life type 是否具有正确业务含义”。

## 8. 下一步执行顺序

### P0 — 先处理 PR 队列

1. Review / merge PR #29 到 `main`。
2. #29 合并后，把 #30 和 #31 base retarget 到 `main`。
3. 重新确认两者 diff 只包含各自 scope，并各跑一次 main-base CI。
4. #30/#31 独立 review 后再合并；不要把 phase-2 dedupe 或 UI schema cleanup 塞进这两个 PR。

### P1 — 新开 UI / schema semantics 专项

重点检查：

- Species detail 对植物 `tankSize` 的 label / 展示是否仍然等同“最低缸容”；
- `feedingProfile.recommendedFoods` 对植物是否展示成“推荐食物”，而实际内容可能是“液肥 + CO2”；
- `diet / temperament / housingMode` 是否在 plant / hardscape 页面产生错误语义；
- `getSuitableSpeciesForCurrentTank()` 等推荐调用方是否需要 life-type filter，避免修完 evaluator 后把不相关对象混进“推荐鱼”场景；
- UI、排序、筛选、推荐是否都消费同一 canonical `lifeType`。

原则：**先修字段解释和 rule routing，不用修改几条异常数据来掩盖 schema 问题。**

### P2 — Phase-2 alias evidence

只对 PR #30 的 7 个 `probable_alias` 做逐组外部证据核验。没有足够证据就继续 hold，不追求把 collision 数字清零。

### P3 — 外部门禁

- 测试 Supabase migration / RLS / RPC；
- 真实新手任务可用性；
- 视觉模型配置后的真实照片校准；
- 低端设备 3D 性能；
- 已知损坏源图重新生成与人工验收。

## 9. 当前禁止事项

- 不把 `same scientificName` 当作 `same business entity`。
- 不因为字段看起来重复就直接删除 catalog row。
- 不跳过 legacy ID migration。
- 不通过修改单条植物 `tankSize` 来掩盖 life-type routing / UI semantics 问题。
- 不把 Golden Path 全绿写成“已经上线”；必须以 PR merge + production deployment 为上线依据。
- 不让自由文本 description 直接产生高风险确定性结论。
- 不让 AI 覆盖 deterministic safety / compatibility gate。

## 10. 当前分支 / PR 快速索引

| PR | Branch | Base | 状态 | 最新验证 |
|---|---|---|---|---|
| #29 | `fix/marine-invertebrate-water-type` | `main` | Open / ready / mergeable | Golden Path #131 ✅ |
| #30 | `audit/species-collision-evidence` | `fix/marine-invertebrate-water-type` | Open / draft / mergeable | Golden Path #136 ✅ |
| #31 | `fix/non-animal-species-fit` | `fix/marine-invertebrate-water-type` | Open / draft / mergeable | Golden Path #140 ✅ |

## 11. 下一位接手者第一步

1. 先读本文件，再读根 `HANDOFF.md` 与 `PROGRESS.md`。
2. 查看 PR #29/#30/#31 当前 base/head 是否仍与本文一致。
3. **如果 #29 尚未合并：不要继续在 #30/#31 扩 scope。**
4. 如果继续修复，下一专项从“Species detail / recommendation 的 life-type UI semantics”开始。
5. 新专项必须先写 regression case，再修代码；继续保留“pre-fix 能失败、post-fix 才变绿”的证据链。
