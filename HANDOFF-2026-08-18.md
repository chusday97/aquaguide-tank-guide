# AquaGuide Handoff — 2026-08-18

## 当前主线

项目继续优先 **Functional CTA / Action Completeness**，暂不回到 compatibility breadth 扩张。验收单位不是“存在 onClick / route”，而是：

**Trigger → Execution → Observable result → Persistence（如适用）→ Failure/Retry（如适用）**。

已确认并回归的本轮 Action Completeness badcases：

- `PUI-BC-030`：Collection 成就模块 building surface 伪装成 live CTA — `regression_verified`。
- `PUI-BC-031`：Settings “分享与隐私” building section 伪装成 live task navigation — `regression_verified`。
- `PUI-BC-032`：Care 已实现养护卡预览/复制能力但没有可达入口 — `regression_verified`。
- `PUI-BC-033`：Global Search “查看全部 N 个物种”承诺与实际 effect 不一致 — `regression_verified`。

Compatibility 仍保持原边界：Decision Engine stable under regression；GP-002 covered；Knowledge Coverage limited。7 reviewed species profiles / 4 reviewed pair rules / priority recordable directions 2 / 132。`PUI-BC-025` 仍为 `investigating`，Action Completeness 未完成前不把 breadth evidence 当当前主线。

## PUI-BC-033 — Global Search show-all action closure

### 发现

`/search` 的 autocomplete 会显示 **“查看全部 N 个物种”**，但旧实现的 `onViewAllSpecies` 只执行 `submitValue(draft)`；结果页自身仍固定 `allSpeciesResults.slice(0, 18)`。因此 CTA 可以点击，但并没有履行“查看全部”的 promise。

第一版修复加入显式 `showAllSpecies` state，让点击后不再固定截断 18，并新增 Chromium regression。该回归随即暴露第二层 contract mismatch：

- autocomplete 的 N 来自 `search-suggestions.service` 的 `totalSpeciesMatches`；
- `/search` 结果页使用另一套过滤字段，额外包含 description / translated description；
- 对永久回归关键词“鱼”，Product Golden Path **#598 / run `32101290979`** 明确 FAIL：**advertised=486 / rendered=372**。

因此 #598 不是“测试坏了”，而是证明第一版修复仍然没有做到文案 promise 与实际 effect 同源。

### 最终修复

`src/pages/Search.tsx`：

1. 抽出 `getSpeciesSearchResults(value)` 作为 Search 页实际结果集合的唯一过滤函数；
2. 普通搜索仍保持最多 18 条首屏预览；
3. 用户显式点击“查看全部”后切换到完整 `allSpeciesResults`；
4. query 正常提交、重新编辑、重新选择时重置 `showAllSpecies=false`；
5. autocomplete 的 `totalSpeciesMatches` 改为 `getSpeciesSearchResults(draft).length`，让 CTA 的 N 与 Search 结果使用完全相同的匹配集合。

`verify-search-and-inline-filter.mjs` 新增永久浏览器断言：

- 宽泛 query 必须产生 >18 个物种；
- 读取“查看全部 N 个物种”的 N；
- 点击后 `rendered species cards === advertised N`；
- 页面无 runtime error。

`.github/workflows/product-golden-path.yml` 已把该 regression 作为独立 `Search show-all action regression` 接入，使用 `SEARCH_UI_GROUP=entries`。

### 证据

- Fail-before-final-fix：Product Golden Path **#598 / run `32101290979`** — Search regression FAIL，advertised 486 vs rendered 372。
- Final head：`cd946cecc9faa05df9f7b2b821acd79374d63425`。
- Draft PR：**#88 `Close global search show-all action`**。
- Product Golden Path **#599 / run `32101402354`**：
  - `global search show-all action (372)` PASS；
  - Product evaluation contracts PASS；
  - TypeScript PASS；
  - production build PASS；
  - Care card action regression PASS；
  - GP-001～GP-005 全 PASS；
  - Vercel preview success。

`PUI-BC-033` 已写入 `evaluation/product/badcases.v1.jsonl`，状态为 `regression_verified`。当前 PR 仍为 Draft，尚未 merge main。

## 下一条已确认 candidate — Identify manual search no-op CTA

继续 promise-level audit 时，在 `src/pages/Identify.tsx` 已确认：手动搜索结果把类似 **“共匹配 N 个物种，请继续输入”** 的 overflow affordance 交给 `SearchAutocomplete` 渲染成 button，但 Identify 传入的 `onViewAllSpecies={() => undefined}` 是真实 no-op。

这不是“点击后效果不明显”，而是没有 effect 的 DEAD CTA。下一步处理原则：

1. 先定义它在 Identify 的真实产品意图；
2. 如果这里只想提示用户继续缩小搜索范围，就必须改成 **非交互提示**，不能保留 button affordance；
3. 如果要支持“查看全部”，则必须提供真实完整候选结果并写浏览器回归；
4. 不为了“让按钮有反应”而接一个与识别流程无关的跳转。

该 candidate 尚未登记为 `regression_verified`，也尚未标记 fixed。建议在 PR #88 完成最终 registry/handoff CI 后，作为下一条独立 Action Completeness badcase 处理。

## 下一次继续时的起点

1. 先确认 PR #88 最新 head（包含 PUI-BC-033 badcase + 本 handoff）的 Product Golden Path 最终 PASS，并确认 badcase count 从 31 → 32。
2. PR #88 保持 Draft；未获得显式合并授权前不 merge。
3. 然后从 Identify no-op overflow CTA 开始下一条 fail-before-fix → patch → permanent regression。
4. 继续沿 Aquarium / Care / Identify / Collection / Settings / Search 做 promise-level Action Completeness；不要退化成只扫描 `onClick` 是否存在。