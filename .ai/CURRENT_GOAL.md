# Current Goal

> 初始化日期：2026-08-25。以下内容仅整理自当前 `PROGRESS.md` 与 `HANDOFF.md`，没有新增产品决策。

## 当前目标

完成本地与 GitHub 的单一进度收口：

1. 以用户确认的 `37a8d4d1` 作为唯一视觉基准。
2. 将 RC 的已验证业务能力按语义选择性迁入统一分支。
3. 让本地工作树、GitHub PR、交接文档和预览地址都指向同一 SHA。

## 关键约束

- 视觉 AI 未配置时必须明确回退到手动物种确认，不得表述为识图已启用。
- 不修改既有业务数据、混养规则或 API 契约，除非另有明确任务。
- 远端推送只在关键节点执行；本次初始化不推送。

## 当前状态

`IN_PROGRESS` — 统一分支为 `codex/unified-rc-visual-v1`。完整状态见 `.ai/PROJECT_STATE.json`；PR #140 不是验收线。
