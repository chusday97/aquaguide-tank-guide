import fs from 'node:fs';

const path = 'HANDOFF-2026-08-17.md';
let source = fs.readFileSync(path, 'utf8');

const oldTail = `状态：**investigating**，等待最终 Product Golden Path。\n\nEvidence Batch 2 暴露：engine 过去对所有 reviewed pair rule 都硬编码追加“由两侧资料推断、并非直接配对实验”。这会把 \`basis=pair_rule\` 的直接 predator–prey 实验证据错误降级成间接推断。\n\n修复后按 provenance 分流：\n\n- \`basis=rule_inference\`：继续明确“根据两种生物各自已审核资料推断，并非直接配对实验”；\n- \`basis=pair_rule\`：明确“有直接配对或捕食风险实验支持”，同时追加“实验条件不等于家庭水族箱长期同缸，不外推为已观察到长期同缸捕食”。\n\nGuard \`32037647831\` 已证明 direct Oscar + zebrafish rule 不再带错误 disclaimer，并且 TypeScript / direct pair regression 通过。只有最终完整 Product Golden Path 全绿后，\`PUI-BC-027\` 才能标 \`regression_verified\`。\n\n## 下一步\n\n1. 为 Evidence Batch 2 开 PR，并在**最终 branch head**跑完整 Product Golden Path：product contracts / compatibility coverage / scorecard / session events / research audit / Daily Check / typecheck / build / GP-001～GP-005。\n2. 如果全绿，将 \`PUI-BC-027\` → \`regression_verified\`；\`PUI-BC-025\` 仍保持 \`investigating\`。\n3. 合并前 scope audit：永久 diff 只允许 Handoff、Badcase、compatibility coverage baseline、reviewed evidence、engine provenance、compatibility evidence regression；不得残留临时 patch workflow/script，不得有依赖或 UI 漂移。\n4. 后续在 PostHog 查询 \`compatibility_pair_evaluated\`，优先统计 \`status=insufficient_data\` 的 pairKey；样本不足时明确标注不足，不制造“高频用户需求”。\n5. \`PUI-BC-023\` 继续等待真实 connected blind rerun；没有 workflow dispatch 能力时不伪造证据。\n6. Golden Path 维持 5 / 5 regression maintenance，不再以新增路径数量作为进度指标。`;

const newTail = `状态：**regression_verified**。\n\nEvidence Batch 2 暴露：engine 过去对所有 reviewed pair rule 都硬编码追加“由两侧资料推断、并非直接配对实验”。这会把 \`basis=pair_rule\` 的直接 predator–prey 实验证据错误降级成间接推断。\n\n修复后按 provenance 分流：\n\n- \`basis=rule_inference\`：继续明确“根据两种生物各自已审核资料推断，并非直接配对实验”；\n- \`basis=pair_rule\`：明确“有直接配对或捕食风险实验支持”，同时追加“实验条件不等于家庭水族箱长期同缸，不外推为已观察到长期同缸捕食”。\n\n回归证据：\n\n- Guard \`32037647831\`：exact-anchor provenance patch / direct pair regression / TypeScript PASS；\n- **Product Golden Path #552**（head \`bdac21ffd19aae7d12dbe31bd3aa8a19be83db58\`）：18 features / 108 states / 27 Badcases；地图鱼 + 斑马鱼 = \`not_recommended\`；7 reviewed profiles / 3 pair rules / priority recordable=2；statuses=108 insufficient_data / 22 not_recommended / 2 caution；typecheck / build / GP-001～GP-005 全 PASS；\n- central Badcase registry 已将 \`PUI-BC-027\` 升为 \`regression_verified\`，\`PUI-BC-025\` 保持 \`investigating\`。\n\n## 下一步\n\n1. 在**状态同步后的最终 branch head**再跑一次完整 Product Golden Path；这是 PR #79 的最终 merge gate。\n2. 最终 run 必须继续满足：27 Badcases / 7 reviewed profiles / 3 reviewed pair rules / priority recordable=2 / statuses=108 insufficient_data,22 not_recommended,2 caution / typecheck / build / GP-001～GP-005 全 PASS。\n3. 全绿后做最终 scope audit：永久 diff 只允许 Handoff、Badcase、compatibility coverage baseline、reviewed evidence、engine provenance、compatibility evidence regression；不得残留临时 workflow/script，不得有依赖或 UI 漂移。\n4. 通过后将 PR #79 转 Ready，并用最终 head SHA 做 squash merge。\n5. 合并后 main Handoff 写入 PR #79 merge SHA 与最终 CI；\`PUI-BC-025\` 继续 investigating。\n6. 下一轮优先读取 PostHog \`compatibility_pair_evaluated\`；若样本仍不足，则 fallback research group 转到 \`Channa argus\`。\n7. \`PUI-BC-023\` 继续等待真实 connected blind rerun；没有 workflow dispatch 能力时不伪造证据。`;

const count = source.split(oldTail).length - 1;
if (count !== 1) throw new Error(`Expected one exact final handoff tail, found ${count}`);
source = source.replace(oldTail, newTail);
fs.writeFileSync(path, source);
console.log('Batch 2 final handoff synced to PUI-BC-027 regression_verified');
