# AquaGuide Handoff — 2026-08-17

## 当前判断

### 混养系统

必须拆成三层：

- **Decision Engine：stable under regression**：deterministic / fail-closed；false predation、导航竞争、保存失败、重复写入等高风险问题已有回归保护；缺 reviewed evidence 时继续 `insufficient_data`。
- **GP-002：covered**：搜索物种 → 详情 → 混养 → 风险 → 确认 → 实际入缸 → 数量持久化已有连续 Chromium。
- **Knowledge Coverage：limited**：上一基线为 486 个目录对象 / 411 个 compatibility-eligible species / 5 个 reviewed profile（1.22%）/ 2 条 reviewed pair rule；priority cohort 132 个有向组合只有 2 个 recordable direction（1.52%）。

准确说法：**混养已经做到“宁可不知道，也不乱判”的安全稳定，但还没有做到“大多数常见组合都有充分证据”的知识稳定。**

Compatibility scorecard 已通过 PR #69 合入 main，merge commit `100905afb69b844ec276266a236b381da02ab77b`。`PUI-BC-025` 保持 `investigating`；coverage 只能通过增加 reviewed evidence 扩张，不能降低 `insufficient_data` 门槛。

## Golden Path：5 / 5 covered

- GP-001：covered，PR #70 merge `2978bb550503b6847f5e4583735ad1683e505c46`。
- GP-002：covered，混养连续路径 + evidence coverage + scorecard 持续进入 Product Golden Path CI。
- GP-003：covered，PR #71 merge `fa6e5d79312acf9006305f547a145f3f47264446`；Daily Tank Check 正确入口后的保存/状态闭环已证明，但不替代 connected blind。
- GP-004：covered，PR #72 merge `85c18ae162e67f833215d8c8b69a8d4019425c0e`；Care Quick Check 保持症状排查边界，不写 `problemType='巡检'`。
- GP-005：covered，PR #73 merge `3538fa9368a4c30db0ae85de53696125c6d99640`；桌面 Right Drawer / 手机 Bottom Sheet 关闭后保持 exact 收藏卡焦点与 horizontal rail 位置。

最终 Golden Path formalization run #520：contracts / type check / build / GP-001 / GP-002 / GP-003 / GP-004 / GP-005 全 PASS。

## PUI-BC-023 — Daily Check navigation ambiguity

状态：**open**。

正确入口后的产品闭环已由 GP-003 证明；但原问题是 autonomous actor 是否还会误选 Care Guide Quick Check。当前 GitHub connector 仍没有 workflow dispatch 写接口，因此 connected blind 尚未真实复跑；没有该证据前不得标 `regression_verified`。

## PUI-BC-025 — Compatibility knowledge coverage

状态：**investigating**。

### Evidence Batch 1 — 当前分支

Branch: `agent/compatibility-evidence-batch-1`

本批只扩 reviewed species profile，不新增未经 pair 级证据支持的 compatibility verdict：

1. `sp_0434 / 白云金丝 / Tanichthys albonubes`
   - catalog identity 已与 `fishData` 精确对齐；
   - FishBase 明确 aquarium keeping 为 5+ 个体；
   - Animal Behaviour 有直接 White Cloud Mountain minnow shoaling 研究；
   - profile：`behaviorTraits=['shoaling']`、`minimumGroupSize=5`、`predationTargets=[]`、`confidence='medium'`。
2. `sp_0436 / 孔雀鱼 / Poecilia reticulata`
   - catalog identity 已与 `fishData` 精确对齐；
   - FishBase 明确 aquarium keeping 为 5+ 个体；
   - Animal Behaviour 有直接 guppy schooling / social-environment 研究；
   - profile：`behaviorTraits=['shoaling']`、`minimumGroupSize=5`、`predationTargets=[]`、`confidence='medium'`。

Evidence source 已直接写入 `src/data/compatibilityEvidence.ts`：每个新 profile 都使用 FishBase + 同行评议行为研究，不使用原始 `fishData` 的 Google search URL 作为 reviewed evidence。

`evaluation/product/compatibility-coverage-v1.json` 的 `reviewedSpeciesProfiles` non-regression floor 已从 **5 → 7**；`reviewedPairRules` 仍为 **2**，`minimumRecordablePriorityDirections` 仍为 **2**。

**证据边界：**本批不声称白云金丝 × 孔雀鱼或它们与其他 priority species 已被证明可混养。未有 pair evidence 时仍应 `insufficient_data`。等待 Product Golden Path / scorecard 计算实际 coverage 后再记录新的百分比。

Badcase registry 已同步：PUI-BC-025 继续 `investigating`，只记录 Batch 1 的 profile expansion，不提前关闭。

## 下一步

1. 为 Evidence Batch 1 跑 Product Golden Path；要求 compatibility scorecard 与 GP-001～GP-005 全绿。
2. CI 后记录实际 reviewed profile coverage / priority profile coverage / recordable directions；如果 recordable 仍为 2，这是预期，不通过降低门槛改变。
3. Batch 1 合并后，再按真实 query / usage frequency 选择下一批物种；无真实使用数据时才继续 common-species fallback cohort。
4. `PUI-BC-023` 继续等待真实 connected blind rerun。
