# AquaGuide Handoff — 2026-08-17

## 当前判断

### 混养系统

当前继续拆成三层：

- **Decision Engine：stable under regression**。Evidence Batch 1 暴露的 pair-evidence 语义缺口已修复：species profile 中“没有记录风险”不能再当成 pair-level clearance；没有 reviewed pair rule 时，除非存在更高优先级 hard block，否则 authoritative decision 继续 `insufficient_data`。
- **GP-002：covered**。搜索物种 → 详情 → 混养 → 风险 → 确认 → 实际入缸 → 数量持久化持续通过 Chromium。
- **Knowledge Coverage：仍 limited，但 pair-level reviewed evidence 开始真实增长**。486 个目录对象 / 411 个 compatibility-eligible species / 7 个 reviewed profile / **4 条 reviewed pair rule**；priority cohort 132 个有向组合仍只有 2 个 recordable direction。

准确说法：**混养继续保持“宁可不知道，也不乱判”的安全稳定；reviewed species profile 保持 7，reviewed pair rule 已从 2 增到 4；新增的是有直接 predator–prey 实验证据支持的阻断型 pair，不代表 broad compatibility coverage 已完成。**

## Golden Path：5 / 5 covered

- GP-001：covered，PR #70 merge `2978bb550503b6847f5e4583735ad1683e505c46`。
- GP-002：covered。
- GP-003：covered，PR #71 merge `fa6e5d79312acf9006305f547a145f3f47264446`。
- GP-004：covered，PR #72 merge `85c18ae162e67f833215d8c8b69a8d4019425c0e`。
- GP-005：covered，PR #73 merge `3538fa9368a4c30db0ae85de53696125c6d99640`。

Golden Path formalization run #520：contracts / type check / build / GP-001～GP-005 全 PASS。

## PUI-BC-023 — Daily Check navigation ambiguity

状态：**regression_verified → challenge_expansion**。

Same-case connected Run `32035944562` 已完整 PASS，PUI-BC-023 已通过 EvalPilot actual promoter 进入稳定 Regression `case-regression-pui-bc-023`。当前不再重复 baseline；后续只把新的 coverage candidate 当 candidate，直到真实执行后才计入 verified coverage。

## PUI-BC-025 — Compatibility knowledge coverage

状态：**investigating**。

### Evidence Batch 1 — 已合入 main

PR #76 `Expand reviewed compatibility evidence batch 1` 已 squash merge。

Merge commit: `9f7fed5d02ecbac3c5a32a0289fe13888cbf80b9`

新增 reviewed species profile：

1. `sp_0434 / 白云金丝 / Tanichthys albonubes`
   - FishBase + Animal Behaviour shoaling evidence；
   - `behaviorTraits=['shoaling']`、`minimumGroupSize=5`、`confidence='medium'`。
2. `sp_0436 / 孔雀鱼 / Poecilia reticulata`
   - FishBase + Animal Behaviour schooling/social-environment evidence；
   - `behaviorTraits=['shoaling']`、`minimumGroupSize=5`、`confidence='medium'`。

reviewed profile non-regression floor：**5 → 7**。reviewed pair rule floor仍为 **2**。

最终 scorecard：

- catalog objects：486
- compatibility-eligible species：411
- reviewed species profiles：**7**
- reviewed pair rules：**2**
- catalog profile coverage：**1.44%**
- eligible profile coverage：**1.70%**
- priority reviewed species：**4 / 12 = 33.33%**
- priority directions：132
- recordable priority directions：**2 / 132 = 1.52%**
- `insufficient_data=108 / not_recommended=22 / caution=2`

因此本批只提高 species-profile coverage，没有人为提高 recordable pair coverage。`PUI-BC-025` 仍不能关闭：7 / 411 仍属于明显有限覆盖。

### Evidence Batch 2 prioritization foundation — 已合入 main

PR #78 `Add usage-grounded compatibility evidence prioritization` 已 squash merge。

Merge commit: `bbda961f70f240ea05531d17779e0cba7452b416`

目标不是继续盲目增加 profile 数量，而是建立 **真实需求信号 → evidence backlog**。

PostHog 历史数据已经核实：

- `compatibility_started` 与 `compatibility_check_run` 都没有 species ID / pair ID，历史事件无法可靠反推出用户查的是哪一对生物；
- 过滤测试账号后，最近 30 天只有 `compatibility_started=1`、`compatibility_check_run=7`，当前样本量不足以声称存在稳定的“高频 pair”。

因此采用两层优先级：

1. **Usage signal（未来主优先级）**
   - 已新增 privacy-safe `compatibility_pair_evaluated`；
   - `pairKey` 只允许两个 catalog ID，自动排序为方向无关 key；
   - 不记录 aquarium name/id、用户文本、水参数、AI 输出或诊断内容；
   - `CompatibilityRiskCalculator` 从 authoritative `relevantPairs` 逐 pair 发报，使用每个 pair 自己的 deterministic status；
   - 事件位于原 compatibility calculation 去重边界内，代表一次实际计算会话中的 pair 暴露，不按 React 重渲染刷次数。
2. **Research fallback（usage 样本不足时）**
   - `audit-compatibility-evidence-priority.ts` 只用 catalog aggression / territorial / solitary / large-size / behavior-text / high-consequence-role signals 排人工研究优先级；
   - 已有 deterministic reviewed profile 的物种自动排除；
   - catalog 变体已按基础二名法学名聚类去重，避免同一物种多个品系占满研究配额；
   - queue 只用于人工 research planning，任何 priority score / catalog signal 都不得进入 runtime compatibility verdict。

#549 最终 audit：

- catalog animals：411
- reviewed deterministic profiles：7
- high-signal catalog entries：293
- deduplicated research groups：**168**
- research-only Top 5：
  1. `Astronotus ocellatus / 地图鱼` — score 18，2 个 catalog variants
  2. `Channa argus` — 白金雷龙代表，score 18
  3. `Channa asiatica` — 红眼白子雷龙代表，score 18
  4. `Channa aurantimaculata / 眼镜蛇雷龙` — score 18，3 个 catalog variants
  5. `Channa barca / 巴卡雷龙` — score 18

这些 Top 5 是**研究风险排序，不是用户需求排序**。真实用户优先级必须等 `compatibility_pair_evaluated` 积累到可用样本后再统计。

永久回归：

- `test:session-events` 锁定 pairKey normalization / privacy rejection / self-pair rejection；
- Product Golden Path contract 阶段包含 `test:session-events` + research-only priority audit；
- #549：Product evaluation contracts / compatibility coverage / session telemetry privacy / research queue / Daily Check contract / typecheck / build / GP-001～GP-005 **全部 PASS**；
- 本 PR 没有新增 reviewed profile 或 pair verdict，coverage 仍保持 7 profiles / 2 pair rules / recordable 2 / 132。

**当前证据边界：**telemetry 只是让下一批 evidence 能逐步从“common cohort 猜测”切换到“真实用户查询”；telemetry 本身不计入 knowledge coverage。

### Evidence Batch 2 — direct pair research — 已合入 main

PR #81 `Expand reviewed compatibility evidence batch 2` 已 squash merge 到 main，merge commit：`87251111f467cdd282595e5e07c872779fdd47a5`。最终分支 `agent/compatibility-evidence-batch-2-final` 从包含 PUI-BC-023 Challenge expansion 的 main 重建，因此没有覆盖其 connected/regression/challenge 证据。

首个 fallback research group 为 `sp_0451 / 地图鱼 / Astronotus ocellatus`。本批不新增 Oscar species profile，而是找到 `sp_0451 地图鱼 + sp_0435 斑马鱼 / Danio rerio` 的直接 predator–prey 实验证据并新增 reviewed pair rule：

- verdict：`not_recommended`
- riskType：`predation_threat`
- basis：`pair_rule`
- confidence：`medium`
- reviewStatus：`reviewed`
- 证据限制：实验支持直接 predator–prey / predation-threat 关系，但不是家庭水族箱长期同缸吞食试验，不得外推为“已观察到长期同缸捕食”。

Coverage 边界：reviewed profiles **7 → 7**；reviewed pair rules **2 → 3**；priority recordable directions 继续 **2 / 132**；priority statuses 继续 `108 insufficient_data / 22 not_recommended / 2 caution`。这是阻断型 pair knowledge 扩张，不是放宽 `insufficient_data`。

Compatibility evidence regression 已锁定 pair-rule floor >=3、audit 中必须存在 Oscar–zebrafish direct pair rule、authoritative decision=`not_recommended`、reviewed citations 保留、direct evidence 不得被错误标成“并非直接配对实验”、且必须保留 lab→home-aquarium 限制。

最终 current-main merge-ref Product Golden Path **#567 / run 32039779091** 验证 head `9c499e56d976c2a784db67f6f1f168b6208faf78` + base `9576cb81bb8932e382f42efe0d6e8b10c6a4aa0e`：18 features / 108 states / 27 Badcases；7 reviewed profiles / 3 reviewed pair rules；132 priority directions / recordable=2；statuses=108 `insufficient_data` / 22 `not_recommended` / 2 `caution`；session telemetry privacy、research-only audit、Daily Check、typecheck、build、GP-001～GP-005 全 PASS。

### Evidence Batch 3 — Channa argus × Rhodeus ocellatus direct pair — 已合入 main

PR #82 `Expand reviewed compatibility evidence batch 3` 已 squash merge，merge commit：`19ef8d62f731b1d6a95049359659cafde8de66fe`。最终 Product Golden Path **#572 / run 32042182281** 在 head `95c52fcc61f3252c706294aeb6a5dcda005ab96d` + base `fe82042b0648b43b01bed120d2ca04dca7ec63e9` 上验证：contracts / typecheck / build / GP-001～GP-005 全 PASS。

Usage-first selector 先检查当前 PostHog taxonomy：**尚未观察到 `compatibility_pair_evaluated` 事件**。因此当前仍没有可用于 pair-frequency 排序的真实 usage 样本，本批不得把任何 pair 称为“高频用户需求”。Prospective telemetry 已在产品代码中，但事件未进入当前项目 taxonomy 可能意味着已部署版本尚未产生实际 exposure；在真实事件出现前，usage-driven prioritization 仍不可用，telemetry 本身也不计入 knowledge coverage。

按既定 research-only risk fallback 转到 `Channa argus`。研究阶段主动排除 `Carassius auratus` / `Cyprinus carpio` 作为本批落库对象：AquaGuide 对金鱼/锦鲤按多个观赏品系 ID 建模，复制同一 taxon-level evidence 会虚增 pair-rule coverage，只挂到某一品系又会造成任意性。

本批选择 catalog 唯一映射：`sp_0224 / 白金雷龙 / Channa argus` + `sp_0475 / 高体鳑鲏 / Rhodeus ocellatus`。

- 2015 Entropy exact-species predator–prey experiment：prey=`Rhodeus ocellatus`，predator=`Channa argus`；猎物表现出远离捕食者的行为。
- 2026 Comparative Biochemistry and Physiology Part A：将 `Channa argus` 明确描述为 `Rhodeus ocellatus` 的 natural predator，并进行 20-day 持续视觉/化学捕食压力处理。
- verdict=`not_recommended`；riskType=`predation_threat`；basis=`pair_rule`；confidence=`medium`；reviewStatus=`reviewed`。
- 证据边界：两项实验均为物理隔离/非家庭缸长期同缸条件，因此只支持明确 predation threat，不得改写成“已观察到家庭缸内吞食”。

Guard Run **32041864463** 已 PASS：exact-anchor patch / product registry / compatibility evidence regression / compatibility scorecard / TypeScript 全绿，临时 workflow/script 已自删除。 Handoff sync Run **32042035371** 同样 PASS 并自删除临时 tooling。

Coverage 边界：reviewed profiles **7 → 7**；reviewed pair rules **3 → 4**；scorecard non-regression floor 提升到 profiles=7 / pairRules=4；priority recordable directions 继续 **2 / 132**，本批阻断型 evidence 不放宽任何未审核 pair。`PUI-BC-025` 继续 **investigating**。

## PUI-BC-023 — Daily Check connected closure / Regression

- Connected retest：EvalPilot Run **32035944562**，target AquaGuide **3d73c033b6899e3a92144f6de99a05db8babde78**。
- 同一 EvalCase：`blind-daily-check-risk`。
- 协议：`protocolHealthy=true`；Actor Oracle leak=0；Judge Oracle visible=true；provider/evaluator/unknown failures=0。
- 产品：Hybrid **PASS**，`failureSource=null`；semantic Judge **pass + complete**（0.95）；6/6 deterministic assertions PASS。
- Blind Actor 第一跳直接进入真实 Daily Aquarium Check，完成 6 项观察并保存当天记录，不再误入 Care Guide / Quick Check。
- PUI-BC-024 的端到端保护同时通过：正确 breathing-only summary；错误“水体异常” summary 与 20%-30% 换水动作均未出现。
- EvalPilot commit **2d994e22200215c03a14e999d3288c3c41cdc211** 已通过 actual `promoteFixedBadcaseToRegression()` 生成稳定 Regression **case-regression-pui-bc-023**；CI Run **32038227247** 全绿。
- Issue #66 已按 connected same-case PASS 证据关闭为 completed。
- 边界：这是 AI-blind connected evidence，不外推真人失败率。

### Challenge expansion — candidate generation corrected

- EvalPilot commit **be6a70789f21d1a4b16a218b9b5ace3e5c472bc7** 修复 Challenge generator gap selection：source case coverageDimensions 稀疏时，优先按目标 missingValue 选择 gap，而不是误拿该维度第一个 gap。
- CI Run **32039192110** 全绿：Node 20 / Node 24 / Chromium 均 PASS；新增 `connected-aquaguide-pui-bc-023-challenge.test.ts` 3 / 3 PASS。
- 生成 3 个未执行 Challenge candidates：
  1. `case-challenge-blind-daily-check-risk-boundary` → `input_quality=boundary`；
  2. `case-challenge-blind-daily-check-risk-journey` → `journey_stage=backtrack`；
  3. `case-challenge-blind-daily-check-risk-persona` → `persona=persona-low-patience`。
- 首选下一条：**low-patience persona**。原因是它直接攻击 PUI-BC-023 原始 information-architecture 风险，同时保持已验证 Daily Check 输入与 Oracle 不变。
- boundary / backtrack 暂时只保留 candidate：generic mutation 还不足以构成具体可执行的 Daily-specific interaction contract。
- 最新 connected PASS 的 `observedPreFailureSignals=[]`；因此 Run #10 的 `/api/ai/chat 500` 不升级为本轮首要 Challenge，也不登记新 Product Badcase。
- 当前状态严格为 **candidate_not_run**；没有新 connected execution，就不能声称 Challenge coverage PASS。

### Low-patience persona Challenge — executable harness ready, not run

- EvalPilot runner commit **c833a03b0c44ab331cd8e7eb1e31d5839da712a5** 已让现有 Daily runner 支持显式 `--challenge persona`；默认 Daily retest 路径保持兼容。
- 手动 workflow **Connected AquaGuide Daily Persona Challenge** 已被 GitHub 注册为 active；只允许 `workflow_dispatch`，没有 push / schedule 自动执行。
- workflow 默认仍 pin AquaGuide **3d73c033b6899e3a92144f6de99a05db8babde78**、DeepSeek `deepseek-v4-flash`、`max_steps=12`、screenshots=false、prerequisite remote calls=0；provider timeout 保持现有 45s，不为 Challenge 放宽。
- 显式付费授权串：`RUN_CONNECTED_AQUAGUIDE_DAILY_PERSONA_CHALLENGE`。没有精确授权就必须在 remote-call gate 退出。
- verdict contract：protocol unhealthy 才是 harness failure；healthy + trustworthy Product FAIL 是有效产品评测结果，不得误分类为 evaluator failure；只有 trustworthy PASS 才能把该 Challenge 计入 verified coverage。
- 永久测试 commit **250d2d9c730f552ad84d1af66bafb1aa0e917c19**；CI Run **32040107854** 完整 PASS：Node 20 / Node 24 / Chromium 全绿，persona workflow test 3 / 3 PASS，Chromium real-benchmark / evaluator-regression / browser / runner / dashboard / public-example 全部实际执行。
- 当前仍为 **ready_but_not_executed / candidate_not_run / paid calls=0**。本次 harness 准备不能被写成产品 PASS，也不能更新 PUI-BC-023 的 `regression_verified` badcase 状态。

## PUI-BC-027 — Direct pair evidence provenance mislabeled

状态：**regression_verified**。

engine 过去对所有 reviewed pair rule 都追加“由两侧资料推断、并非直接配对实验”，会把 `basis=pair_rule` 的直接证据错误降级。修复后：`rule_inference` 保留间接推断 disclaimer；`pair_rule` 明确存在直接 pair/predation-risk 实验支持，同时保留“实验条件不等于家庭水族箱长期同缸”的限制。

回归证据：direct-pair guard `32037647831`、Product Golden Path #552，以及最终 current-main merge-ref Product Golden Path **#567 / run 32039779091** 均通过；#567 记录 27 Badcases / 7 profiles / 3 pair rules / priority recordable=2 / statuses 108-22-2，GP-001～GP-005 全 PASS。

## PUI-BC-026 — Sparse species profile incorrectly clears pair evidence

状态：**regression_verified**。

### 发现

Evidence Batch 1 初始版本给孔雀鱼新增 reviewed profile 后，在没有新增 pair rule 的情况下，孔雀鱼 ↔ 红绿灯/宝莲灯 4 个方向从 `insufficient_data` 被提升为 `caution`。根因是稀疏 species profile 中“未记录 predatory / territorial 风险”被错误解释成 pair-level “行为资料已审核无阻断”。

### 修复边界

- reviewed species profile 可支持明确 species-level 阻断；
- profile 未记录某类风险 **不等于** 已证明 pair 中不存在该风险；
- 没有 reviewed pair rule 时，只要没有更高优先级 hard block，authoritative product decision 必须继续 `insufficient_data`；
- hard block 优先级高于 missing pair evidence；
- explicit reviewed pair rule 仍可产生 `compatible / caution / not_recommended`。

`src/modules/knowledge/compatibilityKnowledge.ts` 已在 authoritative `evaluateCompatibilityDecision` 边界加入 `pair_evidence_unreviewed`。

`test:compatibility-evidence-coverage` 锁定：

- 红绿灯 ↔ 宝莲灯 explicit reviewed pair 继续双向 `caution`；
- 孔雀鱼 → 红绿灯保持 `insufficient_data + pair_evidence_unreviewed`；
- 两个 reviewed species profile 没有 pair rule 时不得自动进入 `compatible/caution`；
- hard block 可直接 `not_recommended`；
- recordable pair 不允许残留 high/medium unresolved missing-data；
- Batch 1 的 priority recordable directions 必须继续 **2**。

### CI 证据

- #523：原 Batch 1 全绿但 recordable 2 → 6，拒绝直接合并。
- #524：exact audit 确认新增 4 个孔雀鱼 caution。
- #525：确认 low missing-data 可以和 explicit caution 共存。
- #529/#530：修正 hard-block 优先级的过窄测试假设。
- **#531：修复行为全 PASS**，25 Badcases / 7 reviewed profiles / recordable=2 / GP-001～005 全绿。
- **#533：Badcase registry 已把 PUI-BC-026 标 `regression_verified` 后再次全 PASS**：contracts / type check / build / GP-001～GP-005 均通过。

Scope audit：PR #76 只修改 Handoff、Badcase、coverage baseline、coverage gate、reviewed evidence 和 authoritative compatibility boundary 六个目标文件；无 UI、依赖或其它业务模块漂移。

## PUI-BC-028 — Compatibility evidence fallback repeated research

状态：**regression_verified**。

Evidence Batch 3 后审计 research-only fallback，发现旧 selector 在 base-species 聚类前按单个 catalog ID 排除 reviewed profile，而且完全不知道 reviewed pair rule 已经完成过某个 base species 的直接 evidence research。结果是 `Channa asiatica` 可通过未审核品系重新进入队列，`Astronotus ocellatus` / `Channa argus` 虽已完成 direct-pair research 仍会继续占据 breadth fallback 前列。

PR #83 将 breadth backlog 的“已完成 research group”定义为 **reviewed species profile groups ∪ reviewed pair-rule groups**，再按基础二名法排除整个 group。这个排除只服务 research planning：`runtimeEvidencePropagation=none_research_backlog_only`，不会把某个品系或 pair 的证据外推成其他运行时组合的安全结论。

PR #83 `Fix compatibility evidence fallback group exclusion` 已 squash merge 到 main，merge commit：`df4ad1ccf55545e335c4314d89684a0b9503debd`。最终 exact merge-ref Product Golden Path **#580 / run 32044865900** 验证 final head `85d2d4d0aa76bfce1223ed8996ccc787cbb65bbf` + base `ec94f68c108635699b7e57f31e236c87ad5547fa`：18 features / 108 states / **28 Badcases**；contracts / typecheck / build / GP-001～GP-005 全 PASS。

#577 / run `32044515710` 已验证：completed research groups=11；high-signal catalog entries=286；high-signal research groups **168 → 164**；`Channa asiatica`、`Astronotus ocellatus`、`Channa argus` 均不会重新进入 breadth fallback。新 Top 5 为：

1. `Channa aurantimaculata / 眼镜蛇雷龙` — score 18，3 variants
2. `Channa barca / 巴卡雷龙` — score 18
3. `Channa lorti / 蓝月光雷龙` — score 18
4. `Gymnotus carapo` — score 18
5. `Nimbochromis livingstonii` — score 18

同一 run 中 compatibility coverage 无漂移：7 reviewed profiles / 4 reviewed pair rules / priority recordable 2/132 / statuses=`108 insufficient_data / 22 not_recommended / 2 caution`，contracts / typecheck / build / GP-001～GP-005 全 PASS。

## 下一步

1. **Usage-first 仍优先**：继续查询 PostHog `compatibility_pair_evaluated`；当前 taxonomy 仍未观察到该事件，因此不得声称存在真实高频 pair。
2. 若 usage 样本仍不可用，Batch 4 breadth fallback 从修正后的 Top 1 **`Channa aurantimaculata / 眼镜蛇雷龙`** 开始，但必须优先找 exact-species / pair-level reviewed evidence；找不到就记录 research outcome，不为凑 coverage 生成 verdict。
3. `PUI-BC-025` 继续 `investigating`。7 profiles / 4 pair rules / priority recordable 2/132 仍是 limited knowledge coverage；PUI-BC-028 只修 research backlog 重复研究，不提高 runtime knowledge coverage。
4. PUI-BC-023 保持 regression maintenance / challenge expansion；新的 persona candidate 只有真实 connected run 后才计入 verified coverage。
