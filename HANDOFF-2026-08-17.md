# AquaGuide Handoff — 2026-08-17

## 当前优先级

P0：闭环 `PUI-BC-023`（Daily Tank Check 与 Care Guide Quick Check 的任务语义混淆），并把修复证据回流到 GP-003。

原计划补齐 GP-001 首次建缸连续路径；检查 2026-08-17 最新 Badcase 后发现 `PUI-BC-023` 为 high 且来自两次独立 connected blind run，因此优先级高于单纯 coverage 补齐。GP-001 保留为下一项。

## 已确认问题

- 目标：已有鱼缸用户从 `/aquarium` 完成每日检查并查看风险与下一步动作。
- 正确路径：`Daily Tank Check / Start today’s check` → 巡检问答 → 保存当天记录 → 回到鱼缸后状态更新。
- 错误路径：`Care Guide → Quick Check` → 症状排查结果；这条路径不会写入 `todayDailyCheckRecord`，不能算今日巡检完成。
- Run #7 / #8 都发生同一误路；Run #8 在 protocol/provider/evaluator 健康的情况下仍被独立 Judge 判为 product fail。
- 根因归类：`information_architecture`，不是 Daily Check 功能本身不存在，也不是 provider failure。

## 本分支已完成

Branch: `agent/daily-check-task-separation`
Draft PR: `#67 Separate Daily Tank Check from Care Quick Check`

1. `StatusSummaryCard` 在今日主任务为 `daily_check` 时增加显式任务边界：
   - 中文：每日鱼缸检查会写入今天的巡检记录；养护指南里的快速检查只用于症状排查，不会完成今日巡检。
   - English: Daily Tank Check records today’s aquarium status. Care Guide Quick Check is symptom troubleshooting and does not complete today’s check.
   - DOM contract: `data-daily-check-scope="records-today"`。

2. 新增静态契约回归：
   - `test:daily-check-task-separation`
   - 强制保留 Daily Tank Check → `setDiagnosisIssueType('巡检')` → today record 的语义。
   - 强制 Care Quick Check 不得声明 `records-today`，也不得依赖 `todayDailyCheckRecord`。

3. 新增真实 Chromium 路径：
   - `test:daily-check-task-separation-ui`
   - fixture 为已有可用鱼缸 + 已记录生物 + 今天已换水 + 今日尚未巡检。
   - 验证首页今日任务明确展示记录语义。
   - 点击“开始今日检查”必须打开真实“每日鱼缸检查”任务。
   - 仅打开任务不得提前写入 diagnosis record。
   - Care 侧使用真实 detail deep-link `/care?topic=guide_water_deteriorate` 打开诊断型养护指南；不得声明 `records-today`，也不得把今日巡检标记完成。

4. Product Golden Path CI 已加入两层门禁：
   - contracts 阶段运行 `test:daily-check-task-separation`。
   - preview 启动后运行 `test:daily-check-task-separation-ui`。

5. `evaluation/product/golden-path-v1.json`：
   - `updatedAt` → `2026-08-17`。
   - GP-003 增加上述两条 automation。
   - GP-003 仍保持 `partial`，因为本次只闭环 Daily Check 任务识别，不把整个“老用户回访 → 今日任务 → 完成养护动作”提前宣称 covered。

6. `evaluation/product/badcases.v1.jsonl`：
   - `PUI-BC-023` 从 `open` → `investigating`。
   - `fixedBy` 指向本分支。
   - CI/browser contract 已通过，但原 connected blind case 尚未复跑；复跑前不得标 `regression_verified`。
   - 首轮 CI 暴露新的评测漂移 `PUI-BC-024`；修正后的浏览器路径已连续通过 Product Golden Path run #479 / #481 / #485，`PUI-BC-024` 已为 `regression_verified`。

## 2026-08-17 AquaGuide CI 记录

### Product Golden Path run #478 — 首轮失败，已定性为 evaluation drift

- `npm ci`：PASS。
- Playwright Chromium install：PASS。
- Product evaluation contracts：PASS。
- Type check：PASS。
- Build：PASS。
- Preview start：PASS。
- `test:daily-check-task-separation-ui`：FAIL。
  - Aquarium 侧“今日巡检”前半段已通过。
  - 唯一失败点：用例在 `/care#care-search` 等待“快速检查”可见超时。
  - 定性：evaluation drift，不是 PUI-BC-023 的产品失败证据。
  - 修正：改为 `/care?topic=guide_water_deteriorate` → “开始快速检查”。
- GP-002 continuous browser path：因上一步 fail 被 skip。

### Product Golden Path run #479 — PASS

- install / product contracts / type check / build / preview：PASS。
- `test:daily-check-task-separation-ui`：PASS。
- GP-002 continuous browser path：PASS。

### Product Golden Path run #481 — PASS

包含最新 24 条 Badcase registry 的后续流水线全绿：

- Product evaluation contracts：PASS。
- Type check：PASS。
- Build：PASS。
- Daily Check separation Chromium path：PASS。
- GP-002 continuous browser path：PASS。

### Product Golden Path run #485 — PASS

当前 AquaGuide 分支 head 在 EvalPilot 参数化准备完成前的最新完整验证再次全绿：

- Product evaluation contracts：PASS。
- Type check：PASS。
- Build：PASS。
- Preview：PASS。
- Daily Check separation browser path：PASS。
- GP-002 continuous browser path：PASS。

因此当前产品证据结论：

- `PUI-BC-024`（evaluation drift）=`regression_verified`。
- `PUI-BC-023` 仍保持 `investigating`：代码 contract 与真实 Chromium product path 已连续通过，但原 connected blind case 尚未复跑。

## Branch diff 审计

`main...agent/daily-check-task-separation` 只涉及目标范围文件：

- Product Golden Path workflow
- `HANDOFF-2026-08-17.md`
- product Badcase registry
- Golden Path coverage registry
- `package.json`（仅新增 2 个 test scripts）
- 2 个 Daily Check regression scripts
- `StatusSummaryCard.tsx`

审计结果：没有无关 dependency 版本漂移，没有修改其它产品页面/服务层；产品修复范围保持收敛。

## Connected blind 复跑路径

进一步检查 EvalPilot 后确认：**connected AquaGuide 不依赖 Vercel preview**。connected smoke / 3-run cohort 会在 GitHub Actions 内 checkout AquaGuide，然后直接启动本地 `http://127.0.0.1:3000` 做真实 Blind Actor 评测。

原阻塞是 EvalPilot workflow 把 `TARGET_APP_COMMIT` 和 checkout ref 固定为旧 AquaGuide SHA `8663b469c50605529367daf1b69ac0cd7cfb0cac`，无法精确复测本 PR 修复 commit。

### EvalPilot PR #49 — 已合并

Repo: `chusday97/evalpilot-local`

PR: `#49 Parameterize AquaGuide target commit for connected reruns`

Merge commit: `414ad119f6816744b6ff37f26864c1a3dc7732c8`

最终变更为 4 个文件：

- Connected AquaGuide Blind Smoke workflow
- Connected AquaGuide 3-Run Cohort workflow
- Blind Smoke workflow safety contract test
- 3-Run Cohort workflow contract test

能力与安全边界：

- 新增必填 `target_app_commit`，AquaGuide checkout 与 evidence validation 使用同一精确 SHA。
- 在任何远程模型调用前拒绝非 40 位 lowercase Git SHA。
- 保留显式付费授权字符串、DeepSeek provider/model、no-screenshot、locale、Blind Actor / Judge 边界。
- PR 与 CI 本身没有 dispatch connected paid workflow，没有发生付费模型调用。

### EvalPilot CI #381 — 首轮发现旧契约漂移

- workflow YAML / TypeScript check 正常。
- `npm test` 只有 1 个失败：旧测试仍要求 `TARGET_APP_COMMIT` 必须等于固定旧 SHA。
- 其余 90 个 test files / 426 tests 通过。
- 定性：workflow contract test 未跟随参数化能力更新，不是 runtime regression。
- 修复不是删除断言，而是把契约升级为：
  - `target_app_commit` 必填并有稳定默认值；
  - job env 必须来自 `${{ inputs.target_app_commit }}`；
  - AquaGuide checkout ref 必须来自同一输入；
  - 远程调用前必须验证 `^[0-9a-f]{40}$`；
  - 原 secret / paid authorization / protocol boundary 断言继续保留。

### EvalPilot CI #383 — PASS

修正契约测试后全部验证通过：

- Node 24 verify：PASS（check / npm test / build / package audit / consumer）。
- Node 20.19 verify：PASS（同上）。
- Chromium smoke：PASS。
  - build
  - AI agent
  - semantic verifier
  - real benchmark
  - evaluator regression
  - browser
  - runner
  - dashboard
  - public example

因此 Vercel `build-rate-limit` 已不再构成 PUI-BC-023 的证据阻塞；EvalPilot main 现在具备“按精确 AquaGuide commit 复跑 connected blind / 3-run cohort”的能力。

## 当前验证门

已完成：

1. Product Golden Path 多轮全绿，run #485 再次验证当前产品修复路径。
2. 静态 contract、TypeScript、Vite build 与 Chromium 产品路径均通过。
3. Branch diff 审计通过，无无关 dependency / 产品范围漂移。
4. EvalPilot target commit pin 已参数化、CI #383 全绿并通过 PR #49 合入 main。
5. connected rerun 已不依赖 Vercel，可直接针对精确 AquaGuide repair commit 在 GitHub Actions 内启动本地产品进行盲测。

仍需完成：

6. 用 `PUI-BC-023` 原 connected blind case 再跑至少 1 次；目标是从 `/aquarium` 选择 Daily Tank Check，而非 Care Guide Quick Check，并真正写入今日巡检状态。
7. 只有 connected blind 复跑通过后，把 `PUI-BC-023.status` 升为 `regression_verified`；若仍误路，继续改 IA，而不是调 Judge。
8. 当前没有自动 dispatch 付费 workflow；本轮也没有发生付费模型调用，因此不能把 PUI-BC-023 提前写成已闭环。

## 下一项

`PUI-BC-023` connected blind 闭环后回到 GP-001：补一条连续 Chromium Golden Path，覆盖“首次使用 → 选择建缸 → 创建真实空缸 → 进入 setup → 保存必要参数 → aquariumConfigured=true → 返回鱼缸”，并专门验证创建/保存失败不产生假完成。
