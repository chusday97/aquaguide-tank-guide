# AquaGuide Handoff — 2026-08-17

## 当前优先级

P0：闭环 `PUI-BC-023`（Daily Tank Check 与 Care Guide Quick Check 的任务语义混淆），并把修复证据回流到 GP-003。

原计划补齐 GP-001 首次建缸连续路径；检查 2026-08-17 最新 Badcase 后发现 `PUI-BC-023` 为 `high / open`，且来自两次独立 connected blind run，因此优先级高于单纯 coverage 补齐。GP-001 保留为下一项。

## 已确认问题

- 目标：已有鱼缸用户从 `/aquarium` 完成每日检查并查看风险与下一步动作。
- 正确路径：`Daily Tank Check / Start today’s check` → 巡检问答 → 保存当天记录 → 回到鱼缸后状态更新。
- 错误路径：`Care Guide → Quick Check` → 症状排查结果；这条路径不会写入 `todayDailyCheckRecord`，不能算今日巡检完成。
- Run #7 / #8 都发生同一误路；Run #8 在 protocol/provider/evaluator 健康的情况下仍被独立 Judge 判为 product fail。
- 根因归类：`information_architecture`，不是 Daily Check 功能本身不存在，也不是 provider failure。

## 本分支已完成

Branch: `agent/daily-check-task-separation`
Draft PR: `#67 Separate Daily Tank Check from Care Quick Check`

1. `StatusSummaryCard` 在今日主任务为 `daily_check` 时增加显式任务边界：
   - 中文：每日鱼缸检查会写入今天的巡检记录；养护指南里的快速检查只用于症状排查，不会完成今日巡检。
   - English: Daily Tank Check records today’s aquarium status. Care Guide Quick Check is symptom troubleshooting and does not complete today’s check.
   - DOM contract: `data-daily-check-scope="records-today"`。

2. 新增静态契约回归：
   - `test:daily-check-task-separation`
   - 强制保留 Daily Tank Check → `setDiagnosisIssueType('巡检')` → today record 的语义。
   - 强制 Care Quick Check 不得声明 `records-today`，也不得依赖 `todayDailyCheckRecord`。

3. 新增真实 Chromium 路径：
   - `test:daily-check-task-separation-ui`
   - fixture 为已有可用鱼缸 + 已记录生物 + 今天已换水 + 今日尚未巡检。
   - 验证首页今日任务明确展示记录语义。
   - 点击“开始今日检查”必须打开真实“每日鱼缸检查”任务。
   - 仅打开任务不得提前写入 diagnosis record。
   - Care 侧使用真实 detail deep-link `/care?topic=guide_water_deteriorate` 打开诊断型养护指南；不得声明 `records-today`，也不得把今日巡检标记完成。

4. Product Golden Path CI 已加入两层门禁：
   - contracts 阶段运行 `test:daily-check-task-separation`。
   - preview 启动后运行 `test:daily-check-task-separation-ui`。

5. `evaluation/product/golden-path-v1.json`：
   - `updatedAt` → `2026-08-17`。
   - GP-003 增加上述两条 automation。
   - GP-003 仍保持 `partial`，因为本次只闭环 Daily Check 任务识别，不把整个“老用户回访 → 今日任务 → 完成养护动作”提前宣称 covered。

6. `evaluation/product/badcases.v1.jsonl`：
   - `PUI-BC-023` 从 `open` → `investigating`。
   - `fixedBy` 指向本分支。
   - 明确要求 CI 后仍需原 connected blind case 复跑；复跑前不得标 `regression_verified`。
   - 首轮 CI 暴露新的评测漂移 `PUI-BC-024`：测试错误假设 `/care#care-search` 会直接显示“快速检查”，实际产品契约是先进入具体 diagnosis topic。该用例已改为 `/care?topic=guide_water_deteriorate`，等待下一轮 CI 复验。

## 2026-08-17 CI 记录

### Product Golden Path run #478

- `npm ci`：PASS。
- Playwright Chromium install：PASS。
- Product evaluation contracts：PASS。
  - 18 个功能 / 108 个状态 / 23 个 Badcase（当时尚未登记 PUI-BC-024）。
  - GP contract 仍明确待闭环：GP-001 / GP-003 / GP-004。
  - compatibility evidence：132 个真实方向，recordable=2；未审核组合继续 fail closed。
  - core-flow v1/v2：PASS。
  - `test:daily-check-task-separation`：PASS。
- Type check：PASS。
- Build：PASS。
- Preview start：PASS。
- `test:daily-check-task-separation-ui`：FAIL。
  - Aquarium 侧“今日巡检”前半段已通过：`daily_check` 卡、scope 文案、真实 Daily Tank Check Dialog、打开任务不提前写记录均未触发失败。
  - 唯一失败点：用例在 `/care#care-search` 等待“快速检查”可见超时。
  - 定性：evaluation drift，不是 PUI-BC-023 的产品失败证据。仓库现有 Care 浏览器回归使用 `?topic=<guideId>` 进入详情；诊断型 Quick Check 是 detail-level action。
  - 已修正测试为 `/care?topic=guide_water_deteriorate` → “开始快速检查”。
- GP-002 continuous browser path：因上一步 fail 被 CI skip，下一轮必须恢复验证。

### 外部发布状态

- Vercel check 当前因 `build-rate-limit` 返回 failure；这是账号/部署额度状态，不是本分支 TypeScript 或 Vite build failure。
- 在 Vercel preview 不可用期间，不能把 connected blind rerun 宣称完成。

## 当前验证门

尚未完成闭环，必须依次满足：

1. 修正后的 Product Golden Path GitHub Actions 全绿：product eval / golden path contract / compatibility evidence / task-entry / core-flow / daily-check separation / lint / build / browser regression / GP-002。
2. 检查 branch diff，确认没有无关 dependency 或产品范围漂移。
3. 用 `PUI-BC-023` 原 connected blind case 再跑至少 1 次；目标是从 `/aquarium` 选择 Daily Tank Check，而非 Care Guide Quick Check，并真正写入今日巡检状态。
4. 只有第 3 步通过后，把 `PUI-BC-023.status` 升为 `regression_verified`；若仍误路，保留 open/investigating 并继续改 IA，而不是调 Judge。
5. `PUI-BC-024` 在修正后的 Chromium 回归通过后可升为 `regression_verified`。

## 下一项

`PUI-BC-023` 闭环后回到 GP-001：补一条连续 Chromium Golden Path，覆盖“首次使用 → 选择建缸 → 创建真实空缸 → 进入 setup → 保存必要参数 → aquariumConfigured=true → 返回鱼缸”，并专门验证创建/保存失败不产生假完成。
