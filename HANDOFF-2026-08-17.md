# AquaGuide Handoff — 2026-08-17

## 当前结论

### 混养系统：核心稳定，知识覆盖仍有限

混养必须拆成三层判断：

- **Decision Engine：stable under regression**
  - deterministic / fail-closed。
  - false predation、导航竞争、保存失败、重复写入等高风险问题均已有回归保护。
  - 缺 reviewed evidence 时继续返回 `insufficient_data`，不允许 AI 文案覆盖确定性阻断。

- **Golden Path GP-002：covered**
  - 连续 Chromium 覆盖：搜索物种 → 详情 → 混养 → 风险结果 → 确认 → 实际入缸 → 数量持久化。

- **Knowledge Coverage：limited**
  - catalog objects：486。
  - compatibility-eligible species：411。
  - reviewed species profiles：5，eligible coverage **1.22%**。
  - reviewed pair rules：2。
  - priority cohort：12 个物种 / 132 个有向组合。
  - recordable directions：2，coverage **1.52%**。
  - priority matrix：`caution=2 / not_recommended=22 / insufficient_data=108`。

准确说法：**混养已经做到“宁可不知道，也不乱判”的安全稳定；还没有做到“大多数常见组合都有充分证据”的知识稳定。**

### Compatibility Coverage Scorecard

PR #69 `Add compatibility coverage stability scorecard` 已 squash merge。

Merge commit: `100905afb69b844ec276266a236b381da02ab77b`

已进入 main：

- `evaluation/product/compatibility-coverage-v1.json`
- `scripts/test-compatibility-coverage-scorecard.ts`
- Product Golden Path coverage gate
- `PUI-BC-025 / compatibility / knowledge_coverage / high / investigating`

规则：coverage 只能通过补 reviewed evidence 扩张，不得通过降低 `insufficient_data` 门槛制造更高“可回答率”。

## Golden Path 状态

### GP-001：covered，已合入 main

PR #70 `Close GP-001 first tank setup path` 已 squash merge。

Merge commit: `2978bb550503b6847f5e4583735ad1683e505c46`

连续 Chromium 覆盖：首次进入 `/welcome` → 建立真实空缸 → 确认尺寸/水体/温度仍 unknown → 真实 onboarding setup CTA → 填 60×30×30 / Freshwater / 25°C → 保存 → `aquariumConfigured=true` → onboarding 前进。

关键保护：创建空缸本身不能靠伪默认尺寸/水体/温度制造“配置完成”。

### GP-002：covered

混养连续路径与 compatibility scorecard 持续进入 Product Golden Path CI。

### GP-003：deterministic continuous E2E 已通过，正在收口为 covered

Branch: `agent/gp003-returning-user-daily-check`

Draft PR #71: `Add GP-003 returning user Daily Check path`

真实连续路径：

1. 已有配置完成鱼缸 + 已记录生物 + 今天尚未巡检；
2. `/aquarium` Today primary task 为 `daily_check`；
3. 点击真实“开始今日检查”；
4. 完成 6 个必答巡检问题；
5. 生成检查结果；
6. 通过实际主操作保存今天巡检记录；
7. local persisted state 中出现且仅出现 1 条 `problemType='巡检'`，关联当前 aquarium；
8. 返回鱼缸后 `[data-daily-action='daily_check']` 消失；
9. Daily Tank Check 状态从“今日未检查”前进为“今日已检查”或“建议重新检查”。

Product Golden Path run #503：**PASS**。

- Product evaluation contracts：PASS
- type check：PASS
- build：PASS
- GP-001：PASS
- GP-002：PASS
- GP-003：PASS

前两轮 GP-003 失败均为测试对合法 UI 状态枚举/同名元素定位不完整，不是产品持久化失败：

- Daily Check 结果可能显示“保存今天记录 / 更新今天记录 / 查看补救步骤”；三种主操作都遵循“先保存，后执行可选后续动作”的产品契约。
- 保存后低风险显示“今日已检查”，high/medium/unknown 显示“建议重新检查”；不能把“状态更新”错误等价为单一文案。
- PR #68 新增了另一个同名“每日鱼缸检查”导航项，因此 E2E 最终改为检查业务状态，而不是依赖同名 DOM 父子结构。

当前正在把该路径注册为正式 npm script `test:golden-path-gp003-ui`，并将 Golden Path registry 的 GP-003 `partial → covered`。完成最终 CI 后可合并 PR #71。

### GP-004：partial

下一条真正未 covered 的核心路径：异常 → 快速检查 → 养护指南 → 立即行动。

### GP-005：mostly_covered

收藏滑动 / 详情 / 返回上下文已有较强覆盖，优先级低于 GP-004。

## Badcase 状态

### PUI-BC-023 — Daily Check navigation ambiguity

**仍保持 `open`。**

确定性产品证据已经明显增强：PR #68 的任务语义修复已在 main，GP-003 continuous Chromium 已证明“正确入口 → 完成巡检 → 持久化 → Today 状态前进”闭环成立。

但这仍不能替代原问题的判定条件：EvalPilot connected blind actor 是否还会从 `/aquarium` 错选 `Care Guide → Quick Check`。

因此：

- deterministic E2E PASS ≠ autonomous blind rerun PASS；
- connected blind 未真实 dispatch 前，不得把 PUI-BC-023 改成 `regression_verified`；
- EvalPilot main 已具备精确 `target_app_commit` 的 connected smoke / 3-run cohort 能力，但当前会话的 GitHub connector 没有 `workflow_dispatch` 写接口。

### PUI-BC-025 — Compatibility knowledge coverage

**仍保持 `investigating`。**

Scorecard 已解决“覆盖率不可见”的问题，但 reviewed evidence 本身仍低：411 个 compatibility-eligible species 中仅 5 个有 reviewed profile。后续按真实 compatibility query / 用户需求优先补 evidence，不重写已稳定的核心规则引擎。

## 下一步

1. 完成 PR #71 的 npm script / registry / Handoff 最终 CI，合入 GP-003 covered。
2. 在不伪造 PUI-BC-023 blind evidence 的前提下，转向 **GP-004 异常 → 快速检查 → 养护指南 → 立即行动** continuous E2E。
3. Compatibility 暂不继续改算法；单独建立 reviewed evidence expansion backlog，等有真实 query/usage 数据后按需求频率补知识。
