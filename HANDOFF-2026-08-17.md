# AquaGuide Handoff — 2026-08-17

## 当前判断

### 混养系统

必须拆成三层：

- **Decision Engine：stable under regression，但 Evidence Expansion 暴露出新的 pair-evidence 边界缺口，当前分支正在修复。**
- **GP-002：covered**：搜索物种 → 详情 → 混养 → 风险 → 确认 → 实际入缸 → 数量持久化已有连续 Chromium。
- **Knowledge Coverage：limited**：main 基线为 486 个目录对象 / 411 个 compatibility-eligible species / 5 个 reviewed profile（1.22%）/ 2 条 reviewed pair rule；priority cohort 132 个有向组合只有 2 个 recordable direction（1.52%）。

准确说法仍是：**main 当前混养做到“宁可不知道，也不乱判”的安全稳定；Evidence Batch 1 暴露的 pair-evidence 语义问题必须在新增 profile 合并前修掉，不能让覆盖扩张破坏 fail-closed。**

## Golden Path：5 / 5 covered

- GP-001：covered，PR #70 merge `2978bb550503b6847f5e4583735ad1683e505c46`。
- GP-002：covered。
- GP-003：covered，PR #71 merge `fa6e5d79312acf9006305f547a145f3f47264446`。
- GP-004：covered，PR #72 merge `85c18ae162e67f833215d8c8b69a8d4019425c0e`。
- GP-005：covered，PR #73 merge `3538fa9368a4c30db0ae85de53696125c6d99640`。

Golden Path formalization run #520：contracts / type check / build / GP-001～GP-005 全 PASS。

## PUI-BC-023 — Daily Check navigation ambiguity

状态：**open**。

正确入口后的产品闭环已由 GP-003 证明；但 autonomous actor 是否仍会误选 Care Guide Quick Check 尚未真实复跑。当前 GitHub connector 仍没有 workflow dispatch 写接口，没有 connected blind 证据前不得标 `regression_verified`。

## PUI-BC-025 — Compatibility knowledge coverage

状态：**investigating**。

### Evidence Batch 1

Branch: `agent/compatibility-evidence-batch-1`
Draft PR #76: `Expand reviewed compatibility evidence batch 1`

新增 reviewed species profile：

1. `sp_0434 / 白云金丝 / Tanichthys albonubes`
   - FishBase + Animal Behaviour shoaling evidence；
   - `behaviorTraits=['shoaling']`、`minimumGroupSize=5`、`confidence='medium'`。
2. `sp_0436 / 孔雀鱼 / Poecilia reticulata`
   - FishBase + Animal Behaviour schooling/social-environment evidence；
   - `behaviorTraits=['shoaling']`、`minimumGroupSize=5`、`confidence='medium'`。

reviewed profile non-regression floor：**5 → 7**。reviewed pair rule floor 仍为 **2**。

### CI #523 / #524 暴露的异常

在没有新增 pair rule 的情况下，scorecard 从：

- reviewed profiles：5 → 7
- eligible profile coverage：1.22% → **1.70%**
- priority reviewed species：2/12 → **4/12 = 33.33%**

同时意外把：

- recordable priority directions：2 → **6**
- recordable coverage：1.52% → **4.55%**
- `insufficient_data`：108 → 104
- `caution`：2 → 6

新增 4 个 caution 全来自孔雀鱼 ↔ 红绿灯/宝莲灯两个方向组合；白云金丝没有新增 recordable direction。

这不是本批目标，因此 PR #76 没有直接合并。

## PUI-BC-026 — Sparse species profile incorrectly clears pair evidence

状态：**investigating / 修复中**。

### 发现

源码审计确认：当两个物种都有 reviewed species profile、但没有 explicit reviewed pair rule 时，底层逻辑会把“profile 中没有记录 predatory / territorial 等阻断特征”解释成行为层已审核无阻断，或者在 tank decision 中直接不再产生 behavior missing-data。

这会把**稀疏 species-level evidence 的“未记录风险”错误推断成 pair-level 的“已证明无阻断”**。

Evidence Batch 1 恰好让孔雀鱼同时具备 reviewed profile，于是孔雀鱼 ↔ 两种 reviewed tetra 被从 `insufficient_data` 推成 `caution`，即使没有新增 pair rule。

### 正确边界

- reviewed species profile 可支持明确的 species-level 阻断，例如 predatory / territorial / solitary；
- **不能用 profile 中“没有写风险”作为 pair-level clearance；**
- 没有 reviewed pair rule 时，只要没有更高优先级硬阻断，authoritative product decision 必须继续 `insufficient_data`；
- explicit pair rule 仍可产生 `compatible / caution / not_recommended`；
- hard block（淡海水冲突、明确 predation、领地冲突等）仍优先 `not_recommended`。

### 修复

`src/modules/knowledge/compatibilityKnowledge.ts` 已新增 authoritative `pair_evidence_unreviewed` boundary：

- 两边都有 reviewed profile；
- 没有 reviewed pair rule；
- 且没有更高优先级 `not_recommended`；

则补 medium missing-data `pair_evidence_unreviewed` 并把最终状态压回 `insufficient_data`。

`test:compatibility-evidence-coverage` 已新增回归：

- 红绿灯 ↔ 宝莲灯 explicit pair rule 继续 `caution`；
- 孔雀鱼 → 红绿灯必须 `insufficient_data + pair_evidence_unreviewed`；
- 白云金丝 → 孔雀鱼必须 `insufficient_data + pair_evidence_unreviewed`；
- Batch 1 只增加 species-profile coverage，priority recordable directions 必须继续 **2**；
- recordable pair 不允许残留 high/medium unresolved missing-data。

### CI 记录

- #523：原 Batch 1 全绿，但首次暴露 recordable 2 → 6；因此不作为可合并证据。
- #524：加入 exact recordable-direction audit 后全绿，明确新增 4 个孔雀鱼 caution。
- #525：把所有 missing-data 都禁止后失败于原 explicit tetra pair；确认 low missing-data 可以和 caution 共存，因此测试口径修正为只禁止 high/medium missing。
- 当前等待 pair-evidence boundary 修复后的完整 Product Golden Path 复验。

## Badcase 状态原则

- `PUI-BC-023`：继续 open，等待 connected blind。
- `PUI-BC-025`：继续 investigating，7 个 profile 仍远非广覆盖。
- `PUI-BC-026`：只有在修复后 recordable 回到 explicit pair 的 2 个方向、scorecard/typecheck/build/GP-001～GP-005 全绿后，才能标 `regression_verified`。

## 下一步

1. 跑 pair-evidence boundary 修复后的 Product Golden Path。
2. 确认 profile coverage 提升到 7 / eligible 1.70%，但 recordable priority directions 回到 2。
3. 立即同步 PUI-BC-026 到 Badcase registry；通过后标 `regression_verified`。
4. scope audit 后再决定是否合并 PR #76。
5. `PUI-BC-025` 合并后仍保持 investigating，下一批 evidence 继续按真实 query / usage frequency 选择。
