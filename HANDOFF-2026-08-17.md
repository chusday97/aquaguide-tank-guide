# AquaGuide Handoff — 2026-08-17

## 当前判断

### 混养系统

当前必须拆成三层判断：

- **Decision Engine：stable under regression**：deterministic / fail-closed；false predation、导航竞争、保存失败、重复写入等高风险问题已有回归保护；缺 reviewed evidence 时继续 `insufficient_data`。
- **GP-002：covered**：搜索物种 → 详情 → 混养 → 风险 → 确认 → 实际入缸 → 数量持久化已有连续 Chromium。
- **Knowledge Coverage：limited**：486 个目录对象、411 个 compatibility-eligible species 中只有 5 个 reviewed profile（1.22%），2 条 reviewed pair rule；priority cohort 132 个有向组合只有 2 个 recordable direction（1.52%），`caution=2 / not_recommended=22 / insufficient_data=108`。

准确说法：**混养已经做到“宁可不知道，也不乱判”的安全稳定，但还没有做到“大多数常见组合都有充分证据”的知识稳定。**

Compatibility scorecard 已通过 PR #69 合入 main，merge commit `100905afb69b844ec276266a236b381da02ab77b`。`PUI-BC-025` 继续保持 `investigating`，后续只能通过增加 reviewed evidence 扩 coverage，不通过降低 `insufficient_data` 门槛制造更高可回答率。

## Golden Path

### GP-001：covered

PR #70 已合入 main，merge commit `2978bb550503b6847f5e4583735ad1683e505c46`。

首次用户真实路径已覆盖：welcome → 创建未知参数空缸 → onboarding setup → 填尺寸/水体/温度 → 保存 → `aquariumConfigured=true` → onboarding 前进。

### GP-002：covered

混养连续路径 + evidence coverage + scorecard 持续进入 Product Golden Path CI。

### GP-003：covered

PR #71 已合入 main，merge commit `fa6e5d79312acf9006305f547a145f3f47264446`。

连续路径已覆盖：老用户回访 → Today Daily Check → 完成 6 个必答问题 → 生成结果 → 保存巡检 → exactly one `problemType='巡检'` record → Today `daily_check` 主任务消失 → 状态变为“今日已检查”或“建议重新检查”。

最终 Product Golden Path run #507：contracts / type check / build / GP-001 / GP-002 / GP-003 全 PASS。

注意：该 deterministic E2E 不替代 `PUI-BC-023` 的 autonomous connected blind rerun，因此 `PUI-BC-023` 仍保持 `open`。

### GP-004：covered

PR #72 `Add GP-004 abnormal care path` 已 squash merge。

Merge commit: `85c18ae162e67f833215d8c8b69a8d4019425c0e`

连续路径使用诊断型养护指南 `水质变差怎么办？`：

1. 进入具体养护指南；
2. 第一屏展示“先做快速评测”，主 CTA 为“开始快速检查”；
3. 回答最少 3 个可观察异常问题；
4. “查看处理建议”变为可操作；
5. 结果第一屏出现 actionable non-low risk；
6. `data-care-assessment-next` 直接显示“现在按顺序做”与具体行动；
7. 同屏显示“暂时不要”“处理后复查”和“设置复查时间”；
8. Care Quick Check 不写入 `problemType='巡检'`，继续与 Daily Tank Check 保持状态边界。

Product Golden Path run #509 首次连续 E2E PASS；正式注册 npm script / registry 后的 run #514 再次全绿：

- product evaluation contracts：PASS
- type check：PASS
- build：PASS
- GP-001：PASS
- GP-002：PASS
- GP-003：PASS
- GP-004：PASS

因此 GP-004 已从 `partial → covered`。

### GP-005：mostly_covered

现在是**唯一还不是 `covered` 的 Golden Path**。已有：

- `test:collection-swipe-cards`
- `test:collection-hub-ui`
- `test:responsive-detail-surface`

下一步补连续 E2E：水族册 → 横向滑动 → 打开 exact saved object → 详情 Drawer → 关闭 → 收藏上下文/滚动位置保持。

## Badcase

### PUI-BC-023 — Daily Check navigation ambiguity

状态：**open**。

正确入口后的产品闭环已经由 GP-003 证明；但原问题是 autonomous actor 是否还会误选 Care Guide Quick Check。connected blind 尚未真实 dispatch，因此不得标 `regression_verified`。

### PUI-BC-025 — Compatibility knowledge coverage

状态：**investigating**。

可见性/回归门已建立，知识覆盖本身仍低。后续按真实 compatibility query / usage frequency 补 reviewed evidence，不再重写已稳定规则引擎。

## 下一步

1. 补 GP-005 continuous E2E，使 5 条 Golden Path 全部达到 covered。
2. 保持 `PUI-BC-023` open，直到 connected blind rerun 有真实证据。
3. Compatibility 单独进入 evidence expansion backlog，不继续改核心算法。
