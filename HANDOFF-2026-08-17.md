# AquaGuide Handoff — 2026-08-17

## 当前判断

混养系统需要拆成三层看，不能用一个“稳定/不稳定”概括：

1. **Decision Engine：稳定 under regression**
   - 规则决策已经 deterministic / fail-closed。
   - 已修复 false predation、导航竞争、保存失败、重复写入等高风险问题。
   - 未有 reviewed evidence 的组合继续返回 `insufficient_data`，AI 解释不得覆盖确定性阻断。

2. **Golden Path GP-002：covered**
   - 连续 Chromium 路径已经覆盖：搜索物种 → 详情 → 混养 → 风险结果 → 风险确认 → 实际入缸 → 持久化数量。
   - Product Golden Path CI 永久运行 GP-002 与 evidence gate。

3. **Knowledge Coverage：limited，不可称为广泛稳定**
   - 目录对象：486。
   - reviewed compatibility species profile：5。
   - reviewed pair rule：2。
   - 当前覆盖只能证明少量有证据组合可达，其余组合安全地 fail closed；不能把 CI 绿解释为“大多数混养查询都有可靠答案”。

## 本轮新增

Branch: `agent/compatibility-coverage-scorecard`

### PUI-BC-025

新登记 `PUI-BC-025 / compatibility / knowledge_coverage / high / investigating`：

- 问题不是规则引擎继续乱判，而是知识覆盖极低且过去缺少显式 coverage scorecard。
- 现有 `test:compatibility-evidence-coverage` 只要求 priority matrix 至少 2 个 recordable direction，容易造成“功能已经稳定”的错觉。

### Compatibility Coverage Scorecard v1

新增：

- `evaluation/product/compatibility-coverage-v1.json`
- `scripts/test-compatibility-coverage-scorecard.ts`
- Product Golden Path CI 中新增 scorecard gate。

Scorecard 持续输出：

- catalog objects
- compatibility-eligible species
- reviewed species profiles
- reviewed pair rules
- catalog / eligible profile coverage %
- priority species profile coverage %
- priority matrix recordable direction coverage %
- priority matrix status distribution

回归门只防止已有 evidence / recordable path 倒退，不通过降低 `insufficient_data` 门槛来提高覆盖率。

## 接下来怎么补知识

不建议现在一次性给 486 个物种全部补 profile，也不建议凭主观判断决定“最常见物种”。正确顺序：

1. 优先读取真实 compatibility 查询/搜索/入缸数据，得到用户实际会问的物种与 pair frequency。
2. 如果目前没有足够真实查询数据，临时使用仓库现有 common-species cohort 做 benchmark，但明确它不是用户需求真相。
3. 每个新增 reviewed profile / pair rule 必须带来源；没有足够证据继续 `insufficient_data`。
4. 覆盖率提升后再评估是否需要扩 Golden Path fixture，而不是为了通过测试先改规则。

## 与其它主线的关系

- `PUI-BC-023 Daily Check`：main 已合入 #68 的导航语义修复，但 Badcase registry 仍保持 `open`；原 connected blind case 尚未复跑，因此不能宣称闭环。
- GP-001（首次使用 → 建缸 → 补参数）仍是产品下一条未 covered Golden Path。
- Compatibility 本轮不继续重写算法；先把 coverage visibility 与 evidence expansion 机制固定下来，再回到 GP-001。
