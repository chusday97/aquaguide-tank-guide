# AquaGuide Handoff — 2026-08-17

## 当前判断

### 混养系统

当前必须拆成三层判断：

- **Decision Engine：stable under regression**：deterministic / fail-closed；false predation、导航竞争、保存失败、重复写入等高风险问题已有回归保护；缺 reviewed evidence 时继续 `insufficient_data`。
- **GP-002：covered**：搜索物种 → 详情 → 混养 → 风险 → 确认 → 实际入缸 → 数量持久化已有连续 Chromium。
- **Knowledge Coverage：limited**：486 个目录对象、411 个 compatibility-eligible species 中只有 5 个 reviewed profile（1.22%），2 条 reviewed pair rule；priority cohort 132 个有向组合只有 2 个 recordable direction（1.52%），`caution=2 / not_recommended=22 / insufficient_data=108`。

准确说法：**混养已经做到“宁可不知道，也不乱判”的安全稳定，但还没有做到“大多数常见组合都有充分证据”的知识稳定。**

Compatibility scorecard 已通过 PR #69 合入 main，merge commit `100905afb69b844ec276266a236b381da02ab77b`。`PUI-BC-025` 继续保持 `investigating`；coverage 只能通过增加 reviewed evidence 扩张，不能降低 `insufficient_data` 门槛。

## Golden Path：5 / 5 covered

### GP-001：covered

PR #70 已合入 main，merge commit `2978bb550503b6847f5e4583735ad1683e505c46`。

首次用户真实路径：welcome → 创建未知参数空缸 → onboarding setup → 填尺寸/水体/温度 → 保存 → `aquariumConfigured=true` → onboarding 前进。

### GP-002：covered

混养连续路径 + evidence coverage + compatibility scorecard 持续进入 Product Golden Path CI。

### GP-003：covered

PR #71 已合入 main，merge commit `fa6e5d79312acf9006305f547a145f3f47264446`。

连续路径：老用户回访 → Today Daily Check → 完成 6 个必答问题 → 生成结果 → 保存巡检 → exactly one `problemType='巡检'` record → Today `daily_check` 主任务消失 → 状态变为“今日已检查”或“建议重新检查”。

该 deterministic E2E 不替代 `PUI-BC-023` 的 autonomous connected blind rerun，因此 `PUI-BC-023` 仍保持 `open`。

### GP-004：covered

PR #72 已合入 main，merge commit `85c18ae162e67f833215d8c8b69a8d4019425c0e`。

连续路径：诊断型养护指南 → 第一屏快速评测 → 最少可观察问题 → actionable non-low risk → 第一屏具体行动 / 暂时不要 / 处理后复查 → Care Quick Check 不写 `problemType='巡检'`。

Product Golden Path run #514：contracts / type check / build / GP-001 / GP-002 / GP-003 / GP-004 全 PASS。

### GP-005：covered（PR #73 待最终合并）

Branch: `agent/gp005-collection-context-e2e`

Draft PR #73: `Close GP-005 collection context path`

新增 `scripts/verify-golden-path-collection-context.mjs` 并接入 Product Golden Path workflow。

连续路径同时验证桌面和手机：

1. 从 `/collection` 水族册首页进入 `种草图鉴`；
2. Wishlist horizontal rail 必须可滚动；
3. 先移动到非零 `scrollLeft`；
4. 打开 exact saved object `sp_0002 / 水晶虾`；
5. 桌面必须打开 `data-surface='right-drawer'`；
6. 手机必须打开 `data-surface='bottom-sheet'`；
7. 关闭详情后仍留在 `/collection/wishlist`；
8. exact saved object 的卡片或其原交互元素重新获得焦点；
9. horizontal rail `scrollLeft` 与打开前误差不得超过 3px；
10. 页面不能产生横向 overflow，也不能有 page error。

### GP-005 CI evidence

Product Golden Path run #517：FAIL，仅 GP-005 失败。

- GP-001～GP-004：PASS。
- 失败点：测试错误要求 `document.activeElement.id === article.id`。
- 实际 Dialog 合法地可能把焦点恢复到 exact card 内部的原打开按钮。
- 定性：**evaluation drift，不是产品 Badcase**。

修正后的 run #518：**PASS**。

- product evaluation contracts：PASS
- type check：PASS
- build：PASS
- GP-001：PASS
- GP-002：PASS
- GP-003：PASS
- GP-004：PASS
- GP-005：PASS

因此 `evaluation/product/golden-path-v1.json` 已将 GP-005 `mostly_covered → covered`。目前 5 条核心 Golden Path 全部 `covered`。

## Badcase

### PUI-BC-023 — Daily Check navigation ambiguity

状态：**open**。

正确入口后的产品闭环已经由 GP-003 证明；但原问题是 autonomous actor 是否还会误选 Care Guide Quick Check。connected blind 尚未真实 dispatch，因此不得标 `regression_verified`。

### PUI-BC-025 — Compatibility knowledge coverage

状态：**investigating**。

可见性/回归门已建立，知识覆盖本身仍低：411 个 compatibility-eligible species 只有 5 个 reviewed profile。后续按真实 compatibility query / usage frequency 补 reviewed evidence，不再重写已稳定规则引擎。

本轮 GP-005 首跑失败属于测试焦点契约过严，已在 #518 修正并验证；**不新增产品 Badcase，不改变 PUI-BC-023 / PUI-BC-025 状态。**

## 下一步

核心产品 Golden Path 已完成 5 / 5 covered，下一阶段不应继续为了 coverage 而新增路径。

优先级转为：

1. **PUI-BC-023 connected blind rerun**：使用当前 AquaGuide main 精确 commit，验证 autonomous actor 是否仍把 Daily Tank Check 错选成 Care Quick Check；没有真实 connected 证据前保持 open。
2. **Compatibility Evidence Expansion**：保持规则引擎不动，按真实 compatibility query / usage frequency 扩 reviewed species profile 与 pair rule，让“安全稳定”逐步变成“知识覆盖稳定”。
3. Golden Path 进入 regression maintenance：后续产品改动必须保持 GP-001～GP-005 全绿，不再以新增 Golden Path 数量作为进度指标。
