# AquaGuide Handoff — 2026-08-17

## 当前结论

### 1. 混养系统：核心稳定，知识覆盖仍有限

不能把“混养稳定”理解成所有物种组合都已经能可靠回答。当前应拆成三层：

- **Decision Engine：stable under regression**
  - deterministic / fail-closed。
  - false predation、导航竞争、保存失败、重复写入等高风险问题均已有回归保护。
  - 缺 reviewed evidence 时继续返回 `insufficient_data`；不允许 AI 文案覆盖确定性阻断。

- **Golden Path GP-002：covered**
  - 连续 Chromium 已覆盖：搜索具体物种 → 打开详情 → 进入混养 → 查看风险 → 确认 → 实际入缸 → 数量持久化。
  - Product Golden Path CI 持续运行 GP-002、compatibility evidence 和 coverage scorecard。

- **Knowledge Coverage：limited**
  - catalog objects：486。
  - compatibility-eligible species：411。
  - reviewed species profiles：5，eligible coverage **1.22%**。
  - reviewed pair rules：2。
  - priority cohort：12 个物种、132 个有向组合。
  - recordable directions：2，coverage **1.52%**。
  - priority matrix：`caution=2 / not_recommended=22 / insufficient_data=108`。

因此当前准确说法是：**混养已经做到“宁可不知道，也不乱判”的安全稳定；还没有做到“大多数常见组合都有充分证据”的知识稳定。**

## Compatibility Coverage Scorecard — 已合入 main

PR #69 `Add compatibility coverage stability scorecard` 已 squash merge。

Merge commit: `100905afb69b844ec276266a236b381da02ab77b`

新增：

- `evaluation/product/compatibility-coverage-v1.json`
- `scripts/test-compatibility-coverage-scorecard.ts`
- Product Golden Path coverage gate
- `PUI-BC-025 / compatibility / knowledge_coverage / high / investigating`

规则：

1. reviewed profile / pair rule / recordable path 不得回退；
2. coverage 只能通过补 reviewed evidence 扩张；
3. 不得为了提高覆盖率降低 `insufficient_data` 门槛；
4. 下一批 evidence 优先按真实 compatibility query / 用户需求补；无真实数据时才用 common-species cohort 当临时 benchmark。

Product Golden Path run #490：PASS，包括 scorecard、type check、build、preview 与 GP-002 continuous Chromium。

## GP-001：首次使用 → 建缸 → 补必要参数

原状态：`partial`。

本轮已补连续真实 Chromium E2E，Branch: `agent/gp001-onboarding-e2e`，PR #70 `Close GP-001 first tank setup path`。

连续路径：

1. 从 `/welcome` 真实首次进入；
2. 点击“建立第一个鱼缸”；
3. 创建真实 aquarium draft；
4. 立即验证 draft **没有** dimensions / waterType / targetTemperature，且 `aquariumConfigured=false`；
5. 从真实 `OnboardingTaskCard` 点击“建立或完善鱼缸”；
6. `action=setup` 打开真实“鱼缸设置”，首先进入尺寸任务；
7. 填写 `60 × 30 × 30 cm`；
8. 切到参数，选择淡水，目标温度 `25°C`；
9. 点击真实“保存设置”；
10. 验证 local state 持久化 dimensions / Freshwater / 25°C；
11. 只有保存后 `onboarding.aquariumConfigured=true`；
12. 返回 `/aquarium` 后，新手任务不再停留在“建立或完善鱼缸”。

Product Golden Path run #492：**PASS**。

同时通过：

- product evaluation contracts
- type check
- build
- preview
- GP-001 continuous browser path
- 原 GP-002 continuous browser path

因此 `evaluation/product/golden-path-v1.json` 已在 PR #70 分支更新：

- `updatedAt = 2026-08-17`
- GP-001 `partial → covered`
- GP-002 继续 `covered`

## 当前 Badcase

- `PUI-BC-025`：仍为 `investigating`。Scorecard/可见性问题已解决，但知识覆盖本身仍低；后续通过 evidence expansion 收敛，而不是改算法。
- `PUI-BC-023 Daily Check`：main 已合入 #68 的导航语义修复，但原 connected blind case 尚未复跑，因此 registry 保持 `open`，不提前宣称闭环。

## 下一优先级

当前 Golden Path：

- GP-001：covered（PR #70 待最终合并）
- GP-002：covered
- GP-003：partial
- GP-004：partial
- GP-005：mostly_covered

GP-001 合并后，优先处理 **GP-003 / PUI-BC-023**。但 connected blind 的真实付费 rerun 当前不能由本会话的 GitHub connector 直接 `workflow_dispatch`；在不伪造该证据的前提下，可继续补 GP-003 的 deterministic + Chromium 状态闭环，或转向 GP-004 的异常→快速检查→养护指南连续路径。
