name: Compatibility Stage Risk V1

on:
  pull_request:
    branches:
      - agent/uiux-system-refactor-v1

permissions:
  contents: write

jobs:
  validate-stage-risk:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/result-ux-v1
          fetch-depth: 0

      - name: Guarded life-stage capture UI migration
        shell: bash
        run: |
          python3 scripts/migrate-life-stage-ui.py
          if git diff --quiet -- \
            src/components/aquarium/LivestockBatchCard.tsx \
            src/services/aquarium/species-batches.service.ts \
            src/i18n/index.ts \
            src/pages/Aquarium.tsx; then
            exit 0
          fi
          changed="$(git diff --name-only | sort)"
          expected="$(printf '%s\n' \
            src/components/aquarium/LivestockBatchCard.tsx \
            src/i18n/index.ts \
            src/pages/Aquarium.tsx \
            src/services/aquarium/species-batches.service.ts | sort)"
          test "$changed" = "$expected"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -- \
            src/components/aquarium/LivestockBatchCard.tsx \
            src/services/aquarium/species-batches.service.ts \
            src/i18n/index.ts \
            src/pages/Aquarium.tsx
          git commit -m "Expose life-stage capture across livestock flows"
          git push origin HEAD:agent/result-ux-v1

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Same-species life-stage risk contract
        run: npx tsx scripts/test-same-species-stage-risk.ts

      - name: Life-stage capture UI contract
        run: node scripts/test-life-stage-ui-contract.mjs

      - name: Existing compatibility engine regression
        run: npm run test:compatibility

      - name: Type check
        run: npm run lint

      - name: Production build
        run: npm run build
