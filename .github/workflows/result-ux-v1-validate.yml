name: Result UX V1 Validate

on:
  pull_request:
    branches:
      - agent/result-ux-automation-base

permissions:
  contents: read

jobs:
  result-ux-browser:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/result-ux-v1

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - run: npm ci

      - name: Result UX contract
        run: node scripts/test-result-ux-contract.mjs

      - name: Compatibility engine contract
        run: npm run test:compatibility

      - name: Type check
        run: npm run lint

      - name: Production build
        run: npm run build

      - name: Install Chromium
        run: npx playwright install chromium

      - name: Start preview
        run: |
          npm run preview -- --host 127.0.0.1 --port 4173 > /tmp/aquaguide-result-ux-v1.log 2>&1 &
          for i in $(seq 1 40); do
            if curl -fsS http://127.0.0.1:4173 >/dev/null; then exit 0; fi
            sleep 1
          done
          cat /tmp/aquaguide-result-ux-v1.log
          exit 1

      - name: Diagnosis decision-first regression
        env:
          PREVIEW_URL: http://127.0.0.1:4173
        run: node scripts/verify-golden-path-abnormal-care.mjs

      - name: Compatibility decision-first regression
        env:
          PREVIEW_URL: http://127.0.0.1:4173
        run: node scripts/verify-result-ux-compatibility.mjs

      - name: Compatibility navigation regression
        env:
          PREVIEW_URL: http://127.0.0.1:4173
        run: node scripts/verify-encyclopedia-risk-navigation.mjs

      - name: Upload Result UX evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: aquaguide-result-ux-v1
          path: |
            artifacts/result-ux-diagnosis/**
            artifacts/result-ux-compatibility/**
          if-no-files-found: warn
          retention-days: 7
