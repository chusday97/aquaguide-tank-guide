import catalogSpecies from './catalog.generated.json';

// Product/catalog truth remains in the existing AquaGuide repository.
// `predev` / `prebuild` regenerates this lightweight projection from src/data/fishData.ts.
export { catalogSpecies };
