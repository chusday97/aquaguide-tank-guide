# Species SEO Batch 0 审查清单

日期：2026-08-30
范围：本地 `/species/:slug` 试点页面、现有 Figma `71:204` / `71:205` 结构、交互规则与素材门禁

## 审查结论

这不是“所有项目都通过”的证明，而是本批实施前后可复核的审查清单。主线程已修复列出的用户端问题并通过本地浏览器回归；独立 Critic 线程已完成但没有返回可读正文，因此独立审查仍记为 `evidence gap`，不能记录为独立通过。

| 维度 | 级别 | 证据 / 发现 | 检查方法 | 验收标准 |
|---|---|---|---|---|
| 首屏身份与适配 | 应修复 → 已由主线程复核 | 页面首屏展示中文名、学名、难度、水体、简介和“检查我的鱼缸”；适配结果位于首屏后的第一个主要内容区 | 打开 `/species/sp_0001`，先看 H1、CTA 和 `#tank-fit` | 用户无需阅读后台字段即可知道“是什么”和如何开始检查鱼缸 |
| 重复与层级 | 应修复 → 已由主线程复核 | 环境展开内容不再重复温度/pH/最低缸体；适配主结果只保留一个结果卡；FAQ 仍为空 | 检查页面文本重复次数与展开项 | 核心参数只负责 Product Truth，低频细节才进入展开区 |
| Base / Variant | 应修复 → 已由主线程复核 | Variant 通过查询参数切换，补有 Base 面包屑；页面不生成第二份 Base 文章；已知重复 `sp_0027` 不出现在公开卡片 | 打开 `?variant=sp_0030`，检查 H1、面包屑和 Variant 卡片 | Variant 只表达当前对象身份/未来差异入口，不复制共享正文 |
| 图片失败 | 应修复 → 已由主线程复核 | Hero 失败时仍保留布局，并显示“极火虾图片暂不可用”等用户可理解文本；原始图片错误不透出技术信息 | Playwright 拦截 `sp_0001.png` 后观察首屏 | 图片失败可见、可理解、不会显示后台审核术语 |
| 无鱼缸 | 已覆盖 | 显示通用要求与创建鱼缸入口 | 使用空 `aquarium_app_state_v1` 打开页面 | 用户可理解尚未完成比较，并能进入创建鱼缸 |
| 单鱼缸 / 多鱼缸 | 已覆盖 | 有鱼缸显示确定性结果，多鱼缸显示可操作选择器 | 使用单缸与双缸本地状态分别打开页面 | 结果来自现有规则引擎，切换鱼缸不改写物种 Product Truth |
| 资料不足 / 加载失败 | 应修复 → 已由主线程复核 | 适配引擎的 `unknown` 显示“资料不足”；读取异常显示友好错误与重试；加载过程为骨架 | 观察 `#tank-fit` 的 loading/error/unknown 状态 | 每个状态有可见原因和下一步，不显示原始异常 |
| 390 / 600 / 1440 顺序 | 应修复 → 已由主线程复核 | 移动端将任务内容排在装饰图片前；桌面恢复图文双列；纵向 IA 不变 | Playwright 在三个视口读取 H1、图片与分区顺序 | 无横向溢出，移动端先读身份，桌面/移动分区顺序一致 |
| 键盘、焦点、44px | 已覆盖 | 按钮、链接、select 使用可见 focus ring 与至少 44px 最小高度；状态不依赖颜色 alone | DOM 尺寸扫描 + 键盘 Tab 检查 | 交互控件可聚焦、可识别、可操作 |
| Alt | 待人工确认 | 代码按试点清单提供中文 Alt 与英文草稿，但素材状态仍为 `needs_review` | 对照 `src/data/speciesLandingPilot.ts` 和素材审核表 | 人工确认视觉内容后才能将素材改为 `approved` |

## 独立审查证据边界

- Figwright ping：`ok: true`，`hop: e2e`，server/leader `0.4.0`。
- 已读取 Figma `71:204` / `71:205` 的结构；其中仍含 `Index decision`、`editorial review pending` 等内部说明，不能直接作为用户页面文案。
- 独立 Critic task `01a05275-2b85-76f2-9e84-98bbb04617d5` 完成时 `items: []`，没有可读审查正文；因此不把它写成“独立审查通过”。
- 第二次同线程复核与独立短复核 task `01a05280-e87f-72b3-876f-1e740267e011` 同样完成但无可读正文；独立审查门禁继续保持未关闭。
- 主线程证据：`npm run lint`、`npm run build`、`npm run test:species-landing` 均通过；构建仍有既有大 chunk / dynamic-static import 警告，不属于本批功能失败。

## 当前剩余问题

1. `sp_0001` 与 `sp_0030` 的图片、Alt 和 Variant 差异仍待人工事实/视觉审核；状态保持 `needs_review`。
2. Intro、喂养、维护、FAQ 没有已发布编辑证据；页面保持简短缺失状态，不生成 structured data。
3. 需要再次获得可读的独立 Critic 报告后，才可关闭 Batch 0 的独立证据缺口。
