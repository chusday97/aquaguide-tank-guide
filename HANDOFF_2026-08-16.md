# AquaGuide 当前交接补充 — 2026-08-16

> 本文补充 `HANDOFF_2026-08-14.md`。重点不是重复 PR 状态，而是记录 2026-08-16 对 **混养、推荐、鱼缸诊断、库外物种与专业信息解释** 的最新产品与架构结论。历史 PR 状态仍以 2026-08-14 handoff 及各 PR 当前 head 为准。

## 1. 当前产品判断

AquaGuide 当前已经有三个相对独立、但尚未完全闭环的能力：

1. `tankCompatibilityEngine`：回答“候选物种能不能加入当前鱼缸”；
2. `recommendationService`：从 catalog/speciesPool 中筛选和排序候选，并支持空缸方案、建议数量和添加模拟；
3. Care Quick Diagnosis：根据鱼缸事实 + 用户观察回答“当前鱼缸是否可能存在问题”。

当前真正缺的不是再加一个 AI 文案层，而是把它们收敛成统一的决策链：

```text
Aquarium facts
+ Livestock facts
+ User observations
+ Reviewed species / pair evidence
        ↓
Safety / Compatibility verdict
        ↓
Cause / affected species
        ↓
Fixability
        ↓
Action / alternative / intervention
        ↓
Re-evaluate aquarium
```

核心产品原则应保持：**AI 可以解释，但不能覆盖 deterministic safety / compatibility verdict。**

---

## 2. “查看风险与替代方案”当前存在语义过度承诺

当前物种详情在 `unsuitable / conflictRisk / caution` 情况下可以显示“查看风险与替代方案”，但当前 handler 的真实行为主要是把候选送入 compatibility calculator；它没有真正生成“同需求、风险更低”的替代物种列表。

因此现状应定义为：

- “解释风险”：部分完成；
- “指出具体冲突物种”：pairwise evidence 已有基础，但 UI/action 仍不完整；
- “告诉用户怎么调整”：部分完成；
- “生成真正替代方案”：未完成。

在 Replacement Recommendation Engine 上线前，UI 不应把 calculator 跳转包装成已经存在“替代方案”。

### Replacement Recommendation Engine 的正确职责

不能做“全库兼容度最高 Top 3”，而要保留用户原始意图：

```text
被否决候选
  ↓
识别原始饲养意图 / role
  ↓
同类候选池
  ↓
hard safety filters
  ↓
对每个候选调用现有 compatibility engine
  ↓
按 role similarity + evidence quality 排序
  ↓
推荐 / 有条件候选 / 信息不足 / 无安全替代
```

例：用户想养“小型群游鱼”，不能因为苹果螺兼容就把苹果螺当替代方案。

更重要的是，如果当前风险来源是“大型捕食鱼”，系统可能遍历全部同类小型群游鱼后仍全部被捕食规则阻断。此时正确输出是：

> 暂未找到真正解决问题的同类替代。风险来自当前群落结构，而不是原候选这个具体品种。

“没有安全替代”必须被允许成为合法结果，不能为了填满三张卡片硬推候选。

---

## 3. 混养结果必须从“鱼缸里有风险”升级成具体关系

对新手来说：

> 当前鱼缸已有大型掠食鱼，存在吞食该小型鱼的风险。

仍然不够具体。

目标结果应显式包含：

```text
A → B
risk_type: predation
source_species: A
affected_species: B
severity: blocker/high
basis: reviewed evidence / deterministic rule
```

用户需要知道：

- 谁是风险来源；
- 谁是受影响对象；
- 为什么；
- 这是参数可调整问题，还是生物学硬冲突；
- 如果已经同缸，分别移出 A / B 会发生什么。

### Action Engine 目标语义

```text
Verdict → Cause → Fixability → Action
```

至少分：

1. 可调整环境问题：温度、设备、部分空间配置；
2. 不可通过参数解决的生物学冲突：捕食、硬性领地冲突等；
3. 信息不足：明确要求补哪些事实；
4. 容量/群体数量问题：减少候选数量、升级环境、停止添加或寻找同角色替代。

---

## 4. 已经同缸时需要 Intervention Simulation，而不是只给“移出某条鱼”

对已经存在于鱼缸中的冲突，系统不应该替用户做“卖掉/送走哪条鱼”的价值决定，而应做可解释的状态模拟：

```text
current community
  ↓
simulateWithout(A)
simulateWithout(B)
  ↓
重新运行 whole-community compatibility
  ↓
比较 blocker/high/medium 风险数量与剩余问题
```

用户结果示例：

- 当前：2 个 High；
- 移出 A：0 个 High；
- 移出 B：仍有 1 个 High。

系统可以说：

> 从“最少改动解除高风险”的角度，A 是当前关键冲突节点。

但不能直接说：

> 你必须移出 A。

如果用户账户里有多个鱼缸，下一层应把被移出的物种对其他鱼缸逐个跑 compatibility，给“已有鱼缸安置可能性”；没有合适鱼缸则说明该物种所需环境，而不是替用户决定购买、出售或送养。

---

## 5. 当前 Recommendation System 的真实定位

当前 `recommendationService` 已经不是简单硬编码列表，而是一个规则驱动 contextual recommender：

```text
Aquarium Profile
  ↓
Species Pool
  ↓
Species Fit + Tank Compatibility
  ↓
load/group constraints
  ↓
direct / adjustable / blocked
  ↓
fitScore / load / quantity 排序
```

已使用的信息包括：

- 鱼缸尺寸、估算水量；
- 水体、目标温度；
- 过滤、加热、增氧；
- 当前 livestock 与数量；
- 估算 load；
- 水层；
- 群体数量 heuristic；
- `evaluateSpeciesForAquarium`；
- `evaluateTankCompatibility`。

它最强的能力目前是：**排除明显错误和构造安全候选池。**

它还不能证明：

> Top 3 就是用户真正最适合的 Top 3。

原因包括：

- 排序权重为启发式规则，不是线上数据学习所得；
- `UserPreference` 尚未成为核心 ranking 信号；
- bioload 参数是产品启发式，不是科学精确容量模型；
- reviewed behavior/pair evidence 覆盖仍有限；
- 目前没有 Precision@K、acceptance rate、添加后异常率等真实效果评测。

下一阶段推荐架构应明确成：

```text
Safety → Intent → Evidence Quality → Ranking
```

---

## 6. 库外物种：数据库不知道，不等于用户不能记录

### 6.1 正式推荐必须 Catalog Grounded

核心 Recommendation Service 从 `speciesPool` 取候选，原则上不应凭空产生库外 species。

但 AI 对话、视觉识别、自由输入可能提出 catalog 中不存在的名字，因此必须有 `Catalog Resolver / Grounding Gate`：

```text
candidate text
   ↓
resolve canonical species_id
   ├─ found → 进入正式 compatibility/recommendation
   └─ not found → external/unresolved candidate
```

未能解析 canonical species_id 的对象：

- 不能显示“可加入”；
- 不能显示高置信度兼容；
- 不能生成伪精确推荐数量；
- 不能伪装成 AquaGuide 已验证推荐。

可以显示：

> 扩展候选 / 尚未收录 / 暂无法完成完整鱼缸适配判断。

### 6.2 现实已经存在的库外物种必须允许记录

`record_existing` 是事实记录，不应因为 catalog 不完整而拒绝用户。

建议引入物种解析状态：

- `verified`：正式 catalog species；
- `pending`：身份已有较强候选，但专业资料尚未审核完整；
- `unresolved`：用户记录存在，但身份/证据不足。

未解析 livestock 仍必须计入“鱼缸存在未知生物”的事实，但不能被 compatibility 静默忽略。

当鱼缸中存在 unresolved species 时，新候选判断应允许返回 `INSUFFICIENT_DATA`，并明确：

> 当前已有 X 个未确认物种，因此无法完成全部混养/负载判断。

### 6.3 Missing Species Queue 可以成为数据飞轮

```text
用户搜索未命中
→ unresolved record
→ Missing Species Queue 聚合需求
→ 高需求物种优先补证据/补 catalog
→ canonical mapping
→ 历史 unresolved records 自动升级
```

这比试图一次性把所有物种补全更可持续。

---

## 7. 专业信息解释：绝不能把“多少升 = 多少条”做成通用换算公式

用户需要知道专业指标“对我意味着什么”，而不是看到更多数字。

当前推荐模块中存在三个容易被混淆的概念，UI 必须拆开：

### A. Estimated Water Volume

例如 60×30×35cm 几何容积约 63L，系统可能按占用折算成约 54L。

这是**估算水体/空间事实**。

### B. Load Budget / Load Pressure

过滤、换水、增氧等会影响系统对饲养压力的估算，但不会把 54L 物理水量“变成 64L”。

因此 `effectiveVolumeLiters` 这类命名在产品语义上存在风险。应考虑改成：

- `estimatedWaterVolumeLiters`
- `loadCapacity` / `loadBudget`
- `loadPoints`
- `loadPressure`

用户界面优先显示“低 / 中 / 偏高”，数字只作为解释层或内部比较，不应包装为科学精确测量。

### C. Behavioral / Spatial Requirement

鱼的适配不能只用总水量换算条数，还要看：

- 成体尺寸；
- 群体数量；
- 活动层；
- 缸长/底面积；
- 领地；
- 已有物种；
- 生物行为和证据质量。

因此用户看到“建议 6 条”时，需要同时看到：

> 为什么建议群养；当前缸能否承受一个合理群体；这个数量是 reviewed evidence、catalog 数据还是 heuristic。

统一解释格式建议：

```text
结论
→ 为什么
→ 这对你的鱼缸意味着什么
→ 依据 / confidence / missing evidence
```

---

## 8. Care Diagnosis 与 Compatibility 还需要真正合流

Care Quick Diagnosis 已经使用：

- aquarium facts；
- livestock；
- user observation；
- deterministic symptom rules。

Compatibility 已经有 pair-level risk/evidence 基础。

目前问题是：Care 看到“打架”时通常仍给较粗的“领地/空间竞争”建议，没有完整调用 pair compatibility graph 来说明 A 与 B 的具体证据关系。

下一版应形成：

```text
Observation: aggression
       +
Compatibility Graph
       ↓
A ↔ B / A → B specific conflict
       ↓
Immediate safety action
       ↓
Long-term intervention options
```

这才是统一 Tank Diagnosis。

---

## 9. 新的产品真实性原则

从本轮洞察开始，后续所有新增功能应遵守：

1. **Database unknown ≠ user fact does not exist.**
2. **AI knows a name ≠ AquaGuide has verified evidence.**
3. **A number can be computed ≠ it is scientifically precise.**
4. **Compatible ≠ guaranteed safe.**
5. **No suitable alternative is a valid recommendation result.**
6. **Existing reality must be recordable even when unsafe or unknown; planned additions remain safety-gated.**
7. **User-facing professional explanations must state what the number means, not only display the number.**
8. **Recommendations must preserve user intent, not only maximize internal compatibility score.**

---

## 10. 推荐的下一阶段边界

全局上仍先关闭 Phase-0 的事实链路/登录跨设备验收，再进入 Recommendation v2；不要把新推荐能力塞进仍未收口的 Phase-0 PR。

Recommendation v2 的实现顺序应是：

1. Catalog Grounding + unresolved species data contract；
2. Professional Explanation Semantics（water volume / load / group requirement）；
3. Evidence quality 进入推荐输出与排序；
4. Replacement Recommendation Engine MVP；
5. Compatibility Graph + Action Engine；
6. Intervention Simulation / 多鱼缸安置；
7. Care Diagnosis 与 compatibility graph 合流；
8. Golden cases + 真实用户 acceptance/negative feedback metrics。

原则：**先建立不会“胡说”的边界，再增强“会推荐”的能力。**
