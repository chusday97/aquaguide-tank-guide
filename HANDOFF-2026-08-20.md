# AquaGuide Handoff — 2026-08-20

## 当前结论

AquaGuide 目前已经不是“功能缺失很多”的阶段，而是进入 **产品可信性 + UI/UX 完成度 + 行为闭环审计** 阶段。

截至本 handoff：

- GitHub `main` 最新提交：`ed0cf38025652db901ee81aa697ca55b1c1584b6`（2026-08-18）。
- 2026-08-18 之后当前仓库没有新的 main commit，因此 8/19～8/20 新讨论的 UI/UX 方向仍应视为 **待执行产品要求**，不能写成已实现。
- Product Golden Path 仍为 **GP-001～GP-005 = 5/5 covered**。
- Functional CTA Audit 已完成到 v3；最新正式验证为 Product Golden Path #594 / run `32053031556`，31 个 Badcases、typecheck、build、GP-001～GP-005 全 PASS。
- 当前核心问题已经从“按钮有没有 onClick”升级为：**每个入口是否与用户理解一致、是否真的完成业务动作、是否有可见结果、是否持久化、失败后是否能恢复。**

最重要的产品判断：**自动化 regression PASS ≠ UI/UX 已完成。** 已有 browser contract 只能证明交互没有回退，不能代替真实用户对视觉层级、信息密度、可发现性、操作流畅度的验收。

---

## 1. 当前稳定基线

### 1.1 Golden Path

五条核心路径继续保持 covered：

1. GP-001：首个鱼缸建立路径。
2. GP-002：搜索物种 → 详情 → 混养 → 风险确认 → 实际入缸 → 数量持久化。
3. GP-003：核心养护路径。
4. GP-004：核心状态/任务路径。
5. GP-005：水族册上下文恢复路径。

后续任何 UI 改版都不能以“只是视觉调整”为理由绕过 Golden Path；特别是 Drawer、详情卡、收藏卡、任务 CTA、返回路径的改动必须继续跑真实浏览器回归。

### 1.2 核心事实链路

当前仍坚持以下业务边界：

- “现实中已经在缸里”和“未来准备养”必须分开。
- `record_existing`：现实事实先保存，再展示风险；高风险不能删除用户现实记录。
- `planned_addition`：先评估，不写入鱼缸；只有用户明确确认已经实际入缸后才进入事实记录。
- 页面不能伪造尺寸、水量、温度、设备、换水等鱼缸事实。
- 新增/修改核心鱼缸事实继续通过 Repository 命令，不允许页面直接绕过持久化层。

### 1.3 Daily Check / Timeline / 循环养护

- Daily Check 的负向答案误判 badcase 已进入 regression。
- 同鱼缸、同本地日期继续采用 upsert 语义。
- 保存失败必须保留用户已完成的检查结果，并提供重试。
- 换水记录以 `waterChangeHistory` 为唯一事实源。
- 鱼缸时间线已覆盖建缸、设置、加减生物、体态、换水、喂食、巡检和计划完成。
- 循环养护的本地闭环已验证；真实 Supabase 原子事务仍属于外部环境验证项。

---

## 2. Functional CTA Audit 当前状态

项目已经从“按钮存在”审计升级到：

`Trigger → Execution → Observable Result → Persistence → Failure / Retry`

### 已关闭并进入 regression 的 badcase

#### PUI-BC-030 — 成就模块假 CTA

- “成就勋章”仍是 building feature，却使用 live button + 前进箭头。
- 已改为纯信息模块，不再伪装成可执行业务动作。
- `/collection/achievements` 仍可作为建设中说明深链。
- 状态：`regression_verified`。

#### PUI-BC-031 — Settings 分享与隐私假导航

- “分享与隐私”是 building feature，但左侧设置导航曾表现得像 live setting。
- 已改为非交互 building row + 显式“建设中”。
- 只有“了解功能”保留为说明性 meta action。
- 状态：`regression_verified`。

#### PUI-BC-032 — Care 卡片能力存在但不可达

- 页面已有 `CareShareCardPreview`、本地文案生成和复制逻辑，但没有入口把当前 topic 送进现有能力。
- 已新增“生成养护卡”入口，只开放真实存在的本地预览 + 复制文字能力。
- 明确不生成公开分享链接；完整 Sharing & Privacy 仍是 building。
- 已接入永久 browser regression。
- 状态：`regression_verified`。

### 继续审计原则

后续 Aquarium / Care / Identify / Collection / Settings / Encyclopedia 中，只要出现以下情况，都继续登记为候选 badcase：

- 点击后无可观察结果；
- 文案承诺与实际 effect 不一致；
- building 功能伪装成 live CTA；
- UI 显示成功但没有持久化；
- 保存失败后状态丢失；
- 进入大页面而不是具体任务起点；
- 返回后丢失用户上下文；
- 条件式 CTA 依赖测试 fixture，导致测试证明的是 fixture 而不是业务语义。

---

## 3. 混养 Decision Engine / Knowledge Coverage

### 当前可信边界

Decision Engine 继续执行 fail-closed：

- 没有 reviewed pair evidence 时，不允许仅凭物种 profile 中“没记录风险”推导兼容。
- 高优先级 hard block 可以覆盖 missing pair evidence。
- Predator identity 不能从自由文本中猜测，只允许结构化 aggressive / large / explicit predator signals。
- `insufficient_data` 是正常安全状态，不应为了“看起来更智能”被强行减少。

### 当前 coverage

保持 2026-08-17 已验证边界：

- catalog objects：486
- compatibility-eligible species：411
- reviewed species profiles：7
- reviewed pair rules：4
- priority directions：132
- recordable priority directions：2 / 132
- priority statuses：108 `insufficient_data` / 22 `not_recommended` / 2 `caution`

`PUI-BC-025` 继续 `investigating`。

当前不能说“混养知识库已经覆盖常见组合”；准确表述只能是：**决策逻辑稳定，但知识 coverage 仍非常有限。**

### Usage-first evidence priority

- 已有 privacy-safe `compatibility_pair_evaluated` 设计。
- 历史 PostHog 事件缺少 species / pair ID，因此不能反推真实高频组合。
- 截至 8/17 taxonomy 仍未观察到新的 pair telemetry，故当前不能声称已经有 usage-driven research priority。
- 没有 usage 数据时，research-only fallback 只能作为人工研究排序，不能进入 runtime verdict。

---

## 4. 水族册 UI/UX：继续作为当前高优先级，而不是“已完成”

### 用户当前要求

水族册需要进一步做成类似 IceGlide 的 **横向轮播 / swipe card 浏览体验**，并对整个项目继续做 UI/UX 质量审计。

旧 contract 已经存在以下规则：

- 水族册首页模块预览、种草收藏、养护收藏使用横向 snap card track；
- 手机端要露出下一张卡，形成可滑动 affordance；
- 桌面支持触控板 / 横向滚动；
- Card = Open object；收藏、添加、删除、选择必须是独立 control；
- 生命纪念保留独立浏览结构；
- 成就继续保持 building，不展示假进度。

**但这只能说明产品规则已经定义、部分 regression 已存在，不代表实际 UI 已达到目标。**

### 新 handoff 的验收口径

水族册这一轮不能再用“测试通过”直接标 Done，需要同时满足：

1. **视觉层级**：用户第一眼能分辨“模块 → 卡片 → 状态 → 主动作”，不能像后台列表。
2. **滑动可发现性**：手机首屏必须自然露出下一卡；不能只有隐藏滚动条。
3. **节奏**：卡片宽度、间距、标题长度、图片比例需要形成稳定的浏览节拍。
4. **上下文连续性**：打开详情 → 返回时恢复原模块、原横向位置、原 focus。
5. **动作隔离**：点击卡片打开对象；收藏/删除/选择不能误触打开详情。
6. **信息密度**：卡片只保留做选择所需的信息，详细解释进入详情 surface。
7. **响应式**：390px 手机、600px 窄桌面、1280px 桌面至少各验证一次真实浏览器行为。
8. **一致性**：水族册的轮播不能与首页推荐、图鉴卡、Care 收藏形成四套完全不同的交互语言。

### 下一轮 UI/UX audit 重点

优先检查：

- 水族册首页是否仍然“像模块堆叠页面”，而不是可浏览内容空间；
- 轮播是否只有 CSS 横滚，而缺少视觉 affordance；
- 卡片是否承载过多文字；
- Drawer / detail surface 是否遮挡主任务；
- 返回路径是否恢复 carousel position；
- 手机是否需要过多纵向滚动才能发现第二类收藏；
- 卡片之间是否缺少 active / hover / pressed / focus 状态；
- 空状态、单卡状态、2 卡状态、多卡状态是否都成立；
- 删除收藏后轨道是否跳位；
- 从收藏进入详情再切换到“添加到鱼缸/混养”时，task intent 是否丢失。

不要只改视觉；每次 UI 调整都要明确它解决的是：**发现、理解、决策、操作、反馈、恢复** 中的哪一个问题。

---

## 5. 当前仍不能宣称完成的外部门禁

### Supabase

真实 Supabase migration / RLS / RPC 仍未完成生产级实证。现阶段只能说本地契约、mock/preview、浏览器路径通过，不能说云端数据链路已完全验证。

### 养护内容证据

41 篇内容约 228 条动作此前已绑定候选来源，但没有可验证的显式专家审核登记时，继续保持 `pending review`。

不能因为“有引用”就自动标为已审核。

### 真人 UX / 产品效果

当前没有足够真人 evidence 支持以下结论：

- 新用户激活率提升；
- TTV 缩短；
- D1 / D7 留存提升；
- 水族册轮播更容易使用；
- 用户能更快找到混养 / 换水 / 每日检查；
- 视觉识别准确率达到可上线阈值。

这些仍需真人 task-based evaluation 或真实产品数据。

---

## 6. 当前优先级

### P0 — UI/UX 产品完成度审计

优先重新审视整个产品，而不是继续补功能：

1. 水族册 carousel / swipe browsing；
2. 首页任务优先级与信息密度；
3. Aquarium 核心任务入口是否直接；
4. Care 详情首屏是否先给结论和行动；
5. Identify 的识别结果 / 健康分诊 / 混养三个任务是否仍有混淆；
6. Drawer / Dialog / detail surface 的尺寸与层级；
7. 返回、恢复、重新进入任务的上下文连续性；
8. 全站 loading / empty / error / retry / building 状态一致性。

验收必须同时包含：产品逻辑检查 + 真实 Chromium 行为 + 人眼 UI 评估。

### P1 — Functional CTA Audit v4

继续逐模块检查剩余 affordance，重点不是扫描 onClick，而是逐个验证业务 promise。

### P1 — Compatibility usage telemetry

先确认线上是否真正出现 `compatibility_pair_evaluated`。没有 usage 样本就继续明确使用 research fallback，不假装是“用户高频需求”。

### P2 — Supabase 实环境验证

真实 migration / RLS / RPC / retry / idempotency 需要在授权环境中验证。

### P2 — 内容专家 review

228 条 care action evidence 只有显式 reviewer / reviewedAt / sourceIds 后才能进入 reviewed 状态。

---

## 7. 下一位 Agent 的工作方式

开始任何新修复前：

1. 先读取本文件和 `HANDOFF-2026-08-17.md`。
2. 检查 GitHub main 是否仍为 `ed0cf380...`；如果已经有新提交，先以最新代码为准，不照抄本 handoff 状态。
3. 不要因为旧 regression 写着 PASS 就假设 UI 已被用户接受。
4. UI/UX 问题先形成可复现 task / viewport / expected behavior，再修改代码。
5. 新 badcase 必须先 fail-before-fix，再修复，再进入 regression；不能先写“已解决”再找证据。
6. building feature 不得表现为 live action。
7. 用户事实与 AI 建议继续分层，安全规则不得由自由文本推断覆盖。
8. 不为追求 coverage 数字扩大混养结论。
9. 不将自动化测试结果包装成真人 UX evidence。

---

## 8. 推荐的下一次执行顺序

### Step A — 水族册真实 UI 审计

对 `/collection` 在 390 / 600 / 1280px 做实际浏览器评测，记录：

- 首屏看到什么；
- 是否知道可以横向滑；
- 每个模块需要多少滚动；
- 打开详情后返回是否恢复位置；
- 收藏/删除是否造成误触；
- 文字是否过密；
- 是否与 IceGlide 的 carousel browsing 体验仍有明显差距。

### Step B — 只修一个最强 badcase

优先挑一个真实影响最大的 UX failure，完成：

`fail-before-fix → patch → deterministic/browser regression → handoff/badcase/progress sync`

不要一轮同时大改所有页面。

### Step C — 再做全站 UI/UX scorecard

建议维度：

- Discoverability
- Information hierarchy
- Task continuity
- Interaction feedback
- Error recovery
- Mobile ergonomics
- Visual consistency
- Trust / evidence clarity

每个维度都必须给 evidence，而不是只给主观分数。

---

## 当前一句话状态

**AquaGuide 的核心事实链路、5 条 Golden Path 和主要 CTA 语义已经相对稳定；当前真正的瓶颈不再是“有没有功能”，而是 UI/UX 是否达到可理解、可发现、可连续操作的产品完成度，以及混养知识、Supabase、养护内容审核是否有足够真实证据。下一轮优先从水族册 carousel + 全站 UI/UX 审计切入。**
