# AquaGuide Badcases — Recommendation / Catalog / Diagnosis — 2026-08-16

> 目标：把本轮产品洞察转成可执行的 regression / golden cases。每条 badcase 都包含“失败表现 → 正确行为 → 建议门禁”。这些案例不是 UI 文案清单，而是后续 Recommendation v2 / Diagnosis v2 的验收合同。

## 使用原则

- `record_existing` 与 `planned_addition` 分开：现实事实永远允许记录；计划行为继续走安全门禁。
- `unknown` / `unresolved` / `insufficient_data` 是合法状态，不得被 fallback 成“看起来合理”的确定答案。
- AI 解释不能覆盖 deterministic safety verdict。
- 没有安全替代时必须允许返回空结果。
- 用户看到的专业数字必须解释语义和不确定性。

---

## CATALOG-001 — AI 推荐了 catalog 中不存在的物种

**场景**

AI 对话或其他生成层提出“物种 X”，但当前 `speciesPool/catalog` 无 canonical `species_id`。

**错误表现**

- 显示“推荐加入”；
- 直接进入 compatibility；
- 显示兼容分数、推荐数量；
- 写入正式 catalog-like livestock record。

**正确行为**

- Catalog Resolver 返回 `unresolved/external_candidate`；
- UI 明确“当前尚未收录 / 无法完成完整适配判断”；
- 禁止高置信度 `compatible`、推荐数量和 direct-add；
- 可提供“记录为待确认 / 申请收录 / 补充身份信息”。

**门禁**

任何 user-facing “推荐加入”对象必须存在 canonical `species_id`。

---

## CATALOG-002 — 用户已经养了库外物种，但系统不让记录

**场景**

用户现实鱼缸里已经有“黄金胡子 ×2”，catalog 搜不到。

**错误表现**

- 必须先选 catalog species 才能保存；
- 无结果时用户无法完成鱼缸建档。

**正确行为**

- `record_existing` 允许创建 `unresolved livestock`；
- 保存用户原始名称、数量、可选照片/备注；
- 不伪造 scientificName / care parameters；
- 后续可映射到 canonical species。

**门禁**

Catalog coverage 不得成为记录现实鱼缸事实的前置条件。

---

## CATALOG-003 — 鱼缸中有 unresolved species，但推荐系统把它当不存在

**场景**

鱼缸包含已确认物种 + 2 个 unresolved livestock，用户计划加入候选 Y。

**错误表现**

系统只对已确认物种计算，然后返回“兼容 / 可直接加入”。

**正确行为**

- 已确认部分继续计算；
- unresolved livestock 明确进入 missing-evidence 状态；
- 结果至少降级为 `INSUFFICIENT_DATA` 或等价“无法完成全部判断”；
- 告诉用户缺口来自哪几个未确认物种。

**门禁**

未知生物不能被 compatibility 静默忽略。

---

## CATALOG-004 — 视觉识别低置信度结果被直接绑定正式 species

**场景**

照片识别输出“可能是 X”，置信度不足或 catalog 无精确匹配。

**错误表现**

自动写入 canonical `species_id`，随后生成完整 care/compatibility 建议。

**正确行为**

- 显示候选身份；
- 用户确认后才能绑定；
- catalog 未命中时进入 `pending/unresolved`；
- 未确认前不做强安全结论。

---

## REC-001 — “查看风险与替代方案”实际上没有替代方案

**场景**

候选被判定 `not_recommended`，用户点击“查看风险与替代方案”。

**错误表现**

只把当前候选加入 calculator 并跳转，却没有任何 replacement candidates。

**正确行为**

在 Replacement Engine 上线前：

- CTA 改成与真实行为一致的“查看详细风险 / 打开兼容性分析”；

Replacement Engine 上线后：

- 展示同用户意图候选；
- 每个候选都重新跑当前鱼缸 compatibility；
- 明确推荐 / 有条件 / 信息不足。

**门禁**

CTA 文案不得承诺不存在的产品能力。

---

## REC-002 — 替代推荐只追求“最兼容”，丢失用户原始意图

**场景**

用户原本想养“小型群游鱼”，候选被阻断。

**错误表现**

系统推荐苹果螺、清洁虾等完全不同角色，只因为兼容风险低。

**正确行为**

Replacement Intent 至少包含：

- life type；
- role/function；
- social mode；
- size tier；
- difficulty preference（若已知）。

先保证同类意图，再做安全筛选和排序。

---

## REC-003 — 捕食鱼在缸里，系统不断推荐另一种小鱼

**场景**

当前已有大型捕食鱼，用户想找小型群游鱼；原候选因捕食风险被拒绝。

**错误表现**

替代系统换一个小型群游物种继续推荐，产生同样 blocker。

**正确行为**

- 对所有替代候选跑 compatibility；
- 同类候选全部被 blocker 后返回空；
- 解释“问题来自当前群落结构，而不是原候选品种”；
- 提供“保留当前群落 / 调整当前群落”的下一步分支。

**门禁**

“没有安全同类替代”必须是合法结果。

---

## REC-004 — evidence 不足的候选被包装成“高置信推荐”

**场景**

候选环境参数匹配，但 reviewed behavior/pair evidence 缺失。

**错误表现**

显示“推荐 / 高置信 / 安全混养”。

**正确行为**

- 环境适配与行为证据质量分开；
- 可显示“环境条件匹配”；
- 行为证据不足时标记“需要进一步确认 / Medium/Unknown”；
- 不得使用 guaranteed-safe 语义。

---

## REC-005 — 排名分数被用户理解为科学概率

**场景**

内部 `fitScore` 为 87。

**错误表现**

UI 显示“87% 适配 / 87% 安全”。

**正确行为**

- 分数只用于内部排序；
- 用户显示“更适合当前鱼缸 / 有条件 / 信息不足”；
- 解释关键 passed / warning / blocker evidence。

---

## MIX-001 — “鱼缸里有大型掠食鱼”但不说是谁

**场景**

A 对 B 存在 predation blocker。

**错误表现**

只显示：

> 当前鱼缸已有大型掠食鱼，存在吞食该小型鱼的风险。

**正确行为**

结果必须绑定具体关系：

```text
A → B
risk: predation
source: A
affected: B
basis/evidence
```

用户明确知道谁威胁谁。

---

## MIX-002 — 所有 blocker 都统一建议“移除风险或更换候选”

**场景**

blocker 可能来自温度、容量、捕食、领地、群体数量等完全不同原因。

**错误表现**

统一动作：

> 先移除阻断风险或更换候选生物。

**正确行为**

先分类 `fixability`：

- adjustable environment；
- biological hard conflict；
- capacity/group constraint；
- insufficient data。

再给不同动作。

---

## MIX-003 — 已经同缸，只告诉用户“移出 A”，不解释后果

**场景**

A/B/C 已经在缸里发生冲突。

**错误表现**

单点建议“移出 A”，没有重新计算鱼缸，也不说明 A 去哪。

**正确行为**

至少支持：

- simulateWithout(A)；
- simulateWithout(B)；
- 比较 high/blocker 风险变化；
- 如果存在其他用户鱼缸，逐缸评估安置；
- 无合适鱼缸时说明物种所需环境，不替用户决定买/卖/送养。

---

## DIAG-001 — 用户报告“打架”，Care 只给泛化领地建议

**场景**

用户观察到 aggression，同时 compatibility graph 已有 A/B 具体风险 evidence。

**错误表现**

Care 只说“可能是领地或空间竞争，增加躲避”。

**正确行为**

- Observation 与 pair compatibility evidence 合流；
- 输出具体 A ↔ B / A → B；
- 立即安全动作和长期动作分开；
- 如果 evidence 不足，明确要求观察/补充信息。

---

## EXPLAIN-001 — “有效容量”让用户以为过滤器增加了真实水量

**场景**

60×30×35cm 鱼缸估算水量约 54L，安装桶滤后内部 capacity multiplier 提高。

**错误表现**

UI 或字段语义表现成：

> 有效容量 64L

让用户理解成实际水体变多。

**正确行为**

分开：

- `estimatedWaterVolumeLiters`：估算实际水体；
- `loadBudget/loadCapacity`：系统用于比较饲养压力的启发式预算；
- `loadPressure`：低/中/偏高。

**门禁**

过滤、增氧、换水不得改变用户看到的“真实水量”。

---

## EXPLAIN-002 — “54L 能养 10 条鱼”被当成通用规则

**场景**

用户问“多少升水养多少条鱼”。

**错误表现**

用固定 `L/fish` 比例给出通用结论。

**正确行为**

解释水量只是限制条件之一，同时考虑：

- 物种；
- 成体尺寸；
- 群体需求；
- 缸长/底面积；
- 行为/领地；
- 已有 livestock；
- 过滤与维护；
- evidence quality。

用户结果优先给“当前组合的压力与原因”，而不是通用换算公式。

---

## EXPLAIN-003 — “建议 6 条”没有说明 6 从哪里来

**场景**

系统给群游鱼建议数量 6。

**错误表现**

只显示“建议 6 条”，用户会把 heuristic 当作严格科学下限。

**正确行为**

显示数量依据等级：

- reviewed minimum group size；
- catalog structured data；
- heuristic fallback。

如果只是 heuristic，应使用“建议群养 / 约 X 条起，需进一步确认”而不是“科学最低 6 条”。

---

## EXPLAIN-004 — 用户只看到负载百分比，不知道是什么意思

**场景**

内部计算 `loadRate = 73%`。

**错误表现**

UI 只显示“负载 73%”。

**正确行为**

用户首先看到：

> 当前饲养压力：中等/偏高

再解释：

- 该指标用于比较当前组合相对压力；
- 不代表使用了 73% 的真实水；
- 主要受哪些因素影响；
- 加入候选后会怎样变化。

---

## FACT-001 — 缺失数据被默认成“常见值”后继续推荐

**场景**

鱼缸没有 water type / dimensions / target temperature。

**错误表现**

默认 Freshwater 或默认 60×40×40，再产生 direct recommendations。

**正确行为**

- 保持 `Unknown`；
- `needsMoreInfo = true`；
- 不产生 direct-add recommendation；
- 明确要求补充关键数据。

PR #29 已对 water type / dimensions 方向做了修复，后续 recommendation/diagnosis 必须保持同一语义。

---

## FACT-002 — 已经存在的不安全事实因为规则阻断而无法保存

**场景**

用户说“A 已经在缸里”，但 compatibility 为 `not_recommended`。

**错误表现**

因为系统认为不安全而拒绝记录现实状态。

**正确行为**

- `record_existing`：保存事实 + urgent warning；
- `planned_addition`：仍然 block；
- 诊断系统随后基于真实鱼缸继续帮助用户处理风险。

---

## EVAL-001 — 只验证“有结果”，不验证严重风险 false negative

**场景**

Recommendation/Compatibility 测试只检查函数正常返回。

**错误表现**

重大捕食/水体冲突被遗漏仍可 CI 绿。

**正确行为**

Golden set 至少包含：

- freshwater vs saltwater hard mismatch；
- predator → small fish；
- reviewed pair blocker；
- unresolved livestock；
- group minimum + capacity；
- no-safe-alternative；
- evidence-insufficient；
- plant/hardscape 不进入 livestock stocking rules。

**首要指标**

严重安全风险 false-negative target：0（在受控 golden cases 内）。

---

## EVAL-002 — 推荐效果只有工程测试，没有真实用户效果指标

**场景**

Golden Path 全绿，但不知道用户是否认为推荐有用。

**错误表现**

把“CI 通过”写成“推荐准确率高”。

**正确行为**

后续建立至少：

- recommendation impression → detail click；
- save/wishlist；
- simulate-add；
- confirmed planned addition；
- dismiss / not relevant；
- “这个推荐不适合我的鱼缸”负反馈；
- 添加后一段时间的 conflict/diagnosis signal（只作为弱监督，不能直接当因果）。

工程 correctness 与产品 usefulness 分开报告。

---

# 建议测试优先级

## P0 Safety contract

- CATALOG-003
- FACT-001
- FACT-002
- MIX-001
- MIX-002
- EVAL-001

## P1 Recommendation honesty

- REC-001
- REC-003
- REC-004
- REC-005
- EXPLAIN-001
- EXPLAIN-003

## P2 New capability

- CATALOG-001 / 002 / 004
- REC-002
- MIX-003
- DIAG-001
- EXPLAIN-002 / 004

## P3 Product effectiveness

- EVAL-002

---

# Exit criteria for Recommendation v2 MVP

Recommendation v2 不以“页面做完”为完成标准。MVP 至少满足：

1. 正式推荐 100% canonical catalog grounded；
2. unresolved existing livestock 可记录且不会被静默忽略；
3. safety blocker 优先于 ranking；
4. 替代候选保留用户原始 role/intention；
5. 无安全替代时返回空，不硬凑；
6. 用户看得到核心依据和 evidence quality；
7. water volume / load budget / group requirement 的解释语义分离；
8. golden set 中严重风险 false negative = 0；
9. CTA 与真实能力一致；
10. 工程 correctness 与真实用户效果指标分开追踪。
